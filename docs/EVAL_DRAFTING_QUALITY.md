# Drafting quality eval — DRY-RUN smoke output

**Important:** this report was produced with `--dry-run`. The LLM was NOT called, no scoring was performed. Any number appearing as `0.0/5` below is an artefact of skipping the LLM step, not a real quality measurement. Re-run `python -m caseops_api.scripts.eval_drafting_quality` without the `--dry-run` flag to produce a real rating against the **4.8/5** PG-005 target.

Scenarios queued: 2
- `bail` / `bnss-303-simple-theft`
- `writ_petition` / `mandamus-rti-inaction`