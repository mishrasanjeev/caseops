# IPLF-037B Repository Evidence - 2026-08-22

## Exact implementation revision

- Revision: `b96bbeff2496b0ebdddc86889e1499cc873a1048`
- Branch: `codex/iplf037b-renewal-workflow-20260822`
- Environment: local SQLite API/component/browser fixtures
- Schema boundary: no schema change; IPLF-037A tables and all existing adjacent owners remain canonical

## Implemented behavior

- Added a tenant- and ethical-wall-scoped renewal portfolio projection with due, instructed,
  filing-in-progress, filed, registry-accepted, grace, overdue, completed, and cancelled counts.
- Kept recorded renewal state separate from date-derived grace/overdue reporting. The projection
  flags reconciliation instead of silently mutating legal state.
- Exposed confirmed deadline dates, calculation explanation, rule citation, source version, linked
  fee/quote evidence, and payment/billing reconciliation state.
- Added idempotent instruction notification scheduling from active primary/backup deadline
  assignments through `notification_delivery_intents` only. Immediate in-app delivery and future
  offsets use the deployed canonical notification worker contract.
- Added a separately typed no-instruction escalation for the active supervisor assignment and proved
  that it is queued idempotently with the ordinary reminder series.
- Cancelled queued no-instruction reminders when an instruction is received or filing/terminal
  progression makes them inapplicable.
- Preserved append-only, versioned client instruction history. An accepted revision never rewinds an
  already advanced renewal term or increments its workflow version.
- Added the `/app/ip/renewals` portfolio/workspace for instruction capture and review, explicit
  grace/overdue transitions, filing initiation, confirmed filing, registry acceptance, certificate,
  and next-term completion evidence.
- Added IP navigation, user-guide coverage, law-firm landing-page coverage, and regenerated
  OpenAPI client types.

## Journey proof

| Path | Proof | Result |
| --- | --- | --- |
| `UJ-26-NORMAL` | Real Playwright tenant journey schedules reminders, records and accepts instruction, records filing initiation, links filing and registry acceptance, then completes only with accepted certificate and confirmed next term | Passed |
| `UJ-26-EXC-01` | API test delivers the immediate reminder, retains future queued reminders and the supervisor no-instruction escalation idempotently, and proves queued intents are cancelled when instruction arrives | Passed |
| `UJ-26-EXC-02` | API and Playwright tests project a due term into grace without a silent write, show recorded state separately, and require an explicit reasoned grace transition | Passed |
| `UJ-26-EXC-03` | Existing evidence-gate contract and browser journey prove provider initiation remains `filing_in_progress`; filing, acceptance, and completion require separate canonical evidence | Passed |

## Commands and results

- `uv run --project apps/api ruff check ...` - passed.
- `uv run --project apps/api pytest -q apps/api/tests/test_ip_renewals.py apps/api/tests/test_openapi_quality.py apps/api/tests/test_route_coverage_matrix.py` - 9 passed.
- `uv run --project apps/api pytest -q apps/api/tests/test_ip_renewals.py apps/api/tests/test_notification_convergence_20260804.py apps/api/tests/test_ip_program_manifest.py apps/api/tests/test_ip_ownership_ledger.py apps/api/tests/test_ip_m2_ownership_audit.py` - 48 passed.
- `npm test --workspace apps/web` - 133 files and 695 tests passed.
- `npm run typecheck --workspace apps/web` - passed.
- `npm run build --workspace apps/web` - passed; `/app/ip/renewals` included in the production route manifest.
- `npx playwright test tests/e2e/iplf-037b-renewal-workflow-2026-08-22.spec.ts --reporter=list` - 1 passed, including 390x844 overflow assertion.
- `git diff --check` - passed.

## Ownership and rollback

- `ip_renewal_terms` and `ip_client_instructions` remain written only by
  `apps/api/src/caseops_api/services/ip_renewals.py`.
- Legal dates remain in `ip_deadlines`; legal events remain in `ip_docket_events`; fee evidence
  remains in `ip_cost_items`; certificate evidence remains in `ip_documents`; notifications remain
  in `notification_delivery_intents`; audit remains in the shared audit owner.
- No migration, background scheduler, provider adapter, report table, task table, or duplicate
  renewal persistence was added.
- Rollback is code-only: revert the feature revision. Existing IPLF-037A records and canonical
  notification rows remain valid; no destructive data rollback is required.

## Release boundary

Repository implementation and local verification are complete. Release remains blocked pending
exact-head CI/PostgreSQL/security/CodeQL, merge to `main`, canonical deployment, runtime identity,
production health, and dated production acceptance on the merged revision.
