# Other-IP Source-Cost Workflow Handoff V2

Frozen for parent review on 2026-09-10. This is an integration checkpoint,
not a release, activation, complete UJ-44 journey or completion of IPLF-090A/B
and 091A/B. All four slices remain PARTIAL. Do not wait for future domains to
review this bounded increment. No parent integration has been performed here.

## Immutable Predecessor

`../other-ip-resume-2026-09-10/owned-files-final.json` remains unchanged:
SHA-256 `571f1978e684c01bbb676c119c46fe5eca2f02f2159dc28de6c1a7a6d5e2ee09`.
Its 64 SQLite and 13 UI passes certify that earlier source, not later edits.
The new `owned-files-final-v2.json` records current owned bytes, predecessor
hashes, the exact changed/new inventory and shared-file comparison hashes.
Evidence completed after this freeze belongs in a separate versioned verification
supplement; do not rewrite either frozen manifest.

## Bounded Product Increment

The canonical PRD maps domain names (DOMAIN-01) to UJ-45, while UJ-44 covers
GI, plant variety, semiconductor layout and trade secret. This increment follows
the requested UJ-44 target with semiconductor layout (SICLD-01). It does NOT
implement domain-name registrar/expiry/watch/UDRP/INDRP journeys.

- Restricted, exact-version/hash/locator layout deposits and independently
  identified applications; source-reported preparation, filing, examination,
  objection, response, hearing, registration, refusal and withdrawal.
- Separate registry-publication evidence, never deposit publication or a legal
  eligibility/protection-term/ownership inference.
- Separate opposition, cancellation and infringement records with pinned layout
  targets, explicit transitions, supplied outcomes and immutable closed history.
- Reuse of canonical reviewed rights claims/licences, confidential nonbillable
  cost evidence, supplied-date task/deadline work and atomic performance history.
- Revalidation of current sources, active cost lineage, permissions and locked
  lifecycle. A retained cost cannot silently disappear after voiding; an explicit
  active replacement is required. Refusal/withdrawal requires neutral work first.
- Layout UI fields, current-source/cost selectors, restricted deposit choices,
  actual saved values, rejected-input retention and publication-field clearing.

No parallel lifecycle, finance, access or task/deadline owner was introduced.
Existing ten-domain mixed-link disclosure denial remains in place: an ordinary
trademark link cannot authorize a restricted specialist document for general AI,
portal, export or notification use. No paid provider call is part of this work.

## Parent Integration Instructions

Worktree: `C:/Projects/CaseOps/caseops/.worktrees/codex-other-ip-domains-closure-20260909`.
HEAD remains `5145fb3a4b51b26af116220ff10a7389bde6324d`.

1. Review the predecessor handoff plus V2 `changed_or_new_owned_files`. Apply
   only owned files and previously identified specialist shared hunks. V2 adds
   no shared production-file hunks. Shared full-file hashes are comparison aids,
   never permission to overwrite the parent's newer provider/September 10 work.
2. Keep patent ownership of `services/ip_domain_catalog.py`. This track has NOT
   edited its inherited bytes. Coordinate the exact registration below rather
   than copying any catalog snapshot from this worktree.
3. Migration `20260909_0005` remains reserved. The new delta appends only
   `layout_application` to the explicit workflow-kind CHECK in ORM and migration;
   unknown kinds remain denied. Parent owns predecessor/chain linearization.
   A database which already applied an older 0005 will NOT gain the new CHECK by
   replaying the same revision. Coordinate an explicit upgrade strategy if such
   a database is retained; do not silently stamp or assume it changed.
4. Review/integrate the checkpoint before continuing toward all-domain closure.
   No commit, push, main merge, production action or whole-worktree copy occurred.

Required layout registration: contract `OTHER-IP-2026-09-10.3`, child path
`docs/ip-implementation/child-prds/semiconductor_layout-2026-09-10.md`, SHA-256
`2daba0fa7e4e724f065b3bebe9aece53b2de46b4595c92bee1c5f46ecc195c11`.
Preserve required journey UJ-44 and current catalog capability/acceptance gates.
Use per-domain `contract_version`, `contract_path` and `CHILD_PRD_HASHES`; do not
apply a global version. DES/COPY/LIC keep `.2`; six other domains keep `.1`.
Do not grant authoritative legal automation, disclosure channels or beta/GA status.

## Evidence At Freeze

Each completed pytest attempt retains its incremental setup/call/teardown JSONL,
JUnit, command journal, immutable source archive and SHA-256 inventory.
All failed reports were inspected individually and are preserved.

| Attempt | Actual result | Meaning |
| --- | --- | --- |
| foundation-01 | PostgreSQL startup preflight failed before pytest | Temporary init server shutdown; no product result. Fixed readiness to TCP. |
| foundation-02 | 32 passed | Earlier frozen DES/COPY/LIC source: independent PG workflows, exceptions, lifecycle races, 10,000-row bound and fresh eight-table migration upgrade/empty downgrade/re-upgrade. Not current layout certification. |
| layout-03 | 8 failed | Every case stopped at layout creation on the old workflow-kind CHECK. Later intended assertions were NOT reached. ORM/reserved migration repaired; unknown-kind denial added. |
| layout-04 | 8 passed, 1 failed | Deposit/application/publication, separate proceedings, refusal/work, source restrictions, contract denial, unknown-kind storage and both locked-writer races passed. Cost case stopped at an incorrect test expectation (201 instead of canonical void route's 200); later replacement assertions were not reached. Corrected test retained in V2. |
| web-01 | Typecheck failed before tests | Invalid test-only `getByRole` option; corrected. |
| web-02 | Full web typecheck passed; E2E typecheck failed before Vitest | Snapshot omitted repository PRODUCT_GUIDE_CATALOG.json used by two existing specs; runner now includes the actual fixture. No browser execution. |
| backend-05 | Running / incomplete at checkpoint freeze | Explicit full replacement inventory, including added layout rights/licence and lifecycle/access tests. No success is claimed before completion and reconciliation. |

Docker tests use pinned local images and the approved INTERNAL network
`caseops-sep10-provider`. Every PG run gets its own initially empty server/database;
no application/shared tenant database is cloned. The independently migrated test
template is created within that fresh server. Combined PG/test limits are 1 CPU
and 2 GiB (0.25 CPU/512 MiB plus 0.75 CPU/1536 MiB). Only owned PG containers are
stopped; failed containers/evidence are retained. Web containers use network none,
1 CPU/2 GiB, and exact lockfile comparison against the pinned test image.

## Exact Missing Acceptance

At this freeze, a complete green replacement PG inventory for the V2 bytes is
still missing. In particular, the corrected layout cost-removal/replacement
sequence and newly added layout rights/licence and close/reopen/access tests do
not inherit coverage from earlier attempts. Full isolated migration/race results
after the parent's chain/catalog integration remain required even after a local
green supplement. The inherited ten-domain mixed-link cases run on SQLite in
the replacement inventory, not a claimed ten-domain PostgreSQL delivery race.

No Playwright browser journey, screenshot, responsive visual check, production
acceptance or serving-revision verification has run for V2. The existing dated
DES/COPY/LIC specs also remain unexecuted here. DOM unit tests/typechecks must not
be described as browser acceptance. Catalog default admission remains fail-closed.

Still unfinished: domain names, enforcement, customs, GI, PVP and trade-secret
full workflows; UJ-44-EXC-03 biological-material custody; authoritative law/source
packs, automated statutory rules/fees, external filing/notices, enforcement-Matter
linkage, public projections/reporting, full governance/OpenAPI integration and
complete cross-domain lifecycle/access/delivery-race acceptance. DES/COPY/LIC and
layout source-bounded subsets do not close these remaining requirements.
