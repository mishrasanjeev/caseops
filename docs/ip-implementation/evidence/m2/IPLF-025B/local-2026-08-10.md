# IPLF-025B local evidence - 2026-08-10

## Verdict

IPLF-025B is implemented and locally verified at immutable implementation
revision `bab046e1cc22a0c19a6e8622765047d3076325d7`. Independent CI, canonical
merge, exact production deployment, and dated production acceptance remain
open, so the slice is `ready_for_review`, not deployment-verified.

Only isolated local fixtures were used. No real client record, calendar
provider operation, external notification, filing, fee, payment, or other
legal act occurred.

## Delivered scope

1. IP hearings capture exact/session/unknown time precision, timezone, forum,
   mode, logistics, responsible membership, attendees, recipients, channels,
   reminder offsets, and explicit confirmation before persistence.
2. Unknown hearing time remains date-only throughout CaseOps and calendar
   projections; no hidden 10:00 or other invented precision is introduced.
3. Every IP reminder recipient/channel/offset has inspectable queued,
   cancelled, failed, or delivered state. Delivery revalidates active
   membership and docket access immediately before dispatch.
4. Reschedule and cancellation atomically cancel the prior reminder generation;
   a replacement generation may safely reuse a timestamp that collides with a
   cancelled predecessor. Cancelled work cannot resurrect.
5. Google and Outlook project the canonical task, hearing, and operational
   deadline owners with stable provider identifiers and minimal, date-safe
   titles. Repeated sync upserts one projection; reschedule updates it and
   cancellation deletes it while CaseOps remains authoritative.
6. Provider failure/retry and revoked credentials fail closed without mutating
   canonical legal dates or losing provider history.
7. The IP workflow exposes preview/confirm, reminder outcomes, reschedule,
   cancellation, and connected-provider actions at a 360px viewport.
8. A production regression discovered while verifying the preceding exact
   release is also fixed: successful Matter creation closes immediately and
   does not wait for a slow list-cache refetch.

## Schema and rollback

Migration `20260810_0004` adds hearing logistics and reminder schedule
generation to the existing shared owners. It replaces the old reminder unique
constraint with a generation-aware key. Downgrade coalesces duplicate
generation rows before restoring the old constraint, and the migration test
proves upgrade, downgrade, and re-upgrade.

## Local automated evidence

| Layer | Result |
|---|---|
| Consolidated shared-work/calendar/reminder/migration API batch | 67 passed |
| Legacy Matter hearing-reminder regression | 18 passed |
| CI-affected Matter channel/suppression/shared-work regression | 22 passed |
| Focused New Matter + IP web components | 19 passed |
| API Ruff on all modified Python | passed |
| Web TypeScript generated-route check | passed |
| Current-source Next production build | passed |
| Dated 360px Playwright journey | 1 passed |
| Diff/whitespace integrity | passed |
| PostgreSQL-marked local selection | 14 skipped because no local PostgreSQL URL; CI gate remains required |

The consolidated API batch covers shared-work foundation, Google calendar,
established Outlook/LegalWorkspace routes, Outlook bulk compatibility,
hearing reminders, and the full migration cycle. The browser assertion is
`tests/e2e/iplf-025b-calendar-workflow-2026-08-10.spec.ts` and runs against a
fresh API plus a current-source production web build.

Initial CI on reviewed head `8b6933bf658077a3fc901b99d07bedede963ada6`
passed six API shards, PostgreSQL/pgvector, web, Security, CodeQL, generated
contract drift, and Codex review. Shards 3/8 and 4/8 exposed five legacy Matter
channel/suppression fixtures that intentionally have no membership ID. The
worker now applies mandatory membership/access revalidation only to IP docket
reminders while retaining the existing Matter lifecycle and channel checks;
all 22 affected tests, including IP permission removal, pass at `bab046e1...`.
Final-head independent CI remains required.

## Exact-release context

The preceding IPLF-025A documentation release was independently deployed from
canonical main `b8dbde8b08cb3ef2b4eb13725d66a948090d60db` as API revision
`caseops-api-00269-d5g` and web revision `caseops-web-00249-tsm`. Exact public
identity, immutable images, migration, health, and all six scheduler bindings
passed. Production workflow `31358897193` also passed the IPLF-025A dated
canary, but its broad RAM batch found the unrelated successful-create dialog
latency defect described above (58 passed, four skipped, one failed). This
local evidence does not relabel that workflow as successful; the corrected
exact release must rerun it after merge and deployment.

## Remaining gates

- Independent CI, Security, CodeQL, PostgreSQL, generated-contract, full web,
  and full Playwright checks on the final reviewed head.
- Exact-head merge to canonical `main`, exact immutable build/deployment,
  migration `20260810_0004`, scheduler reconciliation, and public identity
  verification.
- The same dated 360px IPLF-025B journey and the corrected production RAM/Notice
  suites against the exact serving release.
- Genuine named legal/pilot/UAT approval and M0 human program lock; repository
  evidence must not fabricate either approval.
