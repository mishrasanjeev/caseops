# IPLF-027A local evidence - 2026-08-12

## Verdict

IPLF-027A is implemented and locally verified as a repository-controlled,
runtime-unreleased foundation. The migrations, models, neutral services,
cross-language canonicalization fixture, inert workflow provenance, and
fail-closed rollout settings are present. This is `ready_for_review`, not
deployment-verified: independent CI, real PostgreSQL, exact-head merge,
deployment, and dated production evidence remain open.

Only isolated local SQLite fixtures were used. No real client record, external
message, provider operation, filing, fee, payment, or legal act occurred.

## Delivered boundary

1. Shared idempotency preserves one company/actor/method/operation/key identity,
   server-owned retry evidence, completed-result replay, stale-claim fencing,
   and a cross-language canonical JSON/file-hash contract.
2. The transactional outbox persists immutable, catalogue-admitted envelopes;
   enforces expected consumer effects; bounds retry/replay; and records
   tenant-scoped dead-letter replay or resolution without leaking sensitive
   payload/error values.
3. Workflow definition/version and lifecycle-provenance schema is additive,
   unseeded, company-scoped, and unactivated. Mixed-version and populated
   downgrade attempts fail closed rather than deleting legal or operational
   evidence.
4. Runtime event admission exactly matches the governed domain-event catalogue.
   Envelope metadata is persisted separately from the catalogue payload schema.
5. The five data classes are recorded as repository implemented but runtime
   unreleased. IPLF-028 retention/hold/export/purge/restore handlers remain
   explicitly unimplemented and fail closed.

IPLF-027B retains user workflow, approved activation, exception, release, and
deployed-acceptance work. No legal workflow or consumer is enabled by this
slice.

## Local automated evidence

| Gate | Result |
|---|---|
| Shared reliability contracts, outbox, fencing, retry/dead-letter, and effect completion | 14 passed |
| Shared reliability migration rollback | 1 passed |
| Inert workflow/lifecycle provenance migration | 5 passed |
| IPLF-027A rollout and problem-detail contracts | 12 passed |
| Audit contracts | 8 passed |
| Authentication/CSRF contracts | 14 passed |
| Observability/request-context contracts | 17 passed |
| Data-class, ARCH-OPS, and program-control tests | 44 passed |
| API Ruff for affected source/tests/migrations/control validator | passed |
| Program, ownership, ARCH-OPS, and data-class validators | passed |
| Web canonical JSON golden fixture | 4 passed |
| Web TypeScript type-check | passed |

SQLite emitted the repository's known datetime-adapter and TestClient
deprecation warnings. No focused IPLF-027A test was skipped. The workstation
has no `CASEOPS_TEST_POSTGRES_URL`; the real PostgreSQL/pgvector validation is
therefore intentionally deferred to independent CI rather than inferred from
SQLite.

## Remaining release gates

- Independent CI, security, CodeQL/review, full API/web, and real PostgreSQL
  validation on the exact reviewed head.
- Canonical merge to `main`, exact migration job/image/revision evidence, and
  a dated deployed assertion appropriate to the still-dark foundation.
- Explicit operator approval and bounded rollout policy before enabling either
  outbox consumers or IP workflow commands.
- IPLF-028 data-operation handlers and the genuine human/legal approvals
  allocated to later slices.

The pre-existing IPLF-026B production quota blocker remains separate: restore
the configured CaseOps OpenAI project quota and rerun its unchanged complete
production suite; IPLF-027A does not weaken, bypass, or resolve that gate.
