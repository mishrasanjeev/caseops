# IPLF-022B implementation and release evidence — 7 August 2026

## Result and truthful boundary

Repository-controlled implementation and local verification are complete for
IPLF-022B at implementation revision
`f82ff8ea4ffa8cd2a9ebf7ff88e3d52e223c6e66`, restacked directly on
deployment-verified canonical main
`01361f499fdad42bc95884fadcdbfff7e9cafc0a`.

The candidate supplies the capability-gated prosecution and docket-lifecycle
user workflows allocated to this slice. Independent CI/Security/CodeQL,
PostgreSQL migration verification, canonical-main merge, production
deployment, exact serving-revision verification, dated production acceptance,
and genuine human acceptance remain pending. `IP-PROS-01..12`, UJ-06, and
UJ-53 are reciprocal with IPLF-033B, so those program records remain incomplete
until the later owner is implemented and accepted. Overall status remains
`PROGRAM INCOMPLETE`.

Environment: Windows, CPython 3.13, Node 24, Next.js 16.3.0, repository-locked
Python and JavaScript dependencies, ephemeral SQLite migration/API fixtures,
and synthetic IP/Matter/browser data. No provider call, legal filing, fee,
notification, external message, client communication, real legal record, or
real credential was used.

## Delivered behavior

- Added application lifecycle state/version fields with terminal normalization
  and an exact downgrade/re-upgrade migration.
- Added preview and append APIs for every PRD prosecution event kind, with
  docket/application optimistic-concurrency checks and independent application
  phase progression.
- Kept refused/abandoned applications fail-closed; only a controlled,
  version-checked restoration event can reactivate them.
- Added evidence checklists for facts, forms, fees, documents, approvals, and
  unresolved exceptions without equating checklist completion with filing.
- Kept registry facts as candidates, exposed likely duplicates, required
  explicit reconciliation decisions, and preserved correction history.
- Marked backdated events for recalculation and kept resulting deadline
  references unconfirmed unless evidence explicitly confirms them.
- Added the prosecution read model with registry freshness, data-quality gaps,
  conflicts, unconfirmed deadlines, immutable history, and separate operational
  completion/filing evidence/registry acceptance/final disposition counts.
- Added lifecycle impact preview and commit routes with downstream impact rows,
  blocker acknowledgement, optional second approval, stale-write rejection,
  transfer-successor validation, atomic child neutralization, controlled
  reopen, and persistence after reload.
- Added the web prosecution and lifecycle cards with preview-before-commit,
  explicit legal-effect language, and mobile-safe wrapping/full-width actions.
- Updated OpenAPI types and both dated local and read-only production
  Playwright specifications.

## Executed verification

### Widened API and migration set

```text
apps/api/.venv/Scripts/python.exe -m pytest \
  apps/api/tests/test_20260807_application_lifecycle_migration.py \
  apps/api/tests/test_20260807_ip_lifecycle_migration.py \
  apps/api/tests/test_ip_prosecution_workflow.py \
  apps/api/tests/test_ip_lifecycle_service.py \
  apps/api/tests/test_ip_record_workflow.py \
  apps/api/tests/test_ip_core_records.py \
  apps/api/tests/test_ip_workspace_configuration.py \
  apps/api/tests/test_ip_readiness.py -q
27 passed
```

These tests prove preview is non-mutating, every event type has an explicit
phase, registry duplicates remain candidates, reconciliation/correction append
history, backdated events demand recalculation, application terminal state is
fail-closed, restoration is controlled, lifecycle blockers require explicit
acknowledgement, stale writes fail, transfer requires a same-tenant active
successor, and close/reopen state survives reload.

### Frontend, static, and build verification

```text
apps/api/.venv/Scripts/python.exe -m ruff check <IPLF-022B source and tests>
All checks passed

npm --prefix apps/web test -- app/app/ip/page.test.tsx
1 file passed; 7 tests passed

npm --prefix apps/web run typecheck
next typegen succeeded; TypeScript emitted no errors

NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000 npm --prefix apps/web run build
Next.js 16.3.0 production build passed; 65 pages generated

git diff --check
passed
```

### Dated local browser acceptance

```text
npx playwright test tests/e2e/ram-2026-08-01-bugs.spec.ts \
  --config=playwright.app.config.ts --project=app-chromium
2 passed
```

The browser suite provisions an entitlement and allowlist only for each
synthetic E2E tenant, while the API rollout dimension is explicitly enabled in
the E2E process. Production flags and entitlements remain unchanged. The test
creates a docket, asset, application, Matter evidence, deadline coverage,
recordal obligation, and cost evidence; previews and commits a prosecution
event; verifies the immutable timeline; previews downstream close impacts;
commits closure; and measures every grouped action at a 360-pixel viewport.

An initial run was blocked by a localhost/127.0.0.1 CSP mismatch in a locally
built bundle. The web source was rebuilt with the same `127.0.0.1` API origin
used by the harness. The next run reached the IP page and correctly showed the
synthetic tenant as not entitled/configured; the fixture was repaired to use
the real tenant configuration/enablement APIs with a synthetic-only billing
override. A later assertion expected `active → closed` although the preceding
workflow had correctly advanced the docket to `ready`; it was corrected to
assert the authoritative `ready → closed` transition. The final run passed
both scenarios without retry, quarantine, or allowed failure.

Warnings were limited to existing third-party Starlette, SQLite datetime, and
Next.js Edge Runtime deprecations.

The complete set above was rerun after restacking on 8 August 2026. Results
were 27/27 API and migration tests, 7/7 IP-page tests, 46/46 migration-order,
schema-index, manifest, ownership, and architecture/ops tests, full API Ruff,
all four repository validators, TypeScript, the 65-route production build,
and 2/2 dated Playwright scenarios. There were no retries, quarantines,
skips, or allowed failures.

## Production and scheduler context at evidence time

The immediately preceding IPLF-022A release is deployment-verified on
canonical main `01361f499fdad42bc95884fadcdbfff7e9cafc0a`, API revision
`caseops-api-00254-pq4` and web revision `caseops-web-00234-dwd`. Migration
`caseops-migrate-job-wwl4b`, all six immutable Scheduler/Cloud Run Job
bindings, health/ClamAV guards, and independent HTTPS identity checks passed.
Production workflow `31268892105` passed both the complete RAM batch and
Notice module; the received, reply, and sent uploads returned HTTP 200 in
0.417412085, 0.755888145, and 0.336074321 seconds.

The latest natural `caseops-extract-authority-metadata` execution
`caseops-extract-authority-metadata-ghhdx` was not healthy: it correctly
stopped at $40.07 rolling 24-hour Layer-2 spend against the $40.00 safety cap
and exited 2. The job command and `--concurrency 8` argument were valid. This is
recorded as a policy stop, not disguised as scheduler health and not used to
reintroduce a seven-consecutive-day release blocker. The safety cap was not
raised or disabled.

## Security, privacy, rollout, and rollback

All new requests reuse existing server capabilities, company scoping,
restricted-Matter access, and audit ownership. Evidence is referenced rather
than copied into audit metadata. Preview endpoints cannot mutate. External
effects remain absent.

The application migration is additive and old writers receive server defaults.
Before legal writes, downgrade/re-upgrade is proven. After append-only legal
history exists, rollback is rollout-off plus a forward, history-preserving fix;
destructive event removal is not authorized.

The slice is now restacked in dependency order and locally verified. It must
still pass independent PR checks, merge to `main`, deploy serially, run the
migration, prove exact API/web serving identity, and pass the dated read-only
production Playwright specification. None of those pending gates is inferred
from the local results above.
