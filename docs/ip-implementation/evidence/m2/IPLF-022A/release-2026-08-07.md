# IPLF-022A implementation and release evidence — 7 August 2026

## Result and truthful boundary

Repository-controlled implementation and local verification are complete for
IPLF-022A at restacked implementation revision
`4e9cd12f0d5f2c8f2d8f8a1d67607b98e1ec5bbd`, based on the deployment-verified
IPLF-021B release `63bc7587af4a5c3157462c6f668707cc8a08db9c`. The slice establishes the
append-only event store and dedicated lifecycle service required by IPLF-022.

Independent CI/Security/CodeQL, canonical-main merge, production deployment,
exact serving-revision verification, dated production acceptance, and genuine
human acceptance remain pending. The user-facing workflows and the reciprocal
`IP-PROS-01..12`, UJ-06, and UJ-53 allocations remain with IPLF-022B. Overall
status remains `PROGRAM INCOMPLETE`.

Environment: Windows, CPython 3.13, repository-locked `uv` environment in copy
mode, ephemeral SQLite migration/API fixtures, and synthetic IP/Matter data.
No provider call, filing, fee, notification, external message, client
communication, legal deadline calculation, credential, or real legal record
was used.

The restacked candidate reran the complete focused gates on 8 August 2026:
30/30 lifecycle/Matter/IP/migration regressions, 9/9 schema/index/migration
regressions, full API Ruff, and diff checks passed. No required test was
skipped or retried.

## Delivered behavior

- Added Alembic head `20260807_0003`, lifecycle columns/checks on the docket,
  and the tenant-scoped `ip_docket_events` table.
- Added typed event schemas and one command service for append, list, and
  active/terminal transition operations.
- Serialized event sequence creation under the parent lock and rejected stale
  lifecycle versions.
- Preserved original events; corrections/reconciliations create linked rows
  and registry facts remain candidates until an explicit decision.
- Enforced same-tenant docket/application/proceeding/membership ownership and
  same-docket correction/reconciliation links.
- Acquired Matter before docket locks so direct IP lifecycle transitions and
  Matter disposal cannot race in opposite lock order.
- Made ordinary docket and child operations fail closed for terminal parents;
  generic version/metadata commands cannot reactivate a record.
- Atomically neutralized active coverage, open related-right obligations, and
  their shared open deadlines on terminal transition. Controlled reopen leaves
  those children neutralized.
- Extended Matter disposal to archive linked IP dockets consistently, append a
  system lifecycle event, increment lifecycle versions, and neutralize IP
  operational children in the same transaction.
- Exposed lifecycle fields in existing docket responses without adding a
  lifecycle route or enabling a production feature. IPLF-022B owns the
  capability-gated user workflow and exceptions.

## Verification

### Lifecycle, migration, Matter, and existing IP regressions

```text
uv run --frozen pytest -q \
  tests/test_20260807_ip_lifecycle_migration.py \
  tests/test_20260807_ip_workspace_migration.py \
  tests/test_ip_lifecycle_service.py \
  tests/test_ip_record_workflow.py \
  tests/test_ip_prd_slices.py \
  tests/test_matter_lifecycle.py
30 passed
```

This proves append order, immutable corrections, registry candidate admission,
tenant and owned-target rejection, stale-write rejection, terminal
suppression, controlled reopen, non-resurrection, final-state persistence,
Matter-disposal compatibility, and both migration round trips.

### Schema and governance regressions

```text
uv run --frozen pytest -q \
  tests/test_20260807_ip_lifecycle_migration.py \
  tests/test_ip_lifecycle_service.py \
  tests/test_schema_fk_indexes.py \
  tests/test_migration_order.py
9 passed

uv run --frozen pytest -q \
  tests/test_migration_order.py tests/test_schema_fk_indexes.py \
  tests/test_ip_program_manifest.py tests/test_ip_ownership_ledger.py \
  tests/test_ip_arch_ops_contract.py
45 passed before the index gate identified four missing event-FK indexes;
the indexes were then added and the focused schema set passed 9/9.
```

The earlier 45/46 run is recorded as a detected-and-fixed pre-commit defect,
not as passing evidence. The final candidate has leading indexes for every
event foreign key.

### Static and repository checks

```text
uv run --frozen ruff check src \
  tests/test_20260807_ip_lifecycle_migration.py \
  tests/test_ip_lifecycle_service.py
All checks passed

git diff --check
passed
```

Warnings were limited to existing third-party Starlette and Python SQLite
datetime-adapter deprecations. No required test was skipped, quarantined,
retried, or allowed to fail.

## Security, privacy, and side-effect boundary

Every new legal row has composite tenant ownership. Restricted and Matter
access rules are reused; inaccessible records fail as not found. Audit metadata
contains identifiers and event classification, not copied legal evidence.
There is no route or automation enablement in this slice, and the later shared
outbox remains owned by IPLF-027 rather than being duplicated here.

## Rollout and rollback

The production migration is additive except for deterministic normalization of
already-archived docket activity flags. It neither invents a legal event for
historical records nor activates a feature. Mixed revisions remain compatible:
new response fields are additive, old writers receive server defaults, and
Matter disposal was upgraded with the constraint in the same candidate.

Before any production write, downgrade/re-upgrade is proven. After event writes,
rollback must preserve legal history through rollout-off and a forward fix;
destructive table removal is not authorized.

There is no arbitrary seven-consecutive-day scheduler gate. Release readiness
uses deterministic exact-revision checks and the genuine approvals relevant to
the behavior; none is fabricated here.

## Production attempt on 8 August 2026

Candidate `c04f456d53dbd891b6696b8d30cf260bc7999543` passed all PR gates,
merged to canonical `main`, migrated successfully, and was deployed at 100%
traffic as API revision `caseops-api-00251-rz9` and web revision
`caseops-web-00231-rjc`. Independent HTTPS identity verification passed and
the dated production RAM batch passed. The final Notice suite did not pass: a
synthetic reply-document upload returned HTTP 200 only after 246.103970758
seconds, beyond its 90-second response gate.

IPLF-022A therefore remains short of `deployment_verified` at this checkpoint.
The failure, root-cause boundary, repository repair, local verification, and
required exact-revision rerun are recorded in
[`production-attachment-scan-hotfix-2026-08-08.md`](production-attachment-scan-hotfix-2026-08-08.md).

The pre-storage repair subsequently passed all PR gates in `#183`, merged as
`e57a8de9c54f82f4f5a0843ce610221d7f1e578b`, and was deployed exactly as API
revision `caseops-api-00252-57c` and web revision `caseops-web-00232-p68`.
Production run `31262596426` again passed the complete RAM batch. Its primary
notice upload completed in 0.404735352 seconds, proving the original GCS
read-after-write repair, but the reply upload then reached the API and was
terminated by Cloud Run at 300.000802207 seconds with HTTP 504. The document
worker had flushed the parent attachment before an external embedding call,
so the reply transaction could wait on that provider-held row lock. The next
repository candidate moves the initial worker flush after provider return and
adds a two-session concurrency regression. IPLF-022A therefore still remains
short of `deployment_verified` until that candidate passes independent gates,
exact production deployment, and the same dated production workflow.

The attachment-row candidate then passed PR `#184` gates, merged as
`831b72e0674f62501f038c829339677f567d27c2`, and was deployed exactly as API
revision `caseops-api-00253-hnx` and web revision `caseops-web-00233-jsf`.
Production run `31265872079` passed the dated RAM batch. Its received and reply
uploads returned HTTP 200 in 0.381215891 and 9.262007534 seconds respectively,
but the sent upload exceeded the 90-second gate and Cloud Run returned HTTP
504 after 300.000144825 seconds. The remaining transaction held the parent
Matter lifecycle lock across the external provider call.

The current repository candidate removes that provider-held parent lock while
preserving fail-closed lifecycle semantics: it rechecks and locks the Matter
under `no_autoflush` immediately before the first child/vector persistence.
Two-session regressions prove concurrent Matter/attachment writes complete
during provider latency and that a concurrent disposal rolls back the provider
result. Focused worker tests passed 7/7; the widened document, Notice,
security, and lifecycle set passed 76/76. Exact candidate PR gates, merge,
deployment, and the same production workflow remain required, so IPLF-022A is
still not represented as `deployment_verified` at this checkpoint.
