# IPLF-020B implementation and release evidence — 7 August 2026

## Result and truthful boundary

Repository-controlled implementation and local verification are complete for
the IPLF-020B capability/readiness user surface. The candidate is now restacked
on deployment-verified canonical main
`509ba80d7848d6f331d249752b4c3d3318159fc2`; independent CI and its own
production release remain pending. UJ-01 remains `in_progress` because its
jurisdiction/office/taxonomy/rule/provider configuration persistence and tests
are reciprocally allocated to IPLF-021B. This evidence does not claim UJ-01,
M2, human approval, or the full program complete.

Restacked implementation revision: `7274df488a83e1fa96bd5a6d47ce16da98f493a0`

Base revision: deployment-verified IPLF-020A canonical main
`509ba80d7848d6f331d249752b4c3d3318159fc2`

Branch: `codex/iplf-020b-readiness-20260806`

Environment: Windows, CPython 3.13 and Node/npm with repository lockfiles.
Fixtures are synthetic companies, memberships, entitlement/flag states, and IP
docket records only. No provider call, legal message, filing, fee, client
communication, production mutation, or personal data was used.

## Delivered behavior

- Added authenticated `GET /api/ip/readiness` with a generated OpenAPI client
  contract and stable per-feature reason, owner, entitlement, flag, expiry,
  missing-capability, and manual-fallback fields.
- Resolved existing billing entitlements without creating a billing account or
  subscription during a read/authorization check.
- Migrated current IP routes, navigation, and page presentation to canonical
  `ip:read`, `ip:approve`, and `ip:fees_manage` names while retaining bounded
  aliases for mixed revisions.
- Made the web page check readiness before any docket query. A red workspace
  renders no operational records or actions.
- Added a narrow-mobile setup state in which every item is visible, wraps, and
  identifies the blocking reason and accountable owner.
- Kept manual docketing operational when a provider-backed automation is red,
  while visibly labelling only that automation disabled and naming the manual
  fallback.
- Regenerated `apps/web/lib/api/openapi-types.ts` with pinned
  `openapi-typescript@7.13.0 --alphabetize`.

## Journey-path contribution

| Path | IPLF-020B proof | Remaining reciprocal owner |
|---|---|---|
| `UJ-01-NORMAL` | authenticated readiness API, workspace setup surface, timezone and all feature decisions | IPLF-021B configuration persistence, selections, tests, allowlist |
| `UJ-01-EXC-01` | registry/provider automation can be disabled while manual docketing stays visible and usable | IPLF-021B named provider configuration/test state |
| `UJ-01-EXC-02` | disabled capability renders an explicit reason and owner; operational query is suppressed when workspace core is red | IPLF-021B source catalogue/readiness persistence |
| `UJ-01-EXC-03` | fail-closed reason is scoped to the affected gate; other ready/manual features remain available | IPLF-021B persisted provider test result and affected-automation isolation |

These paths remain `in_progress`, not passed or released, until IPLF-021B and
dated deployed desktop/narrow-mobile acceptance complete.

## Verification

### Focused API contract

```text
uv --directory apps/api run pytest \
  tests/test_ip_capability_catalog.py \
  tests/test_ip_readiness.py -q
24 passed
```

This proves unauthenticated rejection, tenant-specific output, exactly 11
feature decisions, dark defaults, stable reasons/owners/fields, and no billing
subscription creation from the readiness GET.

### Existing IP and authorization regression

```text
uv --directory apps/api run pytest \
  tests/test_ip_prd_slices.py \
  tests/test_role_guards.py -q
12 passed
```

The prior IP lifecycle/evidence/deadline/title/cost behaviors and the route
guard sweep pass after the canonical capability migration.

### Frontend and responsive acceptance

```text
npm run test --workspace @caseops/web -- --run \
  app/app/ip/page.test.tsx \
  lib/capabilities.test.ts
9 tests passed

npm run typecheck --workspace @caseops/web
passed
```

The page tests assert fail-closed role denial, no docket fetch while readiness
is red, every setup reason/action visible at 360px, manual docketing preserved
while registry automation is disabled, existing empty/create behavior, and all
grouped operational actions visible at narrow width.

The restacked candidate also passed the dated production-source browser flow:

```text
npx playwright test tests/e2e/ram-2026-08-01-bugs.spec.ts \
  --config=playwright.app.config.ts --project=app-chromium
2 passed
```

Each synthetic company receives an explicit test-only `ip_workspace`
entitlement and the E2E API process explicitly enables the independent rollout
flag. The browser then proves existing create, Matter evidence, deadline
coverage, obligation, cost, and reconciliation operations remain available and
inside the 360-pixel viewport. Production entitlements and flags are not
changed.

### API formatting, lint, OpenAPI, and repository hygiene

```text
uv --directory apps/api run ruff format --check <five impacted Python files>
5 files already formatted

uv --directory apps/api run ruff check <five impacted Python files>
All checks passed

uv --directory apps/api run --no-sync python \
  ../../scripts/dump_openapi.py C:\tmp\iplf020b-openapi.json
npx --yes openapi-typescript@7.13.0 --alphabetize \
  C:\tmp\iplf020b-openapi.json \
  --output apps/web/lib/api/openapi-types.ts
passed; generated client committed

git diff --check
passed
```

The restacked Next.js 16.3.0 production build also compiled, typechecked, and
generated all 65 pages successfully with the local API origin aligned to the
document CSP.

Warnings are existing third-party Starlette, SQLAlchemy/SQLite adapter, and
Next Edge Runtime deprecations. No required test was skipped, focused,
quarantined, retried, or allowed to fail.

## Data, security, and rollback

No schema, backfill, tenant configuration row, provider credential, scheduler,
notification, filing, fee, or production data changes are in this slice. The
readiness resolver is read-only. Frontend gating is presentation only; every
existing route continues to enforce its canonical server capability.

All rollout flags remain false and no IP entitlement is seeded. The release is
therefore dark/fail-closed by default. Operational rollback is to keep or set
the relevant flag false; code rollback is additive because no persisted
representation changed and legacy aliases remain.

## Release dependency

The candidate must not merge or deploy ahead of IPLF-019B and IPLF-020A. Each
slice requires independent CI/Security/CodeQL, canonical `main`, exact image
and Cloud Run revision verification, and the same dated production Playwright
spec. GitHub runner setup failures during the active Actions incident are not
waived as passing evidence.
