# IPLF-025A release evidence - 2026-08-10

## Verdict

Repository-controlled implementation, independent verification, canonical
merge, exact production deployment, ordered migration, scheduler
reconciliation, release-blocking tenant reconciliation, and dated production
acceptance are complete. IPLF-025A is `deployment_verified` at canonical
release `2f27f044dd27d47c805e1d2a35b92f4fbee893ae`.

This verdict is bounded. IPLF-025A closes the shared-owner foundation and
backend contract only. IPLF-025B retains the CAL-OPS-01..14 behavior and all
UJ-10/UJ-62 normal and exception paths. Named legal/pilot/UAT acceptance, M0
human program lock, and later slices remain pending. The overall verdict
remains `PROGRAM INCOMPLETE`.

Only isolated local fixtures and the dedicated CaseOps QA tenant were used.
No real client record, external calendar operation, notification delivery,
filing, fee, payment, or other legal act occurred.

## Released identities

| Purpose | Identity |
|---|---|
| Foundation implementation | `feb52bb2dad5fef8156de7212a8f12e7e0806cfc` |
| Final reviewed head | `dffcd7003ed52f6c4283812f51e1b79f28addc58` |
| Merge / canonical release | `2f27f044dd27d47c805e1d2a35b92f4fbee893ae` (PR #199) |

Local `main` and `origin/main` both resolved to the canonical release after
merge. Deployment used a clean detached worktree at that exact commit.

## Delivered scope

1. Existing task, hearing, next-hearing provenance, operational-deadline,
   calendar, reminder, and durable-notification owners accept a
   tenant-correlated IP docket target without parallel IP work tables.
2. Three ordered migrations expand nullable targets, backfill Matter tenant
   correlation, tolerate and preserve a drained old-writer tail, then switch
   to exactly-one-target and composite tenant integrity constraints.
3. Typed IP GET/POST/PATCH adapters delegate to the canonical task, hearing,
   and operational-deadline rows while existing Matter routes remain valid.
4. Hearing precision, session/mode/responsibility, next-hearing provenance,
   task completion, and notification intent state remain on the shared owners.
5. `ip_deadlines` remains the only legal-calculation writer. Direct mutation
   of its linked operational row returns conflict rather than creating a
   second legal state owner.
6. Reconciliation is tenant-scoped and release-blocking for legacy tails,
   invalid targets, and tenant mismatches. Another tenant's nullable legacy
   row cannot pollute the current tenant's count or readiness result.
7. Required PATCH fields reject explicit null, cross-tenant targets fail
   closed, and IP notification enqueue requires a valid tenant docket.

## Local automated evidence

| Layer | Result |
|---|---|
| Focused shared-work API | 6 passed |
| Mixed-revision migration | 1 passed, including expand/backfill/old-writer-tail/switch/downgrade/re-upgrade |
| Impacted task/deadline/hearing/reminder/notification tests | 36 passed |
| API Ruff | passed |
| Program, ownership, and ARCH-OPS validators | passed |
| Generated OpenAPI typecheck | passed |
| Current-source Next production build | passed |
| Dated local Playwright | 1 passed |

The first local web build lacked a worktree-local `nodemailer` dependency;
`npm ci` restored the declared lockfile environment and the current-source
production build passed. The first dated browser fixture omitted required
docket particulars; the fixture was completed and the unchanged product
assertions passed. Neither result is counted as a product pass before its
successful rerun.

## Independent CI, security, and review

PR #199 validated final reviewed head `dffcd700...`:

- CI `31352111422`: all eight API coverage shards, aggregate per-area coverage,
  Ruff, PostgreSQL/pgvector, web typecheck/Vitest/build, and the full Playwright
  app suite passed; full Playwright completed in 5m52s.
- Security `31352111507`: gitleaks, npm audit, pip-audit, license allow-list,
  OpenAPI generated-client drift, and Cloud Run secret-reference policy passed.
- CodeQL `31352111413`, the new-alert check, and review workflow `31352111414`
  passed. CodeQL review prompted removal of an ambiguous loop temporary and an
  unused test context; focused tests and the remote analyzer passed afterward.
- Canonical merge CI `31354008048` repeated all eight shards, aggregate
  coverage, web build/tests, PostgreSQL, and full Playwright on exact `main`;
  the Playwright app suite passed in 6m18s.

## Exact production deployment

| Control | Exact result |
|---|---|
| Canonical release | `2f27f044dd27d47c805e1d2a35b92f4fbee893ae` (`2f27f04` runtime identity) |
| API Cloud Build | unique prefix `1b778d0c-548e` |
| Web Cloud Build | unique prefix `ee35c663-5e9c` |
| API immutable image | `sha256:f0f77718e1aeea087378d1e6c54cf000c43d753010c19125b235446b1537ab26` |
| Web immutable image | `sha256:8f22990ee70b8a8cc318b751a21bf561d0c3eb57bcf3c11b719cc673af76cabb` |
| Migration | `caseops-migrate-job-shxpm` completed successfully at head `20260810_0003` |
| API revision | `caseops-api-00268-6p9`, 100% traffic |
| Web revision | `caseops-web-00248-spm`, 100% traffic |
| Exact release probe | API and web both served `2f27f04`; health returned `ok` |
| Scheduler inventory | all six canonical scheduler/job bindings passed on the exact API digest |
| Production workflow | `31354008059`, successful in 27m13s against the exact serving release |
| Production RAM batch | 61 passed in 15.1m; 4 fixture/provider-conditional cases skipped |
| IPLF-025A dated assertion | exact shared-work contract and ready tenant reconciliation passed in 5.1s |
| Production Notice module | 2 passed in 26.4s |

The six verified scheduler bindings are legal-update sync at midnight IST,
case-tracking poll at 16:30 IST, activity report at 08:00 IST, reminders every
five minutes IST, authority metadata extraction daily at 00:30 UTC, and database
index health weekly at 02:30 UTC. Every binding is enabled, uses the scheduler
invoker identity, points to its declared Cloud Run job, and runs the exact API
digest above. The superseded midnight case-tracking schedule remains paused.

## Security, rollback, and non-effects

- Tenant and target checks are server-side; cross-tenant rows fail closed.
- Release readiness is derived per tenant and cannot be made green by another
  tenant's rows or by ignoring a legacy tail.
- Rollback is the ordered switch-to-backfill-to-expand downgrade. The migration
  proof exercises downgrade/re-upgrade and preserves the mixed-revision tail.
- Old Matter routes remain compatibility adapters; no existing owner was
  retired and no duplicate IP task/hearing/deadline/calendar/reminder/
  notification subsystem was introduced.
- No provider sync, external event, message, filing, service, fee, payment, or
  other real-world legal effect occurred.

## Remaining work

IPLF-025B is the active slice. It owns CAL-OPS-01..14 and the complete UJ-10
hearing/reminder and UJ-62 external-calendar synchronization normal/exception
paths over these verified owners. Genuine named legal/pilot/UAT approvals and
M0 human program lock remain open; IPLF-025A deployment verification does not
complete its parent epic, M2, or the program.
