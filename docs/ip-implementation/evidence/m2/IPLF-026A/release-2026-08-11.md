# IPLF-026A release evidence - 2026-08-11

## Verdict

Repository-controlled implementation, independent review, canonical merge,
exact production migration/deployment, reconciliation, authorized access, and
authenticated-ungranted denial are complete through PRs #205, #206, and #207.
Exact-release workflow `31509430739` passed the expanded dated canary across
every protected surface. IPLF-026A is therefore `deployment_verified`.

This is a bounded repository and production verdict. IPLF-026B retains the
user-facing grant/revoke/preview workflow, UJ-46 exceptions, and every
IP-ACCESS, SEARCH-ACL, and SEC-GOV completion claim. Named legal, security,
pilot, or UAT acceptance and the M0 human program lock remain pending. The
overall verdict remains `PROGRAM INCOMPLETE`.

Only synthetic fixtures and the isolated `caseops-ip-qa` tenant were used.
No real client record, provider operation, notification, filing, fee,
payment, or other legal act occurred.

## Released lineage

| Purpose | Identity |
|---|---|
| Final implementation head | `7a178314a19816189ab3fc66457f4e6efca9c892` (PR #205) |
| Implementation merge | `90812bfc0401de2fd1a88dbe98f9521166149b0a` |
| Repeatable production-canary head | `8cb3f801624fe1d608da83173f12389541d640ea` (PR #206) |
| Canary-fix merge | `a1ff9a8b8c9d96329685150675c393b9168ea2a8` |
| Authenticated-denial evidence head | `62f085581bb93578c0a1c19e711d192b18cf2811` (PR #207) |
| Authenticated-denial evidence merge and canonical production descendant | `0dd342c656f66d07fdcc3a301c682fc93c0a407c` |

Local `main` and `origin/main` both resolved to `0dd342c656f...` before this
evidence update branch was created. The intermediate `551f4cfe...` and
`f0e5fecf...` releases contain PR #206 and address separate Matter/research
production regressions; they do not broaden the IPLF-026A allocation.

## Delivered boundary

1. `MatterAccessGrant` and `EthicalWall` are the only canonical internal
   record-access owners. Each row targets exactly one Matter or IP docket and
   exactly one membership or team, with tenant-correlated foreign keys,
   effective/revocation windows, optimistic versions, and active uniqueness.
2. Expand/backfill/switch migrations preserve mixed-version Matter writes,
   snapshot linked-Matter access into an independent IP policy, reject
   unreconciled or cross-company rows, and retain downgrade/re-upgrade proof.
3. Restricted IP records require an explicit effective grant after ethical
   wall denial. Company owners do not bypass IP policy.
4. One fail-closed predicate protects list/count, direct and lifecycle reads,
   child operations, document list/detail/download, source open, docket audit,
   and asynchronous audit-export hydration.
5. Worker-time export authorization, direct IP audit correlation, and
   reconciliation prevent stale or uncorrelated data from leaking.
6. Portal grants, access-review campaigns, emergency-access persistence, and
   the user-facing grant/revoke/preview workflow were not introduced.

## Independent CI, security, and review

- PR #205 final head `7a178314...` passed CI `31452414995`, Security
  `31452415056`, CodeQL `31452414992`, and automated review
  `31455108388`. The CI matrix passed all eight API shards, aggregate API
  coverage, Ruff/validators, real PostgreSQL/pgvector, web
  typecheck/Vitest/build, and the complete Playwright app suite.
- PR #206 head `8cb3f801...` passed CI `31458705615`, Security
  `31458705718`, CodeQL `31458705637`, and automated review
  `31458725559`. All eight API shards, aggregate API, real PostgreSQL, web,
  and full Playwright gates passed again.
- PR #207 final head `62f08558...` passed CI `31505168541`, Security
  `31505168572`, CodeQL `31505168573`, and automated review
  `31505168554`. All eight API coverage shards, aggregate API, real
  PostgreSQL, Ruff/validators, web typecheck/Vitest/build, and the full
  Playwright app suite passed on that exact head.
- PRs #205 and #206 had no review comments, requested changes, or unresolved
  review threads at merge. PR #207's authenticated-denial review thread was
  addressed by the expanded test and became outdated; the workflow leaves
  thread state untouched. PRs #206 and #207 were merged with expected-head
  SHA fencing.
- The final local affected batch passed 32 tests with no failure or skip;
  full API Ruff, manifest, ownership, ARCH-OPS, and diff-integrity checks
  passed.

## Exact production deployment

The first canonical implementation merge `90812bfc...` was independently
built and deployed before acceptance. PR #206 then corrected only the dated
canary's repeatability: the original constant file bytes correctly triggered
the document duplicate path on a second run, while the test incorrectly
assumed a newly created document.

The current serving release contains all three merges and provides the
decisive positive, negative, and repeatability proof:

| Control | Exact current result |
|---|---|
| Canonical release | `0dd342c656f66d07fdcc3a301c682fc93c0a407c` (`0dd342c` runtime identity) |
| API Cloud Build | `d957ea18-b6bd-4832-9f1a-a4da64206742`, success |
| Web Cloud Build | `71f1b066-5bc6-4629-b809-4737004a635c`, success |
| API immutable image | `sha256:4d4273c5c050a3b9452f7f81ccb7a0f0d3a63e1dce831d228d01dd246da45b62` |
| Web immutable image | `sha256:c3d8664a20a5f1aae32e0c910aebe09448dc41d11b5ca232039c5d5d82d1baae` |
| Migration | `caseops-migrate-job-sb25b` succeeded on the exact API digest at Alembic head, including `20260811_0001..0003` |
| API revision | `caseops-api-00279-6pg`, 100% traffic |
| Web revision | `caseops-web-00258-gbg`, 100% traffic |
| Independent release probe | full SHA matched both services; health returned `ok` |
| Scheduler inventory | all six canonical bindings passed on the exact API digest |

The six verified scheduler bindings are legal-update sync at midnight IST,
case-tracking at 16:30 IST, activity report at 08:00 IST, reminders every five
minutes IST, authority metadata extraction at 00:30 UTC, and database index
health at 02:30 UTC weekly. Each is enabled, uses the declared scheduler
identity, targets the canonical job, and runs the exact API digest.

## Dated production acceptance

The expanded `tests/e2e/ram-2026-08-11-prod.spec.ts` passed against the exact
serving release in push-triggered workflow `31509430739`. It proved
unauthenticated denial, the published canonical-owner contract, healthy
reconciliation, authorized restricted-docket list/count/direct access,
protected document upload and source redirect, correlated docket audit, and
final zero-defect reconciliation. A separate authenticated but ungranted admin
then proved docket and document list suppression plus docket direct, document
detail, source open, and docket audit 404 denial. The synthetic admin was
deactivated after the successful canary.

| Workflow | Result |
|---|---|
| Push-triggered `31466368302` | exact release identity; IPLF-026A passed in 11.2s; 68 RAM/IP passed with 3 conditional skips; 2 Notice passed |
| Scheduled `31467815420` | exact release identity; IPLF-026A passed in 10.8s; 67 RAM/IP passed with 4 conditional skips; 2 Notice passed |
| Authenticated-denial push `31509430739` | exact `0dd342c656f...` identity; 66 RAM/IP passed with 5 conditional skips in 15.8m; 2 Notice passed in 21.6s |

The conditional skips are documented pre-existing fixture/provider
availability branches; the IPLF-026A dated journey executed and passed in all
three counted runs. Failure artifacts were not uploaded because the workflows
were green.

The earlier `90812bfc...` push run `31455153094` also passed the dated journey
and complete production suite. Explicit dispatch `31455731275` exposed the
non-repeatable constant-byte canary plus nine broad network timeouts; PR #206
fixed the canary, and the two current-release runs above prove it is now
repeatable. A queued `a1ff9a8...` dispatch was cancelled automatically when
later canonical releases superseded it and is not counted as acceptance.

## Security, rollback, and non-effects

- Authorization remains server-side, company-scoped, and fail-closed. IP
  owners have no bypass; team/membership walls take precedence over grants.
- Current Matter behavior remains compatible, including its separately
  documented owner bypass. No parallel IP grant or wall table exists.
- Migration switch and downgrade/re-upgrade tests cover legacy tails,
  composite tenant keys, partial uniqueness, indexes, and cleanup.
- The QA tenant and uploaded records are explicitly synthetic. No external
  message, calendar event, provider mutation, filing, service, fee, payment,
  or other real-world legal effect occurred.

## Remaining work

IPLF-026B is the active slice. It must deliver the user-visible grant,
revoke, preview, warning, and exception workflows, including last-owner,
cross-company, linked-Matter no-broadening, and revoked-session cases, before
claiming UJ-46 or the allocated IP-ACCESS, SEARCH-ACL, and SEC-GOV rows.
Genuine named legal/security/pilot/UAT approval and M0 human program lock also
remain open.
