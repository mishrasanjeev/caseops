# IPLF-026A local evidence - 2026-08-11

## Verdict

IPLF-026A is implemented and locally verified at immutable implementation
revision `9449b00214bc8996a5b6c79921bc7af17c607b8c`, rebased over canonical
main `61f749c037118500f157582dbb2f972675b73d40`. Independent CI, canonical
merge, exact production migration/deployment, reconciliation, and dated
production acceptance remain open, so the slice is `ready_for_review`, not
deployment-verified.

Only deterministic synthetic tenant records and isolated local databases were
used. No real client record, external delivery, filing, provider operation,
fee, payment, or legal act occurred.

## Delivered boundary

1. The existing `MatterAccessGrant` and `EthicalWall` owners now accept exactly
   one Matter-or-IP target and one membership-or-team subject, with company
   correlation, effective/revocation windows, optimistic record versions, and
   active uniqueness. No parallel IP grant or wall tables exist.
2. Expand/backfill/switch migrations preserve the old Matter writer during the
   mixed-version window, snapshot linked-Matter visibility into independent IP
   policy, reject unreconciled/cross-company rows, and retain a tested
   downgrade/re-upgrade path.
3. Restricted IP records require explicit effective grants after wall denial;
   company owners do not bypass IP policy. Unrestricted records remain visible
   unless an effective membership/team wall applies.
4. The same predicate protects portfolio list/count, direct and lifecycle
   reads, child record operations, document list/detail/download, source open,
   docket audit, and async audit export hydration.
5. Audit exports reauthorize correlated records at worker execution time and
   omit uncorrelated record-level IP events fail-closed, including when the
   requester is an owner excluded by an ethical wall.
6. The foundation and reconciliation endpoints publish the canonical owner,
   explicit exclusions, bypass distinction, nullable legacy tails, invalid
   target/subject rows, tenant mismatches, and direct audit-correlation health.

Portal grants, access-review campaigns, emergency-access persistence,
grant/revoke/preview UI, UJ-46 completion, and the allocated IP-ACCESS,
SEARCH-ACL, and SEC-GOV claims remain deliberately deferred to IPLF-026B and
later reciprocal slices.

## Final-head automated evidence

| Gate | Result |
|---|---|
| Access/migration/document/source/audit focused batch | 29 passed, 0 failed |
| Migration expand/backfill/mixed-write/switch/downgrade/re-upgrade | included and passed |
| Owner-no-bypass and worker-time audit-export reauthorization | included and passed |
| Legacy Matter ethical-wall compatibility | included and passed |
| Full API Ruff (`src` and `tests`) | passed |
| Diff/whitespace integrity | passed |

The 29-test batch is the exact final-head selection:

- `test_20260811_record_access_target_migration.py`
- `test_ip_record_access_foundation.py`
- `test_ethical_walls.py`
- `test_ip_record_workflow.py`
- `test_ip_document_workflow.py`
- `test_ip_prd_slices.py`
- `test_audit_export_async.py`

SQLite/SQLAlchemy emitted only the repository's known datetime-adapter and
TestClient deprecation warnings. No test was skipped. Real PostgreSQL remains
an independent CI/release gate and is not inferred from SQLite success.

## Remaining release gates

- Independent CI, Security, CodeQL, real-PostgreSQL migration, generated
  contract, web, review, and complete Playwright gates on the exact reviewed
  head.
- Exact-head merge to canonical `main`, immutable API/web builds, migrations
  `20260811_0001` through `20260811_0003`, recurring-job convergence, and exact
  public release identity.
- Production-safe reconciliation with every count zero plus dated access
  assertions for hidden list/count, direct ID, document/source, audit, expiry,
  revocation, team grants/walls, and no owner bypass.
- Genuine named legal/security/pilot/UAT acceptance where later allocated
  rows require it; repository evidence must not fabricate human approval.
