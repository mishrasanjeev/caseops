# IPLF-021B implementation and release evidence — 7 August 2026

## Result and truthful boundary

Repository-controlled implementation, independent CI, canonical-main merge,
production deployment, exact serving-revision verification, and dated
production acceptance are complete for IPLF-021B. The slice adds the core IP
record commands, identifier reconciliation and correction workflow, persisted
UJ-01 workspace configuration/test state, fail-closed readiness overlay, and
responsive setup surface.

Core implementation revision: `aa7c52d` after the PR history was rewritten to
remove a gitleaks false positive from synthetic test data.

Base revision: canonical IPLF-021A merge
`8c94974724e9116e3c0ac9bbb203b2769ab7ad58`

Branch: `codex/iplf-021b-ip-record-workflow-20260807`

Environment: Windows, CPython 3.13, Node/npm from repository lockfiles,
ephemeral SQLite migration/API fixtures, Vitest/jsdom, and deterministic
synthetic IP values. No real provider call, notification, filing, fee, client
communication, legal deadline, production mutation, credential, or personal
data was used.

This evidence does not claim `IPLF-031B`, full `IP-ID-01..08`, M2, external
provider approval, human acceptance, or the program complete. The slice is
`deployment_verified`; its genuine human acceptance remains pending.

## Delivered behavior

- Added Alembic head `20260807_0002` and SQLAlchemy models for one versioned
  tenant workspace configuration plus append-only readiness-test results.
- Added asset, trademark-application, proceeding, identifier, identifier
  correction, filing-phase, core-record list, and normalized identifier-search
  commands/routes.
- Preserved raw identifier display values and exact normalized variant search;
  normalized collisions are returned as reconciliation candidates and marked
  `needs_review`, never merged.
- Required typed application versus proceeding ownership. Opposition,
  rectification, appeal, and court identifiers cannot be stored as application
  numbers.
- Enforced the filed-phase application-number invariant, explicit pending
  allocation exception, immutable correction successor/history, and optimistic
  filing-phase version checks.
- Reused the authoritative docket lifecycle/restricted-access guard for every
  core mutation/query. Identifier search omits restricted, inaccessible,
  cross-tenant, missing, and terminal docket records.
- Enforced parent-docket-before-child lock order so lifecycle transitions
  cannot race a filing-phase change.
- Persisted enabled asset types, jurisdictions, offices, timezone/calendar,
  working-day policy, taxonomy/event/rule versions, notification/critical-event
  policy, escalation owner, provider references/terms, current tests, tenant
  workspace enablement, and per-automation selections.
- Kept provider credentials outside the new store and linked users to the
  canonical integration, role, and team administration surfaces.
- Made connection/source-open, notification, and deadline tests safe dry runs:
  no external call, message, legal deadline, filing, fee, or provider effect.
- Allowed manual workspace use when a provider is unavailable; a missing or
  failed test blocks only its affected automation.
- Reset tenant enablement on configuration version change and ignored stale
  test evidence. Tenant configuration cannot override missing authorization,
  entitlement, or environment rollout gates.
- Added a mobile-responsive `/app/ip` setup workflow with all grouped actions
  and administration links visible and usable at narrow viewport width.
- Published the implementation model and ownership boundaries in
  `docs/ip-implementation/IP_CORE_RECORD_MODEL.md` and extended the signed
  ownership ledger without introducing a duplicate secret, identity, Matter,
  Client, audit, notification, or deadline owner.

## Verification

### Focused command, UJ-01, and migration regression

```text
apps/api/.venv/Scripts/pytest.exe \
  tests/test_ip_record_workflow.py \
  tests/test_ip_workspace_configuration.py \
  tests/test_20260807_ip_workspace_migration.py \
  tests/test_20260807_ip_core_migration.py -q
8 passed
```

This proves typed identifiers, filing invariant, duplicate reconciliation,
correction history, tenant isolation, restricted-record search suppression,
normal workspace setup, all three UJ-01 exception paths, safe dry runs,
optimistic configuration versions, and both migration rollback/re-upgrade
paths.

### Broader IP, governance, and schema regression

```text
apps/api/.venv/Scripts/pytest.exe \
  tests/test_ip_record_workflow.py \
  tests/test_ip_workspace_configuration.py \
  tests/test_ip_readiness.py tests/test_ip_core_records.py \
  tests/test_ip_capability_catalog.py tests/test_ip_prd_slices.py \
  tests/test_ip_program_manifest.py tests/test_ip_ownership_ledger.py \
  tests/test_ip_arch_ops_contract.py \
  tests/test_20260807_ip_workspace_migration.py \
  tests/test_20260807_ip_core_migration.py tests/test_migration_order.py \
  tests/test_schema_fk_indexes.py -q
95 passed
```

This same 95-test gate was rerun after restacking on canonical `8c949747` and
passed in 86.46 seconds with no failures or skips.

### Responsive web and type contract

```text
npm test -- --run app/app/ip/page.test.tsx
1 file passed; 6 tests passed

npm run typecheck
passed
```

The web tests assert the user-visible setup surface at narrow viewport width,
including Save configuration, each safe readiness test, manual enablement,
automation enablement, terms control, and role/team/integration links.

### Static, generated, and governance gates

```text
apps/api/.venv/Scripts/ruff.exe check <changed API and test files>
All checks passed

apps/api/.venv/Scripts/python.exe ../../scripts/ip_ownership_ledger.py validate
IP ownership ledger valid

apps/api/.venv/Scripts/python.exe ../../scripts/ip_arch_ops_contract.py validate
ARCH-OPS contract valid

apps/api/.venv/Scripts/python.exe ../../scripts/ip_program_manifest.py validate
IP program manifest valid: 436 requirements, 50 families, 68 journeys,
317 atomic paths

git diff --check
passed
```

The OpenAPI TypeScript client was regenerated from the current schema. Warnings
were limited to existing third-party Starlette and Python SQLite datetime
adapter deprecations. No required test was skipped, quarantined, retried, or
allowed to fail.

### CI-discovered PostgreSQL and dependency fixes

The first independent PR run found two genuine release blockers that the
SQLite-focused local gate could not prove:

- PostgreSQL rejected the generated
  `ix_ip_workspace_configurations_provider_terms_accepted_by_membership_id`
  name because it was 71 characters, above PostgreSQL's 63-character limit.
  The migration and SQLAlchemy metadata now use the explicit, FK-leading
  `ix_ip_workspace_config_terms_actor` index.
- npm audit reported the transitive `nanoid` 3.3.16 lock as high severity under
  `GHSA-2v37-7h3g-55p8`. The compatible lock was refreshed to 3.3.18.

The corrected candidate was reproduced locally against the same PostgreSQL 17
and pgvector family as CI:

```text
pytest -q -m postgres tests/test_postgres_validation.py
14 passed

pytest tests/test_20260807_ip_workspace_migration.py \
  tests/test_ip_workspace_configuration.py tests/test_schema_fk_indexes.py \
  tests/test_migration_order.py -q
10 passed

npm ci --ignore-scripts
found 0 vulnerabilities

npm audit --audit-level=high
found 0 vulnerabilities
```

The ephemeral PostgreSQL container was stopped and removed after verification.
The two synthetic calendar fixtures were also changed to the obvious
low-entropy `test-calendar` value and folded into the original implementation
commit so gitleaks sees no flagged value anywhere in PR history. A fresh full
CI/Security/CodeQL run remains required before merge.

The next CodeQL aggregation found a genuine module-level import cycle: the IP
record request schema imported normalization helpers from the record service,
which reaches the operations service/schema chain. The pure identifier-kind,
normalization, and ownership rules now live in dependency-free
`services/ip_identifier_rules.py`; both the request schema and record service
depend inward on that module. Existing imports of `normalize_ip_identifier`
from the record service remain compatible. Verification after the extraction:

```text
ruff check <identifier rules, record schema, and record service>
All checks passed

python -c <schema-first then service import>
schema-first import passed

python -c <service-first then schema import>
service-first import passed

pytest <95-test IP/governance/schema regression> -q
95 passed
```

The corrected import graph still requires a fresh independent CodeQL pass.

The subsequent full Playwright app suite passed 127 scenarios but correctly
failed both pre-existing `ram-2026-08-01-bugs.spec.ts` IP journeys. Screenshots
showed the new `IP workspace setup` readiness surface: the synthetic fixture
granted entitlement and rollout, but had not saved/enabled the newly required
tenant workspace configuration. The application remained correctly
fail-closed. The fixture now persists a provider-free, manual-docketing
configuration and explicitly enables it before exercising the older docket
journeys; it does not bypass the readiness contract or enable an automation.

The exact dated spec was rerun against a fresh production Next build with CI's
build-time API origin (the `NEXT_PUBLIC_*` value is baked into the client):

```text
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000 npm run build:web
compiled, typechecked, and generated 65/65 pages

npx playwright test tests/e2e/ram-2026-08-01-bugs.spec.ts \
  --config playwright.app.config.ts
2 passed (20.3s)
```

This re-proves validated trademark creation, every grouped action visible
within a 360px viewport, and the linked-Matter evidence, obligation, coverage,
and reconciliation workflow. The first local attempt used a build without the
CI API origin and timed out at sign-in; it is recorded as invalid environment
evidence, not a product pass or failure.

## Production release closure — 8 August 2026

The implementation and its production-hardening follow-ups passed independent
CI, Security, CodeQL, PostgreSQL/pgvector, generated-client, dependency, and
Playwright gates before merge. The final release commit is
`63bc7587af4a5c3157462c6f668707cc8a08db9c`; local `main` and `origin/main`
were verified at that exact commit. PR #181's CI run `31254059062` passed all
eight API coverage shards, aggregate coverage, Ruff, PostgreSQL/pgvector, web,
and the 5m06s Playwright application suite. CodeQL and every security gate also
passed.

The canonical deployment completed in the required order:

- API image digest
  `sha256:b49875ca3ad201cf195bb1d693d498933adeb5c859f80e884c9ccc3952b13efb`,
  revision `caseops-api-00250-jzl`, 100% traffic;
- web image digest
  `sha256:cd00b3bf5a4a130efbdb4e870095ed1fde21d309f20e2a64f7a699a77c0a5aa3`,
  revision `caseops-web-00230-8ff`, 100% traffic;
- migration execution `caseops-migrate-job-dc9bp`, successful in 13.07s; and
- all six recurring jobs pinned to the immutable API digest with canonical
  schedules, targets, time zones, and invoker identity.

The independent HTTPS release verifier returned the exact API revision, web
revision, and full release SHA. Production workflow `31255101441` then checked
out that exact serving commit, passed the complete 61-test dated RAM batch, and
passed the notice upload/reply/sent/filter journey. The three notice document
uploads returned HTTP 200 in 0.446s, 1.347s, and 1.145s.

### Production-hardening incidents closed during release

- The API image now preloads the Voyage tokenizer so upload background
  processing does not download tokenizer state on a new revision.
- The ClamAV startup probe converges immediately every two seconds while
  preserving its four-minute failure budget. Cloud Run's omitted zero delay is
  normalized by the deploy verifier.
- The multi-container deploy explicitly preserves API ingress port 8080 and
  the deployed ClamAV image in the same revision.
- The bundled clamd 1.0.2 client previously applied its 30-second timeout only
  after TCP connect. A production reply upload exposed a 207.903s OS-level
  connect retry. The connection now uses the configured timeout from the start
  while antivirus remains required and fail-closed; the exact production
  rerun above proves the corrected path.

No arbitrary seven-day natural-scheduler waiting period was used. Deterministic
configuration and current execution evidence are the scheduler gate; genuine
human and provider approvals remain genuine pending items.

## Data safety and authorization

All new rows are tenant-scoped and use composite foreign keys to tenant-owned
parents. Core record reads/writes use the docket access and lifecycle guard;
workspace administration requires `ip:taxonomy_admin`; legal record reads and
writes require `ip:read` and `ip:write`. Audit metadata does not copy raw legal
identifier values.

Workspace persistence stores only provider references and accepted terms, not
credentials. It does not send notifications or contact providers. Capability,
billing entitlement, deployment rollout, and tenant allowlisting remain
independent fail-closed decisions.

## Rollout and rollback

The production migration is additive. It creates empty configuration/test
tables and performs no backfill or legal-state inference. No production IP
rollout environment flag is enabled by this candidate.

The exact merged commit passed independent CI/Security/CodeQL and PostgreSQL
migration checks, was deployed from canonical `main`, and had its exact
API/web digests and Cloud Run revisions verified. The same dated Playwright
paths passed against that serving revision. Production IP entitlements and
external legal automations remain disabled pending their later owned slices and
genuine approvals.

Before writes, `20260807_0002 -> 20260807_0001` downgrade is tested. After
writes, rollback is tenant/environment flag-off plus forward fix and
export-preserving recovery; destructive deletion of legal/configuration history
is not authorized.

There is no arbitrary seven-consecutive-day scheduler-health gate. Scheduler
health is verified deterministically for the exact production release where
applicable, while genuine named human and provider approvals remain pending.
