# IPLF-023B release evidence â€” 2026-08-09

## Verdict

Repository-controlled implementation and production verification are complete.
Feature revision `e021bb1fb509448f3282be11da3cb2ae3fbb9b39`, coverage-fix
revision `28bb5e913e0e6ae37edc8f4bb593fa8182c7c2af`, and canonical merge
`d8ac94da22f014dfa205079c9b1b049e98bd5347` are retained as distinct
identities. Release status is `deployment_verified`. Human legal fixture and
pilot/UAT acceptance remain genuinely pending. Overall verdict: `PROGRAM
INCOMPLETE` because IPLF-024A and later slices remain.

## Scope and fixtures

- Requirement facets: `CAL-OPS-01..14`, `IP-DL-01..08`, and
  `RULE-GOV-01..08` allocated to IPLF-023B.
- Journey paths: all 16 allocated normal/exception paths under UJ-08, UJ-09,
  UJ-47, and UJ-56.
- Data: isolated SQLite tenants plus a dated Playwright synthetic, non-billable
  law-firm tenant; unrestricted linked Matter; distinct owner, fixture reviewer,
  and legal activator memberships; IP India calendar with 15-17 August 2026
  weekend/holiday boundary; verified-source fixture hashes/references; critical
  primary/backup coverage; in-app-only reminder offsets.
- No real client, court, registry, filing, fee, payment, service, or external
  notification action occurred.

## Delivered assertions

1. Candidate calendars and rules are immutable and tenant governed.
2. Calendar approval rejects self-approval; legal rule activation requires a
   distinct proposer, fixture reviewer, and legal activator.
3. Fixtures are re-evaluated on the server before activation.
4. A selected rule/calendar computes 18 August from a 14 August trigger while
   recording two weekend skips and the official 17 August holiday skip.
5. Uncertain/conflicting triggers remain visible provisional obligations with
   no manufactured due date.
6. Legal proposal and confirmation are separate; no operational effect occurs
   before explicit confirmation.
7. Confirmation is optimistic-concurrency protected and blocks critical dates
   without acknowledged primary and backup/escalation coverage.
8. Confirmation atomically creates the canonical Matter deadline, optional
   internal target, responsibility evidence, accepted coverage, and durable
   in-app reminders.
9. Recalculation creates a reviewable successor while the confirmed predecessor
   remains active until acceptance.
10. Override rejects stale impact tokens, preserves the original calculation,
    cancels only source-dependent projections/reminders, and preserves unrelated
    Matter work.
11. Generic operational task completion cannot complete the legal deadline;
    dedicated evidence/attestation completion is required.
12. Emergency rule disable blocks new calculations, preserves history, and
    surfaces affected exception state.
13. Rule governance, dockets, deadlines, and workspaces fail closed across
    tenant and Matter-access boundaries.
14. The 360-pixel UI exposes every grouped action without horizontal overflow,
    explains the calculation/citation, and keeps the exception queue visible.

## Local automated evidence

| Layer | Exact command/scope | Result |
|---|---|---|
| API lint | `uv run ruff check src tests` in locked worktree environment | passed |
| API focused integration | deadline workflow + foundation + prosecution | 13 passed; zero skipped |
| API widened migration/adjacent | foundation migration + deadline workflow + core/prosecution | 14 passed; zero skipped |
| API workflow and schema regression | `uv run pytest -q tests/test_ip_deadline_workflow.py --cov=caseops_api.schemas.ip_deadlines --cov-report=term-missing` | 9 passed; deadline schema module reached 99% line coverage |
| Web type contract | `npm run typecheck --workspace @caseops/web` | passed |
| Web unit | `npm run test --workspace @caseops/web -- app/app/ip/page.test.tsx` | 8 passed; zero skipped |
| Production web build | `NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000 npm run build --workspace @caseops/web` | compiled, TypeScript, 65 pages, and finalization passed |
| Dated local E2E | `npx playwright test tests/e2e/iplf-023b-deadline-workflow-2026-08-09.spec.ts --config playwright.app.config.ts --project app-chromium` | 1 passed in 20.4s; browser journey 8.6s |
| OpenAPI contract | `scripts/dump_openapi.py` plus pinned `openapi-typescript@7.13.0` | committed contract regenerated successfully |

Known output was limited to existing FastAPI/httpx and Python 3.12 SQLite
adapter deprecation warnings plus the existing Next.js Edge Runtime warning.
No required assertion was relaxed and no sleep was added. The local E2E harness
was rebuilt with its declared `127.0.0.1` API origin after the retained trace
proved that a prior ad-hoc build's `localhost` URL was correctly blocked by CSP;
the same pre-existing dated IP test reproduced that harness mismatch.
The first independent CI run passed every one of the eight API test shards but
identified a fractional schemas-bucket line-coverage regression. Dedicated
negative validator cases raised the new deadline schema module to 99% without
lowering any repository coverage threshold. The corrected revision then passed
the fresh complete matrix described below.

## Independent CI and canonical merge

- Pull request: `#189`.
- Corrected head: `28bb5e913e0e6ae37edc8f4bb593fa8182c7c2af`.
- CI run: `31296469587`, successful after one failed-job retry for a transient
  `setup-uv` external connect timeout. The retry used the unchanged commit.
- All eight deterministic API coverage shards passed; the combined unchanged
  per-area coverage gate passed.
- API Ruff and manifest/ownership/ARCH-OPS validators passed.
- PostgreSQL + pgvector validation passed.
- Web functional-runner security tests, TypeScript, Vitest coverage, and the
  production Next.js build passed.
- The complete Playwright app suite passed.
- Security run `31296469607`, CodeQL run `31296469572`, and Codex review run
  `31296469584` passed, including OpenAPI drift, gitleaks, license allow-list,
  npm audit, pip-audit, and Cloud Run secret-reference policy.
- PR `#189` merged to canonical `main` as
  `d8ac94da22f014dfa205079c9b1b049e98bd5347`; local `main` and `origin/main`
  were both verified at that exact commit before deployment.

## Security and legal-integrity controls

- Server route capabilities distinguish `ip:rules_propose`,
  `ip:rules_activate`, `ip:approve`, and `ip:read`; frontend visibility mirrors
  but does not replace them.
- Rule impact/activation/transition validates the proposing tenant; a tenant
  workspace includes an active version only when its own policy selected it.
- Every deadline command resolves the tenant-scoped docket and existing Matter
  access/ethical-wall contract.
- Exact expected versions and impact tokens reject stale writes and changed
  dependency sets.
- Reminder intents use the existing notification owner and channel `in_app`
  only. External delivery is neither called nor enabled.
- Actor labels, source references/hashes, calculation inputs/trace, citations,
  engine/source versions, overrides, and completion evidence remain retained.
- The seven-consecutive-day scheduler observation is intentionally not a gate.
  Exact scheduler configuration, immutable image, run/probe, and policy evidence
  remain mandatory at deployment.

## Files

- `apps/api/src/caseops_api/services/ip_deadline_workflow.py`
- `apps/api/src/caseops_api/api/routes/ip_operations.py`
- `apps/api/src/caseops_api/schemas/ip_deadlines.py`
- `apps/api/src/caseops_api/services/deadlines.py`
- `apps/api/tests/test_ip_deadline_workflow.py`
- `apps/web/app/app/ip/page.tsx`
- `apps/web/app/app/ip/page.test.tsx`
- `apps/web/lib/api/endpoints.ts`
- `apps/web/lib/api/openapi-types.ts`
- `tests/e2e/iplf-023b-deadline-workflow-2026-08-09.spec.ts`
- `tests/e2e/ram-2026-08-09-prod.spec.ts`
- `docs/ip-implementation/IP_DEADLINE_WORKFLOW.md`

## Exact production deployment proof

| Control | Exact result |
|---|---|
| Canonical release | `d8ac94da22f014dfa205079c9b1b049e98bd5347` (`d8ac94d` runtime identity) |
| API Cloud Build | `242fc41a-6779-4c22-81a6-87654f3aa126` |
| Web Cloud Build | `0732243f-4695-49dd-8ac1-7295f2b28cbd` |
| API immutable image | `sha256:158c809accbc466ef162f47b932253ad4f0950dbaafc24f80b730c2adca61905` |
| Web immutable image | `sha256:c7e495d442ecc00f98fa598d2e65c137e6777e49b0e107bb6f66947749b59e82` |
| Migration | `caseops-migrate-job-5wj7s` completed successfully at existing head `20260807_0005` |
| API serving revision | `caseops-api-00258-zv8`, Ready/Active/ContainerHealthy, 100% untagged traffic |
| Web serving revision | `caseops-web-00238-n7s`, Ready/Active/ContainerHealthy, 100% untagged traffic |
| Runtime probes | `/health` returned `{"status":"ok"}`; API and web release endpoints both returned `d8ac94d` |
| Scheduler inventory | six required scheduler-to-job bindings passed with the exact API digest; superseded midnight case-tracking schedule remained paused |
| Production workflow | `31298468728`, successful in 23m43s against the exact SHA and revisions |
| Production RAM batch | 58 passed in 12.6m |
| Dated IPLF-023B production assertion | `ram-2026-08-09-prod.spec.ts` passed at 360px in 6.6s |
| Production Notice module | 2 passed in 19.9s |

The dated production test is deliberately read-only: the tester tenant is
unentitled, so it proves the legal workspace makes no deadline/rule/calendar
requests and exposes no grouped actions behind the fail-closed readiness gate
at 360 pixels. It does not fabricate legal fixture approval or mutate real
legal data.

The seven-consecutive-day natural scheduler observation is not an acceptance
blocker. Exact scheduler configuration, immutable job image, current health,
and production browser evidence passed in the release window.

## Remaining program and human gates

Named human legal approval of production legal fixtures and pilot/UAT evidence
remain genuine acceptance gates and are not inferred from automation. IPLF-024A
and later M2-M10 slices remain unimplemented; therefore the program remains
`PROGRAM INCOMPLETE` even though IPLF-023B is `deployment_verified`.
