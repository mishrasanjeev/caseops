# IPLF-045A/B Exact Release Verification - 24 August 2026

**State:** `deployment_verified` for repository implementation. This record
does not claim lawyer, provider, Registry, legal-SME, or independent UAT
acceptance.

## Exact revision chain

- Pull request: `#319`, head
  `e91167de2b039e3337b66e3c022f50e1d4af7af2`.
- Required PR checks: 29 successful, 2 expected skips, no failure or pending
  check. The PR was merged with an exact head-SHA fence.
- Main/deployed revision:
  `425715f25f2645cd1ae5a464380819e0ac094d67`.
- Exact-main CI run `32659808723`: successful, including 13 API pytest
  shards, combined coverage, Ruff, PostgreSQL/pgvector, web typecheck,
  Vitest, production build, and Playwright.
- Exact-main Security run `32659808708`: successful.
- Exact-main CodeQL Advanced run `32659808713`: successful.

## Deployment

The canonical guarded deployment script ran from a clean detached worktree at
the exact main revision.

- API Cloud Build: `752244f3-59bb-444b-8298-6f6aac43abea`;
  digest `sha256:35560452f4e0e7afd8d71817f8f24a852066edf6bb44c537c374c6b0f92cd7b2`.
- Web Cloud Build: `cd85d54d-b9b1-4212-badc-71bdb161163c`;
  digest `sha256:65ced5dfbf1c22b737477d9366ab8ef3844ffbc875dc574d07c0da3bcbc40e3d`.
- Migration execution `caseops-migrate-job-6ls2r` completed before traffic.
- Scheduler inventory reconciliation passed with enabled/paused state matching
  the committed inventory.
- API revision `caseops-api-00330-s2w` and web revision
  `caseops-web-00308-mlq` each received 100% latest traffic.
- Both services reported release tag `425715f`; `/api/health` returned
  `{"status":"ok"}`. Image staleness, API/web tag parity, rule governance,
  and the immediate ClamAV startup probe passed.
- `scripts/verify_deployed_release.py` independently resolved both serving
  revisions to the full expected SHA.

## Production acceptance

The push-triggered production verifier run `32659808715` exhausted its
1,500-second release wait while production still served the previous exact
release. It did not reach Playwright and is not represented as an application
test failure or a pass.

After deployment, workflow-dispatch run `32661736243` checked out and tested
the exact serving SHA `425715f25f2645cd1ae5a464380819e0ac094d67` and completed
successfully in 18m15s:

- exact API/web identity and checkout: passed;
- production QA secret gate, dependency install, and Chromium setup: passed;
- full RAM production batch: passed;
- IPLF-027B A0 quiescence acceptance: passed;
- notice-module production suite: passed;
- IPLF-037B renewal acceptance configuration gate: passed;
- IPLF-037B renewal execution: skipped because no approved external legal
  fixture IDs are configured.

The renewal skip remains the independent IPLF-037B legal-governance activation
blocker. It does not block IPLF-045 repository deployment verification and is
not converted into acceptance by this record.

## Result

IPLF-045A/B canonical drafting-target generalization and trademark pleading
workflow are implemented, locally verified, merged, deployed, and verified on
their exact production revision. IPLF-046 validation/lifecycle controls and
IPLF-047 legal-SME fixture/UAT automation remain separate work.
