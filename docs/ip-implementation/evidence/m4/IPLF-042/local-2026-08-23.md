# IPLF-042 local verification - 23 August 2026

## Scope

This record covers the opponent-side opposition workflow and deadline slices on
implementation revision `595b7a7e1b923594024e2794c424a814ff7dbf09`. The
change extends the deployed IPLF-040 aggregate and introduces no migration or
parallel legal-record owner.

## Implemented contract

- explicit application/opposition identifiers and pending Registry opposition
  number allocation;
- exact governed `opposition` / `opponent` notice, Rule 45, and Rule 47
  deadline selection with confirmed trigger dates and distinct primary/backup
  ownership;
- signed TM-O notice filing and corrected refiling facts on the canonical
  append-only docket timeline;
- separate sourced notice service and explicit Rule 45 and Rule 47 elections;
- watch-hit closure without filing, limitation-aware client-instruction
  escalation, and rejected-filing correction through canonical shared tasks;
- fail-closed stage and action sequencing, including no filed stage after a
  Registry rejection;
- capability and tenant boundaries, action-aware duplicate detection, and
  atomic event/task writes;
- opponent workspace, user guide, law-firm page, and machine-readable public
  descriptions aligned to the proven behavior.

## Deterministic verification

| Gate | Result |
| --- | --- |
| API opposition foundation, applicant, opponent, canonical-record, and shared-work tests | `30 passed` |
| Opponent schema coverage | `100%` |
| Opponent service coverage | `84%` |
| Full web test suite | `137 files / 714 tests passed` |
| Web typecheck and 72-route production build | passed |
| Incident/applicant/opponent Playwright regression | `3 passed` in 43.9 seconds |
| Final opponent Playwright rerun after trigger-date correction | `1 passed` in 29.7 seconds |
| OpenAPI quality/route matrix and pinned generated client | passed and regenerated additively |

The API journeys cover `UJ-13-NORMAL`, `UJ-13-EXC-01`, `UJ-13-EXC-02`, and
`UJ-13-EXC-03`. The browser journey creates distinct owner, legal, and reviewer
identities, activates three synthetic governed rule versions and one working
calendar, confirms three critical deadlines, records the signed notice and
service, records Rule 45 and Rule 47 elections, reaches
`reply_evidence_filed`, and verifies the opponent workspace at a mobile
viewport. Unit coverage proves that each evidence-stage proposal resets its
base date to the current confirmed trigger event rather than retaining the
notice limitation date.

## Fail-closed boundary

No synthetic rule, source, identifier, filing, task, or legal conclusion is a
production fact. Hosted exact-head CI, exact-main verification, deployment,
production identity/health, and independent human/legal/provider/UAT
acceptance are not claimed by this local record. Further-evidence leave,
extension detail, hearing, order, appeal, settlement, translations, security
for costs, and downstream application disposition remain allocated to
IPLF-043 and later slices.
