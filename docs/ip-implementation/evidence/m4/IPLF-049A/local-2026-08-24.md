# IPLF-049A local implementation evidence

**Date:** 2026-08-24

**Program status:** `PROGRAM INCOMPLETE`

## Delivered scope

- Extended the canonical `IpProceeding`, `IpIdentifier`, and append-only
  `IpDocketEvent` owners for distinct rectification, cancellation, and non-use
  removal proceedings without a migration or parallel legal-record table.
- Added typed proceeding-number intake, exact proceeding/identifier ownership,
  post-registration stage-template selection, and an explicit rejection of
  opposition numbers and opposition templates.
- Added versioned profile and action contracts with tenant, docket lifecycle,
  proceeding version, responsible-member, source, evidence, correction, and
  audit fences.
- Restricted disposition candidates to the exact proceeding type and retained
  a separate authorized review without mutating the registration.
- Kept authoritative rule automation manual and fail-closed. No Indian rule
  map, deadline, filing, Registry status, or legal effect is inferred.

## Verification

- `apps/api/tests/test_ip_post_registration.py`: five tests passed, including
  complete branch coverage of the new schema validators.
- API Ruff checks passed for all changed schemas, services, and routes.
- API OpenAPI import and generated web client contract passed.
- No schema change; the existing migration head remains authoritative.

## Ownership review

The implementation extends the existing IP bounded context. Shared Matter,
document, deadline, hearing, notification, billing, audit, and access owners
remain unchanged. Production activation remains blocked until exact-head CI,
merge, exact-main CI, exact-SHA deployment, and production verification pass.
