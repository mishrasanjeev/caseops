# IPLF-040B opposition workspace baseline - production release evidence

**Date:** 2026-08-23

**Implementation PR:** `#311`

**Implementation head:** `e1276ee631bb647b4707e93c48be0a5f89741ca5`

**Reliability correction PR:** `#312`

**Correction head:** `1ab813431a28431875210dc5924e392a8114dbca`

**Merge and deployed revision:** `f8181510ce0fab6eb2da636555c5d3bce9a28333`
**Status:** bounded baseline deployment verified; complete UJ-12/UJ-13 remains open

## Exact-candidate and exact-main gates

- PR `#311` passed its required hosted checks before exact-head merge as
  `26e1939b7e4d4916a88d67b123dbe6bcc106b0aa`.
- Fresh-main CI exposed a SQLite writer-lock event-loop stall in a pre-existing
  lifecycle browser regression. PR `#312` narrowed the process mutex to the
  explicit reliability claim paths, moved synchronous lifecycle work off the
  event loop, and made uploaded object keys portable on Windows without
  changing stored original filenames.
- PR `#312` passed CI run `32620407290`, including all 13 API shards,
  PostgreSQL, web, coverage, and the 153-case Playwright suite. Security and
  CodeQL passed at the exact correction head.
- The correction merged with the expected-head fence. Fresh-main CI run
  `32621415194`, security run `32621415179`, and CodeQL Advanced run
  `32621415157` all completed successfully at the deployed revision.
- Local correction verification included 36 focused DB/lifecycle tests, 38
  reliability/billing tests, 8 storage tests, three repeated lifecycle browser
  passes, the notice browser path, and the full app suite with 152 passed and
  one provider-gated skip.

## Deployment identity

- Guarded deployment ran from a clean detached worktree at the exact merge
  revision.
- API build `ce71aafb-dba8-4962-a7da-f326c6769860` and web build
  `2908da08-f487-4eb7-a453-5b3391df0324` succeeded.
- Migration execution `caseops-migrate-job-rq4dq` succeeded in 16.21 seconds
  before either service received traffic. It ran the immutable API image and
  upgraded through Alembic revision `20260823_0002`.
- API revision `caseops-api-00327-gpv` and web revision
  `caseops-web-00305-gks` each received 100 percent traffic and serve release
  `f8181510ce0fab6eb2da636555c5d3bce9a28333`.
- API image digest:
  `sha256:cf169ab7ec27eb04e8d4f3d2e413e1307bdfa52fda03d17b40d68ba6772df4d4`.
- Web image digest:
  `sha256:67647ed44ea4fdfb327de2ba961e8cf3cb84d1332ec769a3373fde13e85aef6e`.
- Production health returned `{"status":"ok"}`. Rule governance remained
  explicitly disabled and the ClamAV immediate startup guard passed.
- Scheduler reconciliation passed for six recurring jobs at the immutable API
  digest. Authority metadata remained intentionally paused and the superseded
  midnight case-tracking scheduler remained paused.

## Production acceptance interpretation

The push-triggered verifier `32621415158` timed out while production still
served the predecessor revision. This was the expected fail-closed stale-release
result; it did not execute product acceptance against a mixed release.

Workflow-dispatch verifier `32622860761` then confirmed exact API/web identity
at the full deployed SHA. Its production results were:

- the 134-test RAM batch passed;
- IPLF-027B A0 quiescence acceptance passed;
- notice-module production acceptance passed; and
- the pre-existing IPLF-037B renewal acceptance failed before fixture creation
  because four genuine governed production IDs remain unset.

The unset values are
`CASEOPS_IP_RENEWAL_CALENDAR_VERSION_ID`,
`CASEOPS_IP_RENEWAL_RULE_VERSION_ID`,
`CASEOPS_IP_RENEWAL_GRACE_RULE_VERSION_ID`, and
`CASEOPS_IP_RENEWAL_NEXT_TERM_RULE_VERSION_ID`. They are an independent
legal-governance activation gate. No identifier was fabricated and this
release did not enable rule governance.

## Deliberate remaining scope

This release proves the deployed opposition workspace baseline: canonical
application/opposition identifier separation, represented-side intake,
effective-dated parties, lawyer-confirmed profile history, challenged scope,
relied-on rights, service facts, readiness gates, and guarded stage
transitions. It does not claim complete `IP-OPP-01` through `IP-OPP-24` or all
UJ-12/UJ-13 paths.

IPLF-041 through IPLF-043 retain applicant/opponent TM-O work product,
rule-versioned deadline/evidence elections, exceptional decisions,
hearing/order/appeal detail, and the remaining path tests. Those capabilities
must extend the canonical records deployed here and remain fail-closed until
their own evidence passes.
