# IPLF-040B local implementation evidence - 2026-08-23

## Evidence identity

- Implementation revision: `74488b00b519095b42c09450ac73e5b48bd5b210`
- Base revision: `99b568df05456a6a0a327c1af124132da9ef8cf2`
- Environment: local SQLite, ephemeral PostgreSQL 17 with pgvector, and fresh production-build API/web Playwright servers
- Data: deterministic synthetic applicant-side and opponent-side trademark records; no live registry filing, legal conclusion, provider credential, or fabricated production identifier

## Delivered baseline

IPLF-040B extends the existing canonical owners instead of creating parallel opposition records. It adds a proceeding-scoped link to `ip_parties_and_roles`, stores lawyer-confirmed profile revisions in append-only `ip_docket_events`, and continues to use `ip_proceedings`, `ip_identifiers`, the IP docket, Matter links, access policy, and audit context.

The existing `/app/ip` docket workspace now supports:

- opposition intake from a linked trademark application with represented side, origin, office, jurisdiction, and a distinct opposition number or explicit pending allocation;
- distinct application-number and opposition-number display;
- applicant, opponent, agent, and counsel roles with effective-dated revisions;
- structured grounds, lawyer-authored detail, AI-assisted classification disclosure, challenged class scope, and opponent relied-on rights;
- applicant service facts and opponent instruction/limitation readiness;
- source and document references, evidence references, optimistic lifecycle/proceeding/profile fences, and append-only profile history;
- readiness-gated canonical stage transitions, exceptional authority, and evidence-backed closure controls; and
- keyboard/addressable segmented controls, source links, responsive layout, and tenant/capability enforcement.

## Verification

| Gate | Result |
| --- | --- |
| Focused opposition API, migration, lifecycle, governance, route, OpenAPI, and adjacent IP suite | 66 passed |
| Final focused API and migration regression after review fixes | 12 passed |
| PostgreSQL 17 append-only stage/profile event guard | 1 passed, 97 deselected |
| Full web unit suite | 135 files, 704 tests passed |
| Opposition component regression after accessibility fix | 2 passed |
| Web typecheck and production build | passed |
| Applicant-side and opponent-side real API/UI Playwright journey | 1 passed in 10.3 seconds |
| Data-governance map generate/render/validate and runtime class projection | passed; 272 tables and 4,223 columns |
| Ruff, migration order, FK index, route coverage, and diff checks | passed |

The browser proof creates both represented-side variants, saves lawyer-confirmed profiles through the real API, verifies readiness, and advances the applicant proceeding from draft to notice filed. It also exercises the distinct identifiers, AI-assisted classification disclosure, optimistic fences, and responsive overflow assertion.

## Review corrections

The final review corrected four defects before evidence was pinned:

1. Mutation callbacks now receive submit-time payloads, avoiding stale pre-hydration optimistic versions.
2. The represented-side segmented control now exposes correct accessible names and pressed states.
3. The browser stage assertion uses a stable scoped hook and bounded action waits.
4. Application-number readiness now accepts only current confirmed `application` identifiers, not another identifier type attached to the application.

## Deliberate remaining scope

This evidence does not claim all `IP-OPP-01` through `IP-OPP-24` or complete UJ-12/UJ-13 normal and exception paths. IPLF-041, IPLF-042, and IPLF-043 still own the role-specific TM-O pleading packages; Rules 45, 46, and 47 deadline/evidence elections; deemed-abandonment and extension decisions; evidence package relationships; hearing preparation; order, appeal, withdrawal, and settlement detail; translation/original-language handling; provider reconciliation; reports; and production acceptance.

The slice remains `in_progress` / `not_run` / `blocked` in the canonical manifest until those allocated paths and exact PR/main/deployment gates are complete. The passing results above prove this bounded baseline only.
