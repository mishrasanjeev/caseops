# IPLF-047A/B Exact Release Verification - 24 August 2026

**State:** `deployment_verified` for repository implementation. The committed
fixture pack remains synthetic and `candidate_unapproved`; this record does not
claim lawyer, legal-SME, Registry, provider, or independent UAT approval.

## Exact revision chain

- Pull request `#321`, head
  `e4b33159e2a8097f9004b3732a3896d4c4642643`, passed all required PR checks and
  merged with the exact-head fence.
- Main implementation revision:
  `d130096bb004f604e9ddbbdeca84034fccbdb4c0`.
- Exact-main CI rerun `32671881679`: successful, including all API shards,
  aggregate coverage, PostgreSQL/pgvector, web build, and 155 Playwright tests.
- The exact implementation revision was deployed as API revision
  `caseops-api-00332-6zq` and web revision `caseops-web-00310-gmh`; health and
  full release identity checks passed.

## Production verification

The first post-deployment verifier run `32675161006` passed 75 production tests
before a final sent-notice filter timed out. The matching endpoint had already
returned successfully elsewhere in the same suite; the assertion lacked the
repository's exact-response wait and production UI margin.

PR `#322` added the exact query/matter/owner response fence and retained the
content assertion. Its merge revision
`349710dbfeecca5f8c51ef058d92bb807c9d1e0c` passed exact-main CI, Security, and
CodeQL, then deployed as API revision `caseops-api-00333-92n` and web revision
`caseops-web-00311-kst`. Manual production verifier run `32679072635` passed the
full RAM production batch, A0 check, and notice module against that exact
serving revision.

## Result

IPLF-047 repository implementation is merged, deployed, and production
verified. Authoritative legal-fixture activation remains fail-closed until
qualified, distinct human reviewers approve an exact fixture version; no such
approval is inferred from software release evidence.
