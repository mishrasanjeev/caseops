# IPLF-046A/B Local Verification - 24 August 2026

**State:** `implementation_complete_local_verified`. Release remains
`blocked` until exact-head PR CI, merge, deployment, and production acceptance.
No Registry submission, legal approval, provider acceptance, or named UAT is
inferred.

**Implementation revision:**
`470ddcd304d52ed0b7c74b03d0e3cf0224a4ea3f`

## Delivered

- Added deterministic, non-LLM validation of frozen trademark pleading
  revisions against current proceeding, identifier, deadline, authority, and
  document-version records.
- Added fail-closed checks for unresolved placeholders, changed or lost
  sources, source SHA/state drift, unknown source/exhibit anchors, and generic
  Annexure/Exhibit references without exact immutable anchors.
- Reused the canonical drafting engine and revision comparison path; no
  parallel IP drafting store or writer was introduced.
- Revalidated the same report before approval and before marking filed.
- Added separate human-controlled `file`, `reject_filing`, and `serve` events
  with actor, acted-on immutable version, external reference, event time,
  method, notes, and audit metadata.
- Preserved rejected and originally filed artifacts while allowing a corrected
  immutable revision to pass through review, approval, finalization, and
  refiling.
- Added Registry-profile DOCX metadata and a filing ZIP that keeps the filed
  document separate from the internal generation manifest and checklist.
- Added validation, comparison, lifecycle, filing-bundle, history, guide, and
  law-firm product surfaces in the existing opposition pleading workspace.
- Added additive migration `20260824_0001` and registered
  `draft_reviews.metadata_json` in the data-governance map and generated
  data-class projection.

## Local verification

- API Ruff over `apps/api/src` and `apps/api/tests`: passed.
- Broad API regression across pleading, all opposition workflows, live role
  guards, route coverage, migration order, and OpenAPI quality: 53 passed.
- Program/data governance tests across manifest, governance map/registry,
  data-class registry/projection, ownership ledger, and M2 audit: 56 passed.
- Program manifest, data-governance map, data-class projection, and M2
  ownership validators: passed independently.
- Alembic reports one head: `20260824_0001`.
- Web typecheck: passed (`next typegen` plus `tsc --noEmit`).
- Focused pleading/opposition component tests: 6 passed. The full web suite
  passed earlier on the same UI/API changes: 140 files and 722 tests.
- E2E production-build journey: 1 passed in 36.2 seconds. It bootstrapped a
  real local tenant and drove sign-in, generation, placeholder/exhibit
  blockers, correction, comparison, review, approval, finalization, bundle
  download, filing rejection, corrected refiling, service, and visible
  append-only history.
- Web production build: passed with all 72 route entries generated.
- `git diff --check`: passed.

## Boundaries

- Filing and service are records of human-controlled external actions; CaseOps
  does not submit to or mutate the Trade Marks Registry in this slice.
- IPLF-047 legal-SME fixtures and UAT automation remain independently pending.
- No production deployment or deployed IPLF-046 acceptance is claimed by this
  local checkpoint.
