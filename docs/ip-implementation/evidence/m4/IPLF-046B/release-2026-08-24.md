# IPLF-046A/B Exact Release Verification - 24 August 2026

**State:** `deployment_verified` for repository implementation. This record
does not claim lawyer, provider, Registry, legal-SME, or independent UAT
acceptance.

## Exact revision chain

- Pull request: `#320`, head
  `9e7fb1ef2ec6fde54b35c071275c14b968688075`.
- Every required PR check passed, including 13 API shards, aggregate coverage,
  PostgreSQL/pgvector, production web build, broad Playwright, Security, and
  CodeQL. The PR was merged with an exact head-SHA fence.
- Main/deployed revision:
  `352c469eccfda8e1ba92b6ad846f5ea77c563d23`.
- Exact-main CI run `32666907361`: successful with all 19 jobs passing.
- Exact-main Security run `32666907364`: successful.
- Exact-main CodeQL Advanced run `32666907360`: successful.

## Deployment

The canonical guarded deployment script ran from a clean detached worktree at
the exact main revision.

- API Cloud Build: `d58b04c8-482c-4873-aded-a1792c6d1401`;
  digest `sha256:26e671ab51cf6b31cea84aa5d2d56cab96747662514ad3d5591a2cd9bbe7e9b5`.
- Web Cloud Build: `eee9e22b-8ea2-46af-ab7c-3f345f86fa57`;
  digest `sha256:00c3ba07d8f80173188cacc15059f49419a7d347892d57b9ca8492434c07d1b3`.
- Migration execution `caseops-migrate-job-v478p` completed before traffic.
- Scheduler inventory reconciliation passed and recurring jobs were pinned to
  the immutable API image digest.
- API revision `caseops-api-00331-472` and web revision
  `caseops-web-00309-ztc` each received 100% traffic.
- Both services reported release tag `352c469`; `/api/health` returned
  `{"status":"ok"}`.
- `scripts/verify_deployed_release.py` resolved both serving revisions to the
  full expected SHA.

## Production acceptance

The push-triggered production verifier run `32666907356` exhausted its exact
release wait before deployment completed. It did not run application tests
against a stale release and is not represented as a product failure or pass.

After deployment, workflow-dispatch run `32668672615` checked out the exact
serving SHA. Its final result and suite-level outcomes are recorded after the
workflow reaches a terminal state; this release record must not be treated as
complete before that run succeeds.

## Result

IPLF-046A/B validation, approval/filing gates, immutable filing lifecycle, and
filing bundle are merged and deployed at the exact main revision. Deployed
production regression is pending run `32668672615`. IPLF-047 legal-SME fixture
approval remains separate and is not inferred by this record.
