# IPLF-007C notification convergence evidence

**Evidence date:** 5 August 2026
**Application revision:** `508327f5e4969b9ec098a613b179f4fe39b92485`
**Environment:** local SQLite/API, exact-head CI with PostgreSQL 17, Next.js
production build, real-API Playwright, and Google Cloud production
**Program state:** `PROGRAM INCOMPLETE`
**Release state:** `deployment_verified`; implementation, PostgreSQL migration,
exact-head CI, canonical-main merge, immutable production rollout, scheduler
convergence, and dated production Playwright passed. Human acceptance remains
pending and is not inferred from automated release evidence.

## Scope and ownership

IPLF-007C implements NOTIF-01 through NOTIF-24 and the normal plus two exception
paths of UJ-11. It extends the existing `NotificationDeliveryIntent`,
`HearingReminder`, `EmailSuppression`, Matter hearing, notification preference,
SendGrid webhook, and admin notification owners. It does not create a second
dispatcher, hearing register, scheduler, email connector, or operational deadline
writer.

The slice makes the durable delivery intent the canonical send/outcome record while
retaining `HearingReminder` as a compatibility projection. A lineage table joins a
scheduled hearing reminder to its durable delivery intents. Matter deadlines and
hearings cancel pending intents through the existing lifecycle transaction.

## Implemented behavior

- Hearing time is explicit: `exact`, `session_only`, or `unpublished`. Exact times
  require a valid IANA timezone; non-exact hearings use the disclosed policy time
  and never invent a hidden 10:00 hearing time.
- Hearing policy persists recipient memberships, channels, offsets, criticality,
  escalation memberships, timezone, and the date-only reminder time.
- Scheduling produces immutable recipient/destination snapshots and a destination
  version. External content uses the minimal confidentiality mode.
- Every target is exactly one active membership, portal user, or external reference.
  Tenant and current-access checks occur at enqueue and immediately before send.
- `scheduled_for` is respected by the dispatcher. Retryable and non-retryable
  failures are distinct; provider-disabled delivery remains fail-closed.
- Critical external delivery always creates an in-app companion/fallback when an
  eligible membership exists. External delivery is not silently represented as
  successful.
- SendGrid events are append-only and idempotent even without an event identifier.
  Provider occurrence time and terminal-state precedence prevent a late `delivered`
  event from overwriting a bounce, suppression, cancellation, or dead letter.
- Bounce, suppression, failure, cancellation, retry, and delivery remain distinct
  statuses. Suppression evidence records provider/category, first/last occurrence,
  affected address, fallback, and recovery.
- Recovery is preview-first and creates a successor intent with a new destination
  version. It never rewrites original attempts or provider events. Permanent-bounce
  recovery requires a changed active destination.
- The current user can create a safe in-app test notification; the endpoint records
  zero external calls. Admin operators receive due/attempted/delivered/failure,
  suppression, bounce, fallback, stale-queue, and critical-alert metrics.
- Closing a Matter deadline or changing/cancelling a hearing neutralizes pending
  notification work atomically; no child is resurrected by legacy projection.

## Persistence and migration

Migration `20260804_0004_notification_recovery_convergence` is additive. It expands
hearing policy/time precision, durable intent target/version/scheduling/recovery
metadata, suppression recovery evidence, provider event idempotency/application
metadata, and reminder-to-intent lineage. Existing reminder rows are backfilled into
durable intents and linked without sending. The migration completed from a clean
database during API startup. Rollback is schema-only and must not be used after new
intent/event data becomes authoritative; forward repair is the operational recovery
path.

## Verification results

All commands ran from the IPLF-007C worktree at the application revision above.

| Gate | Result |
|---|---|
| Ruff, edited API/migration/test scope | passed, zero findings |
| `test_notification_convergence_20260804.py` | 5 passed, 0 failed/skipped |
| IP/durable/email/hearing regression set | 52 passed, 0 failed/skipped |
| Existing hearing reminder suite alone | 18 passed, 0 failed/skipped |
| Notification admin Vitest | 3 passed, 0 failed/skipped |
| Complete web Vitest | 565 passed across 119 files, 0 failed/skipped |
| Web typecheck | passed |
| OpenAPI client regeneration | passed with `openapi-typescript` 7.13.0 |
| Next.js production build | passed; 65 static-generation entries completed |
| Dated real-API Playwright at 360x800 | 1 passed, 0 failed/skipped |
| Functional QA authenticated product-surface Playwright | 1 passed, 0 failed/skipped |
| `git diff --check` | passed |

The first full exact-head CI run `30979629015` passed web, Ruff, PostgreSQL,
Security, CodeQL, and six of eight API coverage shards, then correctly rejected
the notification slice on two global guards. Shard 4 found that
`POST /api/admin/notifications/suppressions/{suppression_id}/recover` had no
HTTP-route test reference. Shard 8 found that the new
`recovered_by_membership_id` and `escalation_membership_id` foreign keys had no
leading indexes.

Revision `560d325e95b09452005461443d23cff40160bade` adds both model/migration
indexes with downgrade cleanup and an authenticated suppression-recovery route
test. The test creates provider unsubscribe evidence, recovers it with a specific
operator action, and proves the original provider message and first-event time
remain unchanged while the recovery membership is persisted. The focused
notification, route-matrix, and FK-index rerun passed 9 tests; Ruff and manifest
validation remained green.

The next exact-head CI run, `30981228836`, passed all eight API shards, combined
API coverage, PostgreSQL integration, Ruff, web unit/type/build gates, Security,
and CodeQL. Its app Playwright job passed 128 tests with one intentional skip and
found one stale assertion in the pre-existing functional QA surface inventory:
`/app/admin/notifications` still expected the former `Hearing reminders` heading.
The shipped page and its component test intentionally expose the broader
`Notification delivery and recovery` heading. Revision
`f2468b44046fac7e52b7262598480397e5d05f3f` updates that user-visible acceptance
contract without weakening route, visibility, or HTTP-status assertions.

The exact CI build sequence was reproduced locally by rebuilding Next.js with
`NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000`. The dated 360x800 UJ-11 test then
passed 1/1, and the complete functional QA authenticated-surface scenario passed
1/1. Two earlier local attempts used a stale Next.js bundle baked for a different
API origin; the rendered health banner and trace showed that no browser POST was
emitted. Those attempts are environment diagnostics, not release evidence. A new
exact-head CI run `30984441833` then passed all eight API shards, combined
coverage, PostgreSQL, web, and the full app Playwright suite at branch head
`841169785c7f0e921597c27ab56e60da2e3525cd`. Security, audit, OpenAPI drift,
secret-scan, and CodeQL gates also passed. PR #161 merged to `main` as
`4803a0a35deea9b36f618fd77bc6746a50a347d8`.

## Failed-closed production migration and remediation

The first exact-merge deployment built API Cloud Build
`44893513-84b0-4a36-bdc6-3b75a305cbe5` at immutable digest
`sha256:e0588a71f04bac6d2a87841883135507480658a6b2c27b85805d67974544837a`
and web Cloud Build `4fb0529b-70ca-4c6a-972a-50f0a1aef5c9` at digest
`sha256:60aeb511a964c195dcfcd5450e3fa170e13edabd6a1b4ef840c38c52bf102215`.
Migration execution `caseops-migrate-job-dd9mw` failed before any service traffic
changed. Cloud Run retried once and both attempts stopped with PostgreSQL
`DatatypeMismatch`: the legacy reminder backfill inserted integer `1` into the
native boolean `notification_delivery_intents.critical` column. The migration
transaction rolled back and API/web stayed on the previously verified revisions.

Revision `ade56083d04da34cdf39c46b657fb2b6762c9ec4` binds Python `True` through
SQLAlchemy instead of embedding a SQLite-tolerated integer literal. It adds a
PostgreSQL 17 regression that downgrades to `20260804_0003`, seeds a legacy
hearing reminder, upgrades to head, and verifies the boolean, destination version,
comparison status, and reminder-to-intent lineage. Focused notification tests pass
5/5 locally; Ruff and whitespace validation pass. The PostgreSQL regression is
intentionally CI-only on this workstation because no local Docker/PostgreSQL
runtime is installed. Exact-head CI, merge, migration rerun, rollout, and fresh
production Playwright are required before release status may change.

Hotfix CI run `31012341290` proved the new PostgreSQL backfill test itself passed,
then exposed test-fixture leakage: its seeded legacy reminder remained for the
module's next independent downgrade/re-upgrade regression, which correctly
reprocessed the same idempotency key and prevented later tests from returning to
head. Revision `fcc40948e4a89d5a68ce38195060056288b96659` deletes the isolated
test tenant after capturing the migrated values, which cascades its reminder,
intent, and lineage without weakening any assertion. The failed run was cancelled
after the required PostgreSQL gate failed. Exact-head hotfix CI run `31012793300`
at `4136d9de01847929606e2cb19d886f5dab61005c` then passed the PostgreSQL 17
suite, all eight API coverage shards and combined coverage, web unit/type/build,
the complete app Playwright suite, Ruff, security and dependency audits, OpenAPI
drift, secret scan, and CodeQL. PR #167 merged to canonical `main` as
`d888a3df7d1f24cf0ac6fb126365490d44573e47`.

## Exact production release

The corrected release used API Cloud Build
`8b5dfdc1-c438-4f0b-bad0-5410a3c305db` and web Cloud Build
`36ae5810-f155-4073-a2b6-d6573a196a34`. The resulting immutable image digests are:

- API: `sha256:b962b369b15bc6acd8fa6e8a6c0ce39692c3b1235c887663d2310c55a0d25333`
- web: `sha256:ce2e7fe8ebf60830a7fc9b05a1c5044334efd06ada3c6bcb377d6bcf8bcaa5fa`

Migration execution `caseops-migrate-job-th6b5` completed successfully in 32.55
seconds against the immutable API digest. The deploy then converged all six
recurring Cloud Run jobs to that same digest, verified each scheduler's intended
identity/schedule/time zone, and paused the superseded
`caseops-case-tracking-poll-midnight` scheduler.

Cloud Run API revision `caseops-api-00235-j22` and web revision
`caseops-web-00215-2v7` are Ready and serve 100 percent of untagged production
traffic. The deployment staleness sweep returned `health={"status":"ok"}`, matched
both seven-character image tags to `d888a3d`, and confirmed the ClamAV sidecar.
Independent post-command service descriptions reproduced both ready revisions,
the 100-percent traffic assignments, API concurrency 1, minimum instances 1, and
the expected images. The dated production workflow `31018210519` checks out exact
main SHA `d888a3df7d1f24cf0ac6fb126365490d44573e47`; its final Playwright result is
recorded below when complete.

The dated local browser test bootstraps an isolated company, authenticates through the real
API, executes the self-service notification from the rendered 360px admin surface,
asserts one delivered in-app intent with destination version 1 and no external
destination, reloads the admin API to prove persistence, and asserts no horizontal
overflow.

## Production UI convergence regression

Production workflow `31018210519` ran 60 RAM tests against merged hotfix
`d888a3df7d1f24cf0ac6fb126365490d44573e47`. It reported 53 passed, four
intentional pre-existing environment/provider skips, two failed, and one test not
run because the RAM gate failed. The independent Notice module therefore did not
run. Both failures were user-visible UI convergence defects, not API or database
failures:

- The global Notice PATCH returned HTTP 200 and persisted `Under Review`, but the
  row selector remained disabled. Its React Query mutation awaited invalidation of
  every notice query, so a slow background refetch incorrectly prolonged the
  mutation's busy state after the authoritative write completed.
- The notification self-test returned HTTP 200 with a delivered in-app intent, and
  a fresh admin API read contained that exact intent and the incremented delivery
  metric. The page rendered only recoverable failure intents, however, so the
  successful durable outcome was absent from the visible 360px surface.

The remediation releases Notice row controls immediately after PATCH settlement
while invalidation continues in the background. A regression test deliberately
leaves the refresh unresolved and proves the selector re-enables. The notification
admin page now renders a responsive newest-first recent-intent register for all
outcomes, while the separate recovery section retains only actionable failures.
Focused Vitest passes 21/21 across the Notice and notification admin pages; full
web coverage passes 565/565 across 119 files; web typecheck, production build,
manifest validation, and whitespace validation pass.

Exact-head CI run `31019908544` at
`cb1db65404434deda48f3cc08c742f6b504926d9` passed all eight API coverage
shards, combined API coverage, PostgreSQL 17, API Ruff, web coverage/type/build,
and the complete app Playwright suite. Security run `31019907327` and CodeQL run
`31019908757` passed. PR #168 merged to canonical `main` as
`508327f5e4969b9ec098a613b179f4fe39b92485`.

The final production deployment used API Cloud Build
`a07b12d2-9bc8-454e-92ca-97d6764015a6` at digest
`sha256:da86f051ebbb21d1cdaade4b35c3db230ad4344b9f8e90c06af7c55e683cc1d7`
and web Cloud Build `b86cfc13-9634-4a3d-918a-004fe8cb37c0` at digest
`sha256:e3fee8c7f079685f08e6080c2189d30805d2ae34c81c0540c4fb177c2e5f8b7a`.
Migration execution `caseops-migrate-job-mds7b` completed successfully in 18.7
seconds. All six recurring jobs converged to the API digest and the superseded
midnight tracker remained paused. API revision `caseops-api-00236-z86` and web
revision `caseops-web-00216-dqv` became Ready at 100 percent traffic; the health,
tag-to-HEAD staleness, and ClamAV checks passed.

The authoritative post-deploy workflow `31025057155` checked out exact main SHA
`508327f5e4969b9ec098a613b179f4fe39b92485`. Its RAM batch ran 60 tests and
reported 56 passed plus four intentional provider/environment skips; the separate
Notice module ran and passed 2/2. The prior push-triggered run started before the
web rollout converged and is retained as non-authoritative negative evidence.

Backend exception tests additionally prove disclosed session-only scheduling,
schedule-to-intent lineage, cancellation, out-of-order bounce/delivery handling,
webhook idempotency, tenant suppression, in-app fallback, minimal external content,
preview-before-recovery, changed-destination enforcement, and immutable successor
versioning.

## Genuine external gates and safe state

No arbitrary elapsed-time or “seven consecutive days” gate applies. External email,
SMS, and WhatsApp activation remains gated by real provider credentials, sender or
template approval, policy approval, and an approved QA recipient. Those gates are not
manufactured by this evidence. Until they pass, adapters remain fail-closed and the
durable intent plus in-app fallback stay visible to operators.

## Remaining acceptance work

No repository-controlled IPLF-007C release item remains. Human lawyer acceptance
remains genuinely pending and is not fabricated. External email, SMS, or WhatsApp
activation remains outside this safe production test and requires real provider,
policy, template/sender, and approved-recipient evidence.
