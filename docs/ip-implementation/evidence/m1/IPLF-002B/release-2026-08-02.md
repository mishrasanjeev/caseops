# IPLF-002B Tracking Operations and Credential Lifecycle Evidence

Date: 2 August 2026

Milestone: M1 Trust Recovery GA

Slice: IPLF-002B

Release state: deployment verified. The application revision is deployed and
independently inspected, the CSRF-correct production test is merged to canonical
`main`, and the complete dated production workflow is green. Human program/product
acceptance remains pending and is not self-approved by this evidence.

## Scope and reciprocal allocation

This is the completion slice for `TRACK-01` through `TRACK-14`, `UJ-25-NORMAL`,
`UJ-25-EXC-01` through `UJ-25-EXC-03`, `UJ-68-NORMAL`, and `UJ-68-EXC-01`
through `UJ-68-EXC-05`. It extends the canonical Case Tracking, Connector Health,
Provider Operations, support-matrix, billing, OAuth connection, audit, and scheduler
owners. It does not create a second provider dashboard, cost catalogue, support
matrix, connector registry, or scheduler.

The arbitrary elapsed-time blocker requiring seven consecutive natural scheduler
days is not used. Release proof is exact-revision configuration and IAM inspection,
bounded canary execution, deterministic normal/exception tests, and dated deployed
acceptance. Natural-run monitoring remains operational evidence, not a release delay.

## Delivered data contract

Migration `20260802_0001` is additive:

- `tracked_cases` receives last attempted, last successful, next scheduled,
  freshness, response class, last-operation, and quarantine state;
- `tracked_case_provider_operations` provides tenant-scoped record-level work with
  a unique correlation ID, provider, operation type, status, response class,
  redacted error, attempts, retry time, cost/currency, quarantine, requester, poll
  linkage, and timestamps;
- `tracked_case_provider_snapshots` is append-only application evidence containing
  redacted/safe provider-normalized raw JSON, stable raw and normalized hashes, the
  normalized record, changed-field diff, source boundary, and operation linkage;
- replay creates a new child operation and retains the original failure. A canary
  therefore cannot overwrite incident evidence;
- no existing table, route, or field is removed. Rollback before writer activation
  is the prior exact image; after writes, roll forward is preferred so evidence is
  not discarded. The downgrade is available only when the new tables/columns are
  proven unused.

## Provider execution and health

Scheduled work still uses the dedicated case-tracking poll job, opt-in bookmark
eligibility, per-tenant bounded batches, and the 4 PM–6 PM Asia/Kolkata window.
Every attempted case now creates a durable operation linked to its poll run.
Scheduler invocation, poll run, per-record operation, snapshot/update, notification,
and audit evidence can be followed through the operation/correlation identifiers.

Response outcomes are distinct: `success`, `no_change`, `timeout`,
`authentication`, `rate_limit`, `parse_error`, and a safely serialized unknown
provider-error class. A failed row receives a bounded retry time. A quarantined row
is excluded before the bulk provider request is constructed, so poison data cannot
consume a provider call and does not stop other eligible rows.

Each failed attempt records the next eligible retry time. The scheduler honors that
time, creates a preserved child operation with an incremented attempt number, and
automatically quarantines the case at the configured bound. An explicit signed,
step-up-protected admin replay may then start one new bounded recovery chain; natural
polling remains stopped until that action.

`ConnectorHealthProvider.CASE_TRACKING` is now synthesized by the canonical health
service. Enabled tracking with eligible records cannot report healthy without a
recent successful record operation. Latest success/failure/retry/error and
quarantine alerts are tenant-scoped and redacted. Provider operations expose the
same last-good, freshness, response, cost, and correlation facts.

## User workflow and stale-data safety

The Case Tracking page shows provider health, last attempt, last good result, next
scheduled time, freshness, response class, and cost for each bookmark. The control
group is shrinkable, full-width on mobile, and wrapping. Manual refresh remains
rate-limited and audited by the existing SaaS billing service, records the operation
cost, and is disabled when the provider is unavailable or the case is quarantined.
Stale evidence remains visible with manual-docketing guidance; it is not silently
presented as current.

Source documents remain behind the existing CaseOps bearer proxy. Tenant JSON does
not expose provider bearer URLs or credentials. The existing support matrix remains
the only court/jurisdiction availability and pricing owner.

## Replay, quarantine, and incident closure

The existing HMAC replay preview covers record operations with the same tenant,
scope, staleness, tamper, expiry, and 1–25 row bounds delivered in IPLF-002A.
Confirmation uses the existing recent-step-up policy, creates a new pending child,
and does not call the provider in the HTTP request. The next bounded poll executes
the child idempotently.

Ignore on a record-level tracking failure is deliberately implemented as quarantine.
The tracked case and operation are both marked, future bulk calls exclude the case,
and other records continue. Generic “mark resolved” is denied for these incidents.
`resolve-incident` requires:

1. a completed `succeeded` or `no_change` replay child;
2. root-cause text;
3. a prevention/monitoring change;
4. canary evidence; and
5. recent security step-up where the existing MFA or tenant policy requires it.

The closure audit stores only redacted operational text and hashed operation
references.

## Connector credential/disconnect lifecycle

Calendar, Gmail, and Google Drive disconnect routes call
`require_recent_step_up(... purpose="connector_disconnect")` before revocation.
Existing connection services remain the token owner and clear/revoke their stored
token references. The route then refreshes connector health in the same request so
polling/sends cannot continue behind a stale green status. Existing records and audit
evidence are preserved for degraded/manual fallback. A failed security check leaves
the existing connection untouched.

Credential creation/rotation continues to use the existing provider-specific OAuth
callback and platform credential-rotation evidence contracts; no raw secret is
returned by these changes.

## Security and tenant isolation

- all provider-operation reads/actions are `workspace:admin` and company scoped;
- replay preview binds ordered IDs, company, status, update version, cost, and expiry;
- record correlation and source references are redacted in tenant responses;
- provider errors are redacted at persistence and serialization boundaries;
- uncontrolled provider-error text is bounded before redaction; secret-assignment
  and email detection use linear token scanners rather than backtracking regexes;
- snapshot metadata removes secret, token, signature, raw-payload, authorization,
  and nested URL-bearing metadata keys before persistence; the top-level source URL
  is retained only in restricted evidence and is never emitted as a tenant bearer URL;
- cross-tenant operation loads return not found;
- source proxy allow-list and bearer injection remain server-side;
- no legal filing, message, provider purchase, destructive delete, or human approval
  was performed.

## Deterministic verification record

At implementation checkpoint:

| Check | Result |
| --- | --- |
| Ruff on changed API, migration, and test files | pass |
| Python compile/import of new ORM and services | pass |
| Existing Case Tracking + Provider Operations API suite | 23 passed before new regressions |
| Failure → quarantine → zero poison provider call → preview → child replay → successful canary → structured closure | pass |
| Calendar, Gmail, Drive connection/revoke regressions | 9 passed |
| Final focused API group: tracking, provider operations, and three disconnect routes | 36 passed in 112.24 seconds |
| Durable notification/redaction regression suite | 16 passed, including adversarial long secret/email tokens |
| Disconnect purpose assertions for all three routes | pass (`connector_disconnect`) |
| Case Tracking + Provider Operations web tests | 9 passed |
| Full web test suite | 118 files / 554 passed; initial concurrent build run caused two unrelated Notices timeouts, isolated rerun 4/4 and sequential full rerun passed |
| Web TypeScript and production build | pass; 65 routes |
| OpenAPI generated client | regenerated twice identically; SHA-256 `2C465A886E97162FFBD77F5DB5A737C8EDC8D986B093DB38139ECF264C8627F7` |
| Dated local production-mode Playwright at 360 px | 2 passed in 17.5 seconds, including fail-closed health and stale/manual-fallback visibility |
| Case Tracking browser compatibility group | 9 passed after legacy mocks were upgraded to the required health/freshness contract |
| Full local application Playwright suite | 125 passed; 1 separately provider-gated Pine Labs test skipped |
| Exact-head CI | run `30772440948`: every required check passed, including four API coverage shards, CodeQL, secret scan, dependency audits, pgvector, OpenAPI drift, web build, and Playwright application suite |

## Production deployment evidence

Validated source was rebase-merged by PR
[`#152`](https://github.com/mishrasanjeev/caseops/pull/152). Local `main` and
`origin/main` both resolved to
`7c7ebbb43d40039fc62d368a267a05056ed4f047` before deployment. The canonical
`scripts/deploy-prod.sh` release was invoked with that full SHA, rather than from a
stale build directory.

| Production item | Observed evidence |
| --- | --- |
| Validated application commit | `7c7ebbb43d40039fc62d368a267a05056ed4f047` |
| API Cloud Build | `3fadf27b-7aa8-492d-9d15-9f6201f13009` |
| API immutable image | `asia-south1-docker.pkg.dev/perfect-period-305406/caseops-images/caseops-api@sha256:31252fea8fa45700368ccc662e1de63182c0fa3f4cd77b7e6fb6a3f7786e5d0f` |
| API serving revision | `caseops-api-00227-kj8`, Ready and 100% traffic |
| Web Cloud Build | `7ef5b838-79a0-4724-8a2d-0ef92d9ac4bd` |
| Web immutable image | `asia-south1-docker.pkg.dev/perfect-period-305406/caseops-images/caseops-web@sha256:d4576f7f5a00a2572fe627c61629c743b0f3c0a8eea44f25060bfa89c7a454db` |
| Web serving revision | `caseops-web-00207-59f`, Ready and 100% traffic |
| Migration | `caseops-migrate-job-jffg8`, 1/1 task succeeded in 15.89 seconds using the exact API digest; Alembic head includes `20260802_0001` |
| Public health | three consecutive `https://api.caseops.ai/api/health` responses returned HTTP 200 and `{"status":"ok"}` after readiness; `https://caseops.ai/sign-in` returned HTTP 200 |
| Runtime sidecar | API revision contains `clamav` image `sha256:57b18e84...` and Cloud Run reports the multi-container revision Ready |

The deployment script's first public API health request timed out after 30 seconds,
so the script correctly refused immediate certification even though both deployments
had completed. Direct inspection then confirmed the exact revisions, immutable
digests, Ready conditions, and repeated public health. A service-wide log read also
showed one database password-authentication failure from the rollout interval, but a
revision-isolated Cloud Logging query found no such entry for
`caseops-api-00227-kj8`; the service-wide command had mixed revision logs. The exact
revision's successful migration, repeated health, live tenant reads, and dated
signed-in journeys are the bounded release evidence. A future failure on the serving
revision must still be treated as a credential incident, not normalized as healthy.

## Scheduler inventory evidence

`scheduler_inventory.py reconcile` returned `result: pass`. Every active recurring
job below points to the exact API digest
`sha256:31252fea8fa45700368ccc662e1de63182c0fa3f4cd77b7e6fb6a3f7786e5d0f`
and invokes as
`caseops-scheduler-invoker@perfect-period-305406.iam.gserviceaccount.com`:

| Scheduler | Run job | Schedule | Time zone | State |
| --- | --- | --- | --- | --- |
| `caseops-legal-update-sync-midnight` | `caseops-legal-update-sync` | `0 0 * * *` | `Asia/Kolkata` | enabled |
| `caseops-case-tracking-poll-1630-ist` | `caseops-case-tracking-poll` | `30 16 * * *` | `Asia/Kolkata` | enabled |
| `caseops-activity-report-0800-ist` | `caseops-activity-report` | `0 8 * * *` | `Asia/Kolkata` | enabled |
| `caseops-reminders-cadence` | `caseops-reminders-job` | `*/5 * * * *` | `Asia/Kolkata` | enabled |
| `caseops-extract-authority-metadata-daily` | `caseops-extract-authority-metadata` | `30 0 * * *` | `UTC` | enabled |
| `caseops-db-index-health-weekly` | `caseops-db-index-health` | `30 2 * * 0` | `UTC` | enabled |

The superseded `caseops-case-tracking-poll-midnight` scheduler was paused. No seven
natural-day delay is imposed; exact digest/IAM/schedule inspection plus deterministic
normal, failure, retry, quarantine, replay, and deployed journey evidence is the
release gate.

## Dated production acceptance and correction

The first post-deploy workflow, run `30774728268`, executed 57 RAM tests. Its new
fail-closed health/360 px journey passed, along with 52 other tests; three legacy
data-dependent tests skipped. The tracking-record journey failed because the test
issued its synthetic POST without the required `X-CSRF-Token`. Production correctly
returned the structured 403 `Missing CSRF token`; this was a test-authentication
defect, not an application authorization bypass.

The follow-up changes in PR
[`#153`](https://github.com/mishrasanjeev/caseops/pull/153) read the signed-in CSRF
cookie, send it on both mutations, include safe structured response detail on setup
failure, and archive the synthetic bookmark in a `finally` block. Against the exact
serving revisions, the corrected dated file passed 2/2 locally in 5.1 seconds. PR
#153 passed every required check in CI run `30775441695`, including four API coverage
shards, the full application Playwright suite, web build/typecheck/tests, OpenAPI
drift, pgvector, CodeQL, secret scanning, and dependency audits. It was rebase-merged,
and local `main` and `origin/main` both resolved to
`336136f97f6107251f9a2fc5f05d9ea0810b9470`.

The automatic post-merge production workflow `30777323024` proved the corrected
IPLF-002B tracking journey but encountered an unrelated transient API interruption in
a July 15 notice-filter journey. Its captured browser state showed “Workspace API is
unreachable” while the request remained loading. An immediate no-mock rerun of the
entire affected production file passed 3/3 in 45.3 seconds. No application or test
assertion was weakened.

The same-SHA canonical rerun
[`30778000276`](https://github.com/mishrasanjeev/caseops/actions/runs/30778000276)
then completed successfully:

- RAM production batch: 57 tests executed, 55 passed, 2 explicit data/provider skips,
  zero failures, in 11.2 minutes;
- notice production module: 2/2 passed in 20.4 seconds; and
- the workflow completed in 12 minutes 14 seconds against `336136f`, while the
  application continued to serve the immutable `7c7ebbb` API/web images recorded
  above because PR #153 changed only the production test.

This supplies deterministic deployed proof without imposing an arbitrary seven-day
observation delay. IPLF-002B `release_status` may therefore be recorded as
`deployment_verified`; acceptance remains pending for an authorized human approver.

## Rollback and data preservation

- prior API target: `caseops-api-00226-pc5` / immutable digest
  `sha256:6f71ad5867f507c3459cca9e54beeb3963fa032bae1d0b544fdd41c52bc893b6`;
- prior web target: `caseops-web-00206-c4x` / immutable digest
  `sha256:adcadf6a5760c16bcd64c97d9f72e44895d7b8fb91e387ed7676d69f3de3c03e`;
- application rollback can route traffic to those revisions, but schema downgrade is
  not the default after provider-operation/snapshot writes. Roll forward preserves
  append-only incident evidence and retry lineage.

This document does not claim completion of all 436 requirements or the wider UJ-25
and UJ-68 allocations. IPLF-056B and other provider credential/rotation slices remain
separate work, and the M0 human program lock is not self-approved.
