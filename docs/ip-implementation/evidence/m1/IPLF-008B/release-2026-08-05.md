# IPLF-008B exact-release tracked-case smoke evidence

**Evidence date:** 6 August 2026
**Application revision:** `f07cbfa26903b07c3f5878703a43534be2a1c28b`
**Environment:** local API/web, exact-head CI with PostgreSQL 17 and app
Playwright, plus Google Cloud production migration/jobs/services and exact-serving
production Playwright
**Program state:** `PROGRAM INCOMPLETE`
**Release state:** `deployment_verified`; implementation, exact-head CI,
canonical-main merge, immutable deployment, exact API/web release identity, fresh
tracked-case provider/source canary, and dated production Playwright passed. Human
acceptance remains pending and is not inferred from automation.

## 29 August 2026 release-gate correction

The original once-per-release provider refresh coupled CaseOps deployment health
to the eCourts provider's account balance. A production `402 Payment Required`
therefore failed an otherwise healthy exact release and also made repeated
verification impossible for the same SHA. The corrected contract first reuses a
successful append-only provider snapshot and complete source text only after
recomputing all three stored SHA-256 values. It records a zero-cost
`verified_cached` canary linked to the original provider operation and source
update. A live provider call is made only when no verified evidence exists.

Cached verification never updates provider freshness, clears an error, or marks
provider health healthy. The response reports the evidence mode, whether an
external call occurred, the evidence operation and completion time, its current
age, and the source hash. Provider availability and billing remain visible in
the canonical provider-health workflow, while application release verification
can still prove exact identity, durable evidence integrity, protected source
access, and responsive UI. Corrupt or incomplete cached evidence fails closed.

## Scope and canonical ownership

IPLF-008B completes the repository-controlled production acceptance path for
TRACK-01 through TRACK-14. It extends the existing `TrackedCase`, bookmark,
provider-operation/cost ledger, support matrix, source proxy, production
Playwright workflow, and release sign-off template. It does not add a parallel
tracking table, scheduler, provider ledger, notification dispatcher, source
store, or deployment system.

The release canary is an authenticated `workspace:admin` operation on one
explicitly tagged QA bookmark. The endpoint requires the full exact SHA exposed
by the serving API, serializes execution under the tracked-case row lock, and
uses the unique company/correlation constraint `release:<40-character SHA>`.
Consequently one company/release pair records at most one canary. It uses a live
refresh only when no verified provider snapshot/source evidence exists; retries
return the already recorded operation and evidence.

## Implemented production contract

- `POST /api/case-tracking/bookmarks/{bookmark_id}/release-smoke` rejects an
  abbreviated, stale, or non-serving SHA and rejects ordinary tenant bookmarks.
- The canary retains provider health, support-matrix, quarantine, poison-record,
  disposed-Matter, source-boundary, audit, snapshot, normalized-diff, and cost
  controls. It does not consume the end-user daily refresh quota.
- Live provider success/no-change or explicitly identified hash-verified stored
  evidence, current freshness/health, correlation, cost/currency, audit event,
  immutable snapshot, and a protected provider source update are returned as
  one fail-closed proof.
- The dated production spec compares API and web release identities, signs into
  the dedicated QA tenant, reuses an approved tagged bookmark or bootstraps one
  from `CASEOPS_QA_TRACKED_CASE_CNR`, executes the canary, downloads non-empty
  bearer-authenticated source content through CaseOps, and verifies the real
  case-tracking surface at 360 px.
- The QA fixture is notification-disabled and synthetic/control-plane scoped.
  The intended CNR is a public example from the provider's API documentation;
  no real client data or client communication is used.
- The source action must be visible, resolve to the exact CaseOps API proxy path,
  open in a protected new tab, and fit the narrow viewport together with every
  grouped bookmark action. DOM presence alone is not accepted.
- The expanded release template records requirements/journeys, ownership and
  duplicate analysis, capabilities, schema/migration state, threats, exact
  build identity, test inventory, documentation, external approvals, and
  production evidence. It contains no arbitrary elapsed-day release gate.

## Local verification

The focused gates below were rerun after integrating deployment-verified canonical
main `cfedd74b852ad90f6c9c295ec57e1bc599f43a84`. The tracked-case smoke
implementation originated at `b92d5d9b3d37113232e3ed9c77fbb4756e2c26b4`;
the application revision above is the authoritative canonical-main production
release.

| Gate | Result |
|---|---|
| Full `test_case_tracking.py` | 15 passed, 0 failed/skipped |
| Focused canary/untagged negative tests | 3 passed, 0 failed/skipped |
| API Ruff | passed, zero findings |
| Case-tracking web component suite | 5 passed, 0 failed/skipped |
| Web typecheck | passed |
| OpenAPI client regeneration | passed using `openapi-typescript` 7.13.0 |
| Dated production Playwright discovery | 1 runnable test, 0 hidden skips |
| Canonical manifest validation | passed: 436 requirements, 50 families, 68 journeys, 317 atomic paths |
| `git diff --check` | passed |

Negative assertions cover untagged bookmarks, stale release SHA, exact-operation
reuse, one provider call, hidden raw provider URL, cost disclosure, correlation,
freshness, and audit persistence. Existing case-tracking regression coverage
continues to cover scheduled retry, quarantine, provider outcome classes,
manual limits, disposed-Matter suppression, source download boundaries, and
tenant scope.

Exact-head CI run `31059296591` at PR head
`b418e3b9c52e9952b230f6a3107aeec74485372e` passed PostgreSQL/pgvector, Ruff,
all eight API coverage shards and combined coverage, complete web
test/typecheck/build, and the full application Playwright suite. Security run
`31059296623` and CodeQL run `31059296587` passed at the same head. PR #164
merged to canonical `main` as
`f07cbfa26903b07c3f5878703a43534be2a1c28b`; local main and origin/main were
verified equal before deployment.

## Exact production release

The canonical deploy created API Cloud Build
`1d1115e8-4f32-4106-b406-271fbdd35651` and web Cloud Build
`aa261d9c-5b46-4a29-b66c-563ad1b3cd73`. Their immutable digests are:

- API: `sha256:497a2066552fc3c3c43e69dcfe31922349d86407f5bd7364e8ef99c129cc5ddd`
- web: `sha256:fbc05ad1caec1807b5cc21a783910e784dc787c10bb5720c843e618fea440acb`

Migration execution `caseops-migrate-job-l8d5t` completed successfully in 17.3
seconds. All six recurring jobs converged to the API digest with their intended
identity, schedule, time zone, and enabled state; the superseded midnight tracker
remained paused. API revision `caseops-api-00238-pxr` and web revision
`caseops-web-00218-lbw` became Ready at 100 percent traffic. Their public identity
endpoints both reported the exact canonical-main SHA, health returned
`{"status":"ok"}`, image tags matched `f07cbfa`, and the ClamAV check passed.

Production workflow `31060850360` waited through rollout convergence, verified
one exact API/web release, and checked out that serving commit before Playwright.
The RAM batch ran 61 tests: 57 passed and four intentional provider/environment
tests skipped. The exact-release canary itself passed in 10.8 seconds against QA
CNR `DLHC010001232024`, including fresh provider evidence, immutable cost and
correlation records, protected non-empty source download, and all grouped actions
visible at 360px. The separate Notice module passed 2/2. The full identity-bound
workflow passed in 24m40s.

## Data, migration, and rollback

No schema migration or backfill is required. The change uses the existing unique
provider-operation correlation constraint, metadata JSON, immutable operation
snapshot, update/source records, and audit ledger. Creating the approved QA
fixture is a normal tenant-scoped bookmark mutation and is safe to retain; it is
notification-disabled.

Rollback removes the canary route/spec and restores the prior workflow/template.
Existing bookmark, update, operation, cost, and audit records remain valid and
need no destructive repair. Ordinary manual/scheduled refresh behavior retains
its previous defaults.

## Remaining acceptance work

No repository-controlled IPLF-008B release item remains. Genuine human acceptance
and future provider credential/source-term renewals remain operational gates and
are not manufactured from automated proof. A provider outage, unsupported
jurisdiction, invalid source, absent QA CNR, or unhealthy/quarantined record
continues to fail closed; none is converted into a skip or time-based waiver.
