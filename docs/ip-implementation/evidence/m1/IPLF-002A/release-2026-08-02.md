# IPLF-002A Integration Health and Bounded Replay Evidence

Date: 2 August 2026

Milestone: M1 Trust Recovery GA

Slice: IPLF-002A
State: repository-controlled IPLF-002A implementation, verification, exact-main
deployment, and dated production proof completed. Human acceptance remains
pending and is not implied by this technical release.

## Scope and ownership

This slice implements the dependency foundation and backend contract for
IPLF-002. It is an administrative decomposition, so the canonical manifest
intentionally assigns the TRACK and UJ paths to completion slice IPLF-002B.
This evidence does not claim those requirements or journeys complete.

Classification is `EXTEND`:

- `ConnectorHealthRecord`, `services/connector_health.py`, and
  `/api/admin/integrations/health` remain the sole connector-health owner.
- `services/provider_operations.py`, its existing admin routes/page, and the
  existing durable job rows remain the sole replay/dead-letter owner.
- `ProviderCostProfile` remains the cost source. A missing matching cost is
  disclosed as `unpriced_provider_call`; zero recorded cost is not described as
  a free provider call.
- `CaseTrackingSupportMatrix` remains the court/provider support owner.
- Existing calendar, notification, mailbox, webhook, candidate, tracking, and
  connector rows remain canonical. No `ip_*` provider dashboard, queue, health
  table, replay table, or cost catalogue was created.

## Delivered contract

Connector health now serializes last attempt, last successful/last good, next
scheduled retry, a 24-hour freshness classification, response class, redacted
current error, and operator-attention state. It computes health fail-closed: an
enabled/configured connector cannot be healthy without a recent successful
check. Disabled, blocked, stale, and never-successful states remain distinct.

Provider-operation output now accepts every kind already emitted by its
aggregator (`drive_file_candidate`, `calendar_event_candidate`,
`inbound_email_event`, and `connector_health` were previously rejected by the
schema). Rows expose redacted correlation reference, response class, attempt,
last-good/next-scheduled timestamps, affected-record count where known,
retryability, quarantine state, and cost basis.

Replay is preview-then-confirm:

- preview accepts 1-25 unique tenant-visible operation IDs;
- the signed token binds tenant, ordered scope, status, update version, expiry,
  currency, and estimated cost;
- malformed, cross-tenant, expired, non-replayable, and changed previews fail
  closed;
- confirmation requires an operator reason and the existing MFA step-up when
  enrolled or policy-enforced;
- batch confirmation and audit commit atomically;
- the same confirmed token is idempotent and cannot create another durable row;
- provider calls never occur in the request; existing workers retain ownership;
- dead-letter rows stay quarantined until explicit preview and confirmation.

## Security and privacy

Both read and mutation routes require `workspace:admin`. Operation loading is
company-scoped before any preview or mutation. Tokens are HMAC-SHA256 signed
with the configured authentication secret and expire after five minutes. Raw
operator reasons and unredacted identifiers are not copied into action audit
metadata. Connector errors are redacted during persistence and again during
serialization, covering direct or legacy unsafe writes. No credentials, OAuth
tokens, raw payloads, message bodies, or provider documents are exposed.

## Data, migration, compatibility, and rollback

There is no schema change, migration, backfill, or writer switch. The API change
is additive except that replay confirmation now requires a preview token.
Generated OpenAPI types and the in-repository Zod boundary were updated in the
same release. Ignore and mark-resolved contracts are unchanged.

Rollback is an exact-image rollback. Previously confirmed rows remain safely
`pending` or `queued` under existing idempotency keys and existing workers. No
data contraction or reverse migration is required. The old UI will not issue a
valid replay against the new API, so API and web images must be deployed and
rolled back together.

## Verification completed before immutable release

Focused API and connector regression:

```text
uv --directory apps/api run pytest tests/test_provider_operations.py \
  tests/test_connector_automation_readiness.py \
  tests/test_google_workspace_configuration.py -q
25 passed, 0 failed, 0 skipped
```

The assertions cover admin permission, tenant isolation, output redaction,
schema completeness, names-only readiness, preview bounds and uniqueness,
cross-tenant token rejection, stale-scope rejection, cost disclosure,
tamper/expiry rejection, idempotent repeat confirmation, atomic bounded batch,
external replay denial,
operator reason, action audit, step-up invocation, fail-closed freshness, and
connector-operation serialization.

Focused frontend regression:

```text
npm test --workspace @caseops/web -- --run \
  app/app/admin/provider-operations/page.test.tsx \
  app/app/admin/integrations/page.test.tsx
6 passed, 0 failed, 0 skipped
```

Complete frontend regression:

```text
npm run test:coverage --workspace @caseops/web
118 files passed; 552 tests passed; 0 failed; 0 skipped
```

Build and browser proof:

```text
npm run typecheck:web
passed
npm run build:web
passed; 65 static/dynamic route entries generated
npx playwright test tests/e2e/ram-2026-08-02-bugs.spec.ts \
  --config=playwright.app.config.ts --project=app-chromium --reporter=list
1 passed, 0 failed, 0 skipped
```

The Playwright test bootstraps an anonymized law-firm tenant, calls the real API
and database, proves no-success health fails closed, signs in through the real
browser, opens both admin surfaces, and asserts no horizontal overflow at
360x800. The committed production companion is
`tests/e2e/ram-2026-08-02-prod.spec.ts`.

Additional successful gates at this checkpoint:

- `npm run lint:api`
- generated OpenAPI dump and pinned `openapi-typescript@7.13.0`
- `git diff --check`

The monolithic local `npm run test:api` command was also attempted. It reached
the command's 1,804-second ceiling without pytest emitting a failure or a final
summary, so it is recorded as `timed out / no result`, not as passed. The 25
focused API/connector tests above completed normally. The exact-commit CI's four
API shards were the required authoritative broad API result; they passed before
each merge and exact-main deployment.

## Merge and deployment chronology

The implementation was committed as
`084644202e867ffd0577ecf0bf80b7cc87a3733a`. PR #148 merged it to `main` as
`3d56415b768b47b2c02153a36248177b9b6daef6`. CI then found an ambiguous dated
Playwright heading selector: both the page heading and an empty-state heading
matched `Provider operations`. The exact locator was corrected in
`7dd3cf1`; PR #149 passed all checks and merged to `main` as the runtime release
commit:

```text
3f02ce65c2ad75461d7f8d928b40acb1c929c28f
```

Exact-main CI run `30748699175`, Security run `30748699170`, and CodeQL run
`30748699177` passed. The CI run included all four Linux API coverage shards,
Postgres plus pgvector validation, API lint, and the complete frontend
typecheck/Vitest/build boundary.

The canonical `scripts/deploy-prod.sh` path built and deployed the exact
`3f02ce6` source. An initial local command ceiling ended the observing shell
after sixty seconds while two valid builds continued in Cloud Build. No build
or deployment was cancelled or rolled back. The canonical command was rerun
with the intended ceiling; duplicate submissions used the same immutable source
and image tag, and the following canonical build/deploy completed successfully:

| Boundary | Immutable production result |
| --- | --- |
| API build | `4bf17ebe-056e-45d2-ab05-beb652721449` |
| API image | tag `3f02ce6`; digest `sha256:7ed05449650564bcd0cdb4032406123e86f2f9193b200f6d2ec56f42c0f5413f` |
| API revision | `caseops-api-00225-ntc`; 100% traffic |
| Web build | `8e42e548-9d55-4409-b841-b6c99e29974e` |
| Web image | tag `3f02ce6`; digest `sha256:40f6466b98793286c33665aefb1117f9bc89f8e68319487ff2677b9b1b0eeea0` |
| Web revision | `caseops-web-00205-bnr`; 100% traffic |
| Migration execution | `caseops-migrate-job-gkms7`; succeeded at `2026-08-02T13:44:34Z` using the exact API digest |
| Schema | application and production Alembic head `20260801_0006` |
| Health | `https://api.caseops.ai/api/health` returned HTTP 200 / `status=ok` |
| Malware scanning | ClamAV sidecar present on the serving API revision |

No schema or backfill was introduced by IPLF-002A. The migration job still ran
as the release compatibility gate and confirmed the unchanged head.

## Scheduler and job convergence

Fresh post-deploy inventory validation reported all six required schedulers as
`pass`, with the dedicated invoker identity, required cadence/timezone, enabled
state, target, and exact API image digest:

1. legal updates at 00:00 Asia/Kolkata;
2. case tracking at 16:30 Asia/Kolkata;
3. activity report at 08:00 Asia/Kolkata;
4. reminders every five minutes in Asia/Kolkata;
5. authority metadata at 00:30 UTC; and
6. database index health weekly at 02:30 UTC.

The superseded midnight case-tracking schedule remains paused. There is no
fixed seven-natural-day release blocker. Exact configuration, IAM, immutable
image, bounded canary, health, and dated production browser evidence are the
release gates; ongoing natural executions remain SLO evidence.

## Dated production browser chronology

Every negative run is retained because a failed check cannot be erased by a
later pass:

- Run `30750721772` reached the new deployed UI but failed the IPLF-002A direct
  API assertion with HTTP 404. Root cause: the production test called the web
  origin (`https://caseops.ai/api/...`) instead of the API origin. Result was
  `51 passed, 1 failed, 4 skipped`; the dependent Notice module did not run.
- PR #150 introduced `PROD_API_BASE_URL`, defaulting to
  `https://api.caseops.ai`, and changed the direct integration-health request
  to that origin. An unauthenticated boundary probe then returned HTTP 401,
  proving the protected route existed instead of the prior web-origin 404.
- Run `30751344085` passed the new IPLF-002A production test but failed an older
  global Notices filter assertion while the page still displayed `Loading
  matching notices...`. Result was `50 passed, 1 failed, 4 skipped, 1 did not
  run`; the Notice module again did not run. The product response had not
  contradicted the test - the locator's arbitrary ten-second window expired while
  the exact filtered request was still pending.
- Commit `15adcf460de5b39acab3c7578094e85e27cbc0cc` strengthened that test without
  adding a sleep or weakening acceptance. It waits for the exact final GET
  query (subject, status, and tenant owner), requires HTTP 200, requires the
  response to contain the just-created notice ID, and then requires the row to
  render.
- Corrective production run `30751941526` passed on 2 August 2026. The RAM
  batch reported `53 passed, 3 provider/data-gated skipped` from 56 tests in
  12.2 minutes. The separate Notice-module suite then reported `2 passed` in
  20.1 seconds. The dated provider-health test proved the fail-closed summary at
  desktop and 360px, the exact protected API origin, and no horizontal action
  loss.

PR #150 CI run `30751939034` passed every required check and merged by rebase
to canonical `main` as
`7b1f834288d3ebfa673eea602bd759bb79e0baf3`. Local `main` and `origin/main`
were fast-forwarded to that commit. The canonical helper then redeployed that
exact source without duplicate submission:

| Exact-main boundary | Immutable production result |
| --- | --- |
| API build | `dd52b874-a6b8-4615-9b15-0ee61fc0f5bb` |
| API image | tag `7b1f834`; digest `sha256:6f71ad5867f507c3459cca9e54beeb3963fa032bae1d0b544fdd41c52bc893b6` |
| API revision | `caseops-api-00226-pc5`; 100% traffic |
| Web build | `4c9616b8-3f43-4a0f-9531-8d36c10bc95e` |
| Web image | tag `7b1f834`; digest `sha256:adcadf6a5760c16bcd64c97d9f72e44895d7b8fb91e387ed7676d69f3de3c03e` |
| Web revision | `caseops-web-00206-c4x`; 100% traffic |
| Migration execution | `caseops-migrate-job-2ts5p`; succeeded using the exact API digest |
| Health and sidecar | HTTP 200 / `status=ok`; ClamAV present |
| Scheduler inventory | all six required jobs `pass` on the exact API digest; superseded midnight tracker paused |

Exact-main production workflow `30754848841` started from `main` after the
traffic shift and passed. The RAM batch ran 56 tests and reported `52 passed, 4
skipped` in 13.3 minutes; the separate Notice module reported `2 passed` in
18.8 seconds. The four skips are pre-existing, explicit data-state probes:

- Calendar zero-event empty state when the production QA tenant is not empty;
- Invoice dialog mobile path when no eligible invoice exists;
- forced recommendation-error UI when no suitable production fixture exists;
- research garbled-OCR production-data path when no matching row exists.

Their deterministic synthetic counterparts remain in the same required batch
and passed. The new IPLF-002A provider-health test, global Notices workflow,
protected API-origin assertion, and desktop/360px checks all ran and passed.
This is the final exact-main deployed proof for the slice.

## Explicit remaining boundary

IPLF-002B remains pending. It owns TRACK-01 through TRACK-14 and UJ-25/UJ-68
paths, including per-linked-case/application freshness, full operator
incident/canary/closure workflow, source download, support matrix integration,
credential rotation exceptions, and dated production acceptance. This document
must not be used to mark IPLF-002 or M1 complete.
