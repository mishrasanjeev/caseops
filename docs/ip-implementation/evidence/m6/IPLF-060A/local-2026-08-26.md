# IPLF-060A Local Evidence - 2026-08-26

## Verdict

The IPLF-060A repository foundation is implemented and locally verified at
functional revision `c599eee92ffeb22bcf761b888c12062a8cee7235`. Release is not
verified. PR review, exact-main CI, migration-first deployment, scheduler
reconciliation, and exact-release production evidence remain required.

This evidence does not mark JUDGE-01 through JUDGE-10, UJ-20, or IPLF-060B
complete. Those allocations require the user workflow and release proof.

## Environment and fixtures

- Windows 11, CPython 3.13, repository-locked `uv` environment.
- Isolated FastAPI/SQLite fixtures for API and service behavior.
- Alembic replay against empty and populated temporary SQLite databases.
- Synthetic courts, judges, aliases, authorities, mapping collisions, reviews,
  predictive classifications, appointments, and audit actors.
- No real client, lawyer, court filing, provider mutation, notification, fee,
  payment, or legal effect.

## Commands and results

```text
uv run ruff check <changed API and test files>
PASS

uv run pytest -q tests/test_20260826_judge_mapping_foundation_migration.py \
  tests/test_ip_060a_judge_mapping.py \
  tests/test_predictive_intelligence.py \
  tests/test_predictive_outcomes.py \
  tests/test_scheduler_inventory.py
PASS: 61 tests

uv --directory apps/api run python ../../scripts/ip_data_governance_map.py generate
PASS: 292 SQL tables and 4,720 columns inventoried

uv --directory apps/api run python ../../scripts/ip_data_governance_map.py render
uv --directory apps/api run python ../../scripts/ip_data_governance_map.py validate
PASS

uv run python ../../scripts/dump_openapi.py ../../.tmp-iplf060a-openapi.json
npx --yes openapi-typescript@7.13.0 --alphabetize \
  .tmp-iplf060a-openapi.json --output apps/web/lib/api/openapi-types.ts
PASS
```

## Assertions proved

- Ambiguous aliases create review work and no canonical mapping.
- Review resolution is staff-only, versioned, audited, court-bound, and refuses
  competing curator-confirmed mappings.
- Alias changes reprocess affected evidence and same-court collisions fail.
- Reprocessing is idempotent and stale automatic mappings are removed.
- Duplicate merge reconciles aliases, appointments, authority mappings, derived
  analytics, and review references; third-judge alias collision aborts atomically.
- Legacy exact mappings remain analytics-eligible; legacy low-confidence and new
  unreviewed mappings fail closed.
- Revoked eligibility removes the judgment from predictive document loading,
  judge-filtered backfill input, and existing judge-scoped aggregates while
  preserving raw evidence.
- Empty migration upgrade/downgrade works; populated migration upgrade works;
  downgrade refuses after IPLF-060A lineage is written.
- The scheduler inventory binds one paused, bounded judge-mapping refresh job.
- OpenAPI types and the data-governance map match the current source schema.

## Duplicate-work review

The change extends existing Court, Bench, Judge, JudgeAlias, JudgeAppointment,
AuthorityDocument, JudgeDecisionIndex, judge-profile, court-sync, predictive,
capability, audit, Cloud Run job, and scheduler owners. It adds only missing
bench-alias and mapping-review records. It does not add a second court/judge
catalog, judgment corpus, profile, source-link store, analytics store, identity
policy, audit log, worker platform, or schedule owner.

## Remaining release gates

1. Final combined focused tests and all control-plane validators.
2. Push branch and open a PR against current `main`.
3. Pass PR CI, Security, CodeQL, PostgreSQL/pgvector, generated-contract, API,
   web, and Playwright gates.
4. Merge to canonical `main` and pass exact-main CI.
5. Build immutable images from the exact merged SHA, run migration first,
   reconcile the paused scheduler job to the exact API digest, deploy API/web,
   clear stale tags, and retain latest-only traffic.
6. Run unchanged exact-release production acceptance and record revisions,
   digests, migration execution, scheduler state, health, and result.

Until all six pass, `release_status` remains `blocked` and no deployed claim is
authorized.
