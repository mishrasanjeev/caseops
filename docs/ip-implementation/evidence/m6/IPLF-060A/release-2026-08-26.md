# IPLF-060A Exact-Release Production Evidence

**Date:** 2026-08-26

**State:** `deployment_verified` for IPLF-060A only. IPLF-060B, JUDGE-01
through JUDGE-10, UJ-20, pilot-court acceptance, and independent legal/provider
acceptance remain pending and are not claimed by this record.

## Release identity

| Item | Exact evidence |
| --- | --- |
| Canonical main | `527adb8d48043b7a6087a4bce7c0026aa95e5cc6` |
| Foundation implementation | PR #350, merged in `c842401a8ec42709860e5be1cec97caf5e87123e` |
| Scheduler bootstrap correction | PR #354, merged in `527adb8d48043b7a6087a4bce7c0026aa95e5cc6` |
| API release | `caseops-api-00363-qrp`, image tag `527adb8`, 100% latest traffic |
| Web release | `caseops-web-00342-2f9`, image tag `527adb8`, 100% latest traffic |
| API image digest | `sha256:2d4ec6b3fcaae80384725bdd6282b7ac87dfa01d2891cb6240df52afa8009206` |
| Web image digest | `sha256:909a17689eac1b86f65fcd516a38497067b4e4882c07b66ae43c5a240c2878d2` |
| Final migration execution | `caseops-migrate-job-rkb2q`, completed before traffic moved |
| Exact-main CI | GitHub Actions run `32935750625`, passed |
| Production verifier | GitHub Actions run `32935750622`, passed |
| Health | `GET https://api.caseops.ai/api/health` returned `{"status":"ok"}` |

The exact release was built from a clean main worktree. Deployment ran Alembic
before service traffic, reconciled all eight recurring jobs to the immutable API
digest, cleared stale revision tags, retained service-level warm capacity, and
left API and web at 100% latest-only traffic. The ClamAV sidecar passed its
immediate startup probe.

## Migration and deploy repair

The first `c842401a` deployment successfully ran migration `20260826_0002` as
execution `caseops-migrate-job-qh69b`, then stopped before traffic cutover. The
Cloud Run existence probe did not recognize the provider's exact `Cannot find
job [...]` response, so it could not apply the checked-in bootstrap contract for
the new judge refresh job. The previous verified API/web release remained live.

PR #354 added the exact missing-resource regression and corrected the inventory
to the governance-required `PAUSED` state. The final deployment reran Alembic as
`caseops-migrate-job-rkb2q` against the already-upgraded database, proving the
migration path idempotent, and then completed scheduler and service rollout.

## Scheduler proof

The production inventory returned `result: pass`. The new runtime contract is:

| Field | Verified value |
| --- | --- |
| Cloud Scheduler job | `caseops-judge-mapping-refresh-daily` |
| Cloud Run job | `caseops-judge-mapping-refresh` |
| State | `PAUSED` |
| Schedule | `15 1 * * *` |
| Time zone | `Asia/Kolkata` |
| Task timeout | 3600 seconds |
| Image | exact API digest `sha256:2d4ec6...09206` |
| Invoker | dedicated `caseops-scheduler-invoker` service account |

No activation is implied. The scheduler remains paused until IPLF-060B and its
approved source, pilot-court, legal, provider, and UAT gates pass.

## Production acceptance

The unchanged merge-triggered workflow checked out the exact serving release
and reported:

| Gate | Result |
| --- | --- |
| Complete RAM/IP batch | 86 passed, 5 documented conditional skips, 22.2 minutes |
| Exact deployed identity | Passed for API and web SHA `527adb8d...` |
| Bench mapping real-data probe | Resolved a real judge mapping and returned a populated bench-strategy payload |
| IPLF-027B A0 quiescence | 1 passed in 30.8 seconds |
| IPLF-037B governed renewal gate | Not run; all four approved fixture IDs remain unconfigured |
| Notice production module | 2 passed in 23.2 seconds |

The five RAM skips were explicit conditional fixtures, not hidden failures. The
renewal non-run remains the documented external legal-governance activation
boundary for IPLF-037B and does not block IPLF-060A.

During the Notice window, three attachment POSTs to one synthetic Matter all
returned HTTP 200 in 0.408, 0.462, and 0.389 seconds. The exact API revision had
no `deadlock detected` record and no Hugging Face/Hub runtime-network record.
An unrelated health probe returned in 204 ms after the long-running production
batch, showing the service remained responsive.

## Ownership and remaining boundary

The release extends the existing Court, Bench, Judge, JudgeAlias,
JudgeAppointment, AuthorityDocument, JudgeDecisionIndex, profile, source,
predictive, capability, audit, Cloud Run job, and scheduler owners. It adds only
the missing bench-alias and mapping-review records. It does not add a second
identity catalog, authority corpus, profile route, source-link mechanism,
analytics store, permission model, audit log, worker platform, or schedule
owner.

IPLF-060A has no remaining repository implementation, automated-test,
documentation, deployment, or production-verification work. The next repository
slice is IPLF-060B. Its curator UI, mapped-judgment browsing, source actions,
coverage-qualified analytics, pilot-court proof, JUDGE requirements, and UJ-20
normal and exception journeys remain fail-closed and pending.
