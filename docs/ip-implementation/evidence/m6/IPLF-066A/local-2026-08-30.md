# IPLF-066A local verification — 30 August 2026

## Verdict

Implementation commit `6aba51fbd27fca4609bd370d42c53a51d6def725`
passes the repository-controlled IPLF-066A foundation gates in the isolated
worktree. The slice is ready for review/integration, not deployed and not a
claim that IPLF-066B or the complete SEARCH-ACL requirement family is done.

## Identity and boundary

- Worktree: `C:\Users\mishr\OneDrive\Documents\CaseOps-worktrees\iplf066a-private-index`
- Branch: `codex/iplf066a-private-index-foundation-20260830`
- Base: `origin/main` at
  `820e34bbf71750d9513ba0efbae6ab614963e916`
- Implementation: `6aba51fbd27fca4609bd370d42c53a51d6def725`
- Alembic source head: `20260830_0002` (one head)
- Data effect: five empty additive private projection/security tables; no
  existing source scan, content backfill, embedding provider call or production
  mutation.

The implementation deliberately leaves user-facing retrieval/revocation,
workers, provider calls, aggregate integrity/lag release gates and exact-release
proof to IPLF-066B. Existing public authority, access, lifecycle, document and
Assistant owners remain canonical.

## Fixtures

All fixtures were synthetic and isolated: two companies; an owner, ordinary
member and an in-transaction viewer role downgrade; a restricted Matter with
grant/revoke; forged cross-tenant projection ID; current and shadow generation;
typed Matter filter; lexical and vector-only request; Matter disposal and
controlled `Disposed -> Intake` reopen; Workspace Assistant answer/citation;
duplicate revocation event; and retained-evidence downgrade refusal. No real
client, document, provider/model call, external message or legal effect was
used.

## Verification

| Gate | Command | Result |
| --- | --- | --- |
| Source migration head | `uv --directory apps/api run --no-sync alembic heads` | `20260830_0002 (head)`; exactly one head |
| Consolidated migration/security regression | `uv --directory apps/api run --no-sync pytest -q tests/test_20260830_private_index_migration.py tests/test_migration_order.py tests/test_database_index_health.py tests/test_private_retrieval_foundation.py tests/test_datagov09_purge_propagation.py tests/test_ethical_walls.py tests/test_matter_lifecycle.py tests/test_ip_lifecycle_service.py tests/test_ip_record_access_workflow.py tests/test_ip_document_workflow.py tests/test_workspace_assistant_foundation.py tests/test_workspace_assistant_qa.py` from `apps/api` | `95 passed`, `0 failed`, `0 skipped` in `241.92s`; 65 dependency deprecation warnings |
| Affected lint | `uv --directory apps/api run --no-sync ruff check` over the changed models, migration service integrations, purge plan, migration/head tests, foundation tests and governance generator | passed |
| Governance registry | `uv --directory apps/api run --no-sync python ../../scripts/ip_data_governance_map.py validate` | valid: 303 SQL tables, 4,920 columns, new identifier-only cache registered |
| Program controls | `uv --directory apps/api run --no-sync pytest -q tests/test_ip_program_manifest.py tests/test_ip_ownership_ledger.py tests/test_ip_m2_ownership_audit.py` | `30 passed`; canonical manifest and eight generated views validate |
| Diff integrity | `git diff --check` | passed after the trailing empty test-file line was removed in the evidence cleanup commit |

The SQLite migration test performs a full
`20260830_0001 -> 20260830_0002 -> 20260830_0001 -> 20260830_0002`
round trip, inspects all five tables, asserts the partial active-generation and
prefilter indexes, checks every foreign key has supporting coverage, and proves
downgrade refuses retained projection/revocation/saved-output evidence.

## Security and lifecycle assertions

- Candidate SQL is fenced by company, current active generation, current
  membership capability, canonical visible Matter/IP predicates, active client,
  exact typed-scope policy version, supported filters and source state before a
  maximum of 200 identifiers reaches lexical/vector ranking.
- Known-ID cross-tenant hydration and restricted-count inference expose no
  label, snippet, score or count.
- Hydration reloads membership, capability, active generation, record/document
  access, source lifecycle/current version and canonical IP document policy. A
  role downgrade or grant revoke blocks an already selected candidate.
- Candidate cache keys partition company, membership, required capability,
  generation, access/tombstone epochs, query hash, filters, source types and
  locale. Values contain identifiers only and every hit reauthorizes.
- Matter/IP access, Matter/IP lifecycle and material IP document changes apply
  one idempotent projection event in the canonical transaction. Tombstones blank
  content and remove embeddings in active and shadow generations.
- Matter disposal followed by controlled reopen leaves the old projection
  tombstoned and emits reindex intent; it does not reactivate derived content.
- Shadow readiness checks expected count and ordered content hashes. Activation
  rejects stale security epochs or unresolved events and cannot resurrect a
  revoked row.
- Saved Assistant answers retain a content-free source/version/hash access
  manifest. Revocation locks the manifest and the existing read surface returns
  `permission_changed` with no citations or copied answer text.

## Remaining gates and risk

The local workstation's pre-existing PostgreSQL service rejected the repository
default `caseops` credentials, so this branch makes no live-PostgreSQL query-plan
or runtime index-health claim. Integration must run the migration and index
health check on a fresh, known-credential PostgreSQL instance and measure the
ACL-prefilter plan at production-scale candidate/scope volume.

Before activation, IPLF-066B must add and verify bounded projection/reindex
workers, provider-minimal embedding batches, route-level current entitlement and
rollout enforcement, private review/report saved-output registration, safe
aggregate coverage/lag/orphan/hydration-denial gates, malicious-source and
portal/internal crossover tests, responsive Playwright revocation journeys,
exact-main CI/Security/CodeQL, migration-first deployment and dated production
evidence. Until then, the foundation remains default-unused and fail-closed.
