# M6 IPLF-064 production release evidence

**Release revision:** `c407ed9399ca501070cb72d3be3200286fa5b3d2`
**Verified:** 30 August 2026
**Implementation pull request:** [#380](https://github.com/mishrasanjeev/caseops/pull/380)
**Transport-hardening pull request:** [#381](https://github.com/mishrasanjeev/caseops/pull/381)
**Production verifier:** [GitHub Actions run 33267609518](https://github.com/mishrasanjeev/caseops/actions/runs/33267609518)

## Exact serving identity

| Surface | Revision | Release SHA |
|---|---|---|
| API | `caseops-api-00384-mwr` | `c407ed9399ca501070cb72d3be3200286fa5b3d2` |
| Web | `caseops-web-00363-j4f` | `c407ed9399ca501070cb72d3be3200286fa5b3d2` |

The API image is pinned at
`sha256:266be063b16a056f7de23e11b21abddfae50afc8d5346b39af47114eb311f978`.
The web image was built as
`sha256:0b13d286f9754101a0ad6febe31ee4ade765038823df609b52053a20fa71d90a`.
Both services serve 100 percent latest-only traffic with no revision tags. The
public API health endpoint returned `{"status":"ok"}`; while the hosted
single-worker browser batch was active, an independent health request returned
HTTP 200 in about 110 ms. API service metadata and the web release-identity
endpoint reported the exact SHA above. The API retained service-level minimum
capacity four, maximum 20 and concurrency one; the web retained service-level
minimum capacity one.

## Migration and recurring work

API Cloud Build `bf5485a7-be90-497e-bf05-5fd33404f80b` and web Cloud Build
`ff15cdee-2024-4da7-bec2-47e369287fed` completed successfully. Migration
execution `caseops-migrate-job-bcj5r` then completed before traffic moved.
Database-index execution `caseops-db-index-health-cwltm` passed the pre-route
health gate on the same immutable API digest.

The complete eight-job recurring-work inventory reconciled successfully to the
new digest. `caseops-extract-authority-metadata-daily` and
`caseops-judge-mapping-refresh-daily` remained `PAUSED` before deployment,
during reconciliation, and after hosted acceptance; neither was executed.

The production QA bootstrap was repinned before traffic without execution.
After all hosted release gates passed, it executed exactly once as
`caseops-ip-qa-bootstrap-fwf99`; the execution count advanced from 16 to 17,
the execution succeeded in 19.45 seconds, and the job used the released API
digest.

## Hosted acceptance

Post-merge main CI run [33265842004](https://github.com/mishrasanjeev/caseops/actions/runs/33265842004),
Security run [33265842048](https://github.com/mishrasanjeev/caseops/actions/runs/33265842048),
and CodeQL run [33265842002](https://github.com/mishrasanjeev/caseops/actions/runs/33265842002)
passed for the exact release commit. PR #380 had independently passed CI,
Security, and CodeQL on its final implementation head. PR #381 then passed the
complete matrix, including the 172-test application suite, after narrowly
hardening the test bootstrap against connection resets without retrying an
application response or reusing a possibly committed tenant identity.

Production verification run `33267609518` first proved one exact API/web
release and checked out that serving source revision. Its single-worker RAM
batch ran 100 tests: 95 passed and five intentionally skipped in 22.0 minutes.
The dated IPLF-064B preview/confirm production journey passed in 13.9 seconds.
The independent Notice-module batch passed two further tests in 18.1 seconds.
The complete verifier passed in 23 minutes 14 seconds.

The historical IPLF-027B A0 replay was not requested. IPLF-037B governed
renewal acceptance remained fail-closed and did not run because none of its
four externally approved legal fixture IDs is configured. Those existing
activation boundaries are not IPLF-064 implementation scope.

## Local release gate

Before publication, the implementation passed 3,681 API tests with 129 skips
and zero failures; 103 live PostgreSQL 17/pgvector tests; 155 Vitest files with
788 passing tests; TypeScript; and a fresh 80-route Next.js production build.
Manifest, ownership, architecture, data-governance, Product Guide, migration,
scheduler, legal-fixture, and golden-query validators also passed. The data
governance projection covered 297 tables and 4,815 columns.

The exact production images passed a clean migration-first Docker run over
fresh PostgreSQL and Valkey state, unrestricted and 512 MiB database-index
checks, API/worker/web health, and exact image identity. A focused bootstrap
transport stress run passed 20 of 20 browser tests. A second clean full run
passed 170 tests with two intentional skips in 7.3 minutes and retained
post-suite health. Every test container, network, and volume was removed after
each run.

## Honest release boundary

This evidence changes IPLF-064, IPLF-064A, IPLF-064B, and their exact
`AI-GUIDE-06` preview/confirmation requirement to `deployment_verified`. It
proves the retained 15-minute assistant action preview, explicit confirmation,
canonical Matter/IP task and Draft delegation, allowlisted Matter updates,
reauthorization, atomic idempotency, responsive UI, and exact hosted journey.
It does not close all of `UJ-23`, M6, safety evaluation, feedback, private
retrieval, analytics, legal, provider, or human UAT work owned by
IPLF-065/IPLF-066 and external reviewers.
