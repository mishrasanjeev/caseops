# IPLF-065A local foundation evidence

**Implementation revision:** `9f7c278d50dd3a2cb6a43b09f7a3d5e04647ec9e`
**Recorded:** 30 August 2026
**Environment:** Windows 11 workstation, Python 3.13, Node.js 22,
exact committed API/web production Docker images, fresh PostgreSQL 17 and
Valkey 8.1 volumes, API, worker, web, and Chromium.

## Foundation delivered

IPLF-065A extends the existing `EvaluationRun` and `EvaluationCase` owner with
an offline release gate covering all eight required AI surfaces and eleven
required safety rules. The fixture result is machine-readable and excludes
raw prompts, answers, source passages, and provider payloads. Optional result
persistence delegates to the existing evaluation writer.

Migration `20260830_0001` adds one empty `ai_feedback_items` table. The queue
is tenant scoped, has composite membership foreign keys, bounded list indexes,
an actor-scoped idempotency key, typed rating/report/category/status fields,
optimistic review timestamps, and a downgrade fence for retained evidence. It
stores canonical Product Guide or Workspace Assistant references and never
copies the underlying prompt, answer, citation, source, document, or provider
content.

Submission audit events contain only redacted identifiers and lifecycle
metadata. Product Guide feedback admits every authenticated membership role;
Workspace Assistant feedback requires current `ai:generate`; review requires
`workspace:admin`. Product Guide targets must match the current catalog and
assistant targets must be tenant scoped, assistant-authored, and creator
private.

## Automated evidence

| Gate | Result |
|---|---|
| Offline safety release gate | 8/8 cases passed; 8/8 required surfaces and 11/11 required rules covered |
| Complete API inventory | 3,821 collected tests covered in deterministic final-source segments; 3,692 passed, 129 intentional skips, 0 unresolved failures |
| Focused migration, feedback, role-guard, evaluation, and index suites | passed after updating every current-head assertion to `20260830_0001` |
| Complete web Vitest inventory | 157 files; 792 passed; coverage thresholds passed |
| TypeScript and fresh Next.js production build | passed |
| Governance and architecture validators | program manifest, ownership, ARCH-OPS, data registries/map/projection, Product Guide, migration preflight, scheduler inventory, pleading fixtures, and golden queries passed |
| Fresh dirty-source Docker acceptance | 171 passed; 2 intentional skips; 0 failed; post-suite health `ok` |
| Exact committed Docker acceptance | 171 passed; 2 intentional skips; 0 failed in 7.1 minutes |
| Exact image/runtime identity | API and web both identified `9f7c278d50dd3a2cb6a43b09f7a3d5e04647ec9e` |
| PostgreSQL schema and index health | head `20260830_0001`; no FK gaps, missing/mismatched/invalid indexes, revision mismatch, or sequential-scan warnings |
| Production-job memory ceiling | index-health gate passed at 512 MiB memory and swap |
| Cleanup | disposable Docker containers, network, and volumes removed after the passing run |

## Failures found and permanently corrected

The first broad Docker run exposed a stale compiled data-class projection after
the new table was registered. The projection was regenerated and its runtime
fingerprint gate now passes. The complete Docker suite was restarted from a
fresh database and passed.

The complete API inventory exposed three stale current-head assertions and two
new mutating routes without an explicit role/capability dependency. Current
head assertions now resolve to `20260830_0001`. The Product Guide route admits
all membership roles, the Workspace Assistant route requires `ai:generate`,
and the repository-wide role-guard test plus a surface-specific viewer test
both pass.

## Honest residual scope

Local implementation and exact-commit acceptance are complete. GitHub PR,
exact-main CI/Security/CodeQL, production migration and deployment, exact
hosted identity, dated hosted feedback/admin review, and production regression
batches remain mandatory before `deployment_verified` may be claimed.

No provider sandbox, live-model quality, human legal acceptance, UAT, or policy
approval is inferred from deterministic fixture success. The authority-metadata
and judge-mapping refresh schedulers remained PAUSED and were not executed.
