# IPLF-060B Exact-Release Production Evidence

**Date:** 2026-08-26

**State:** `deployment_verified` for IPLF-060B and parent IPLF-060, UJ-20 and
its four paths, and IPLF-003B. This release proves IPLF-060B's allocated
JUDGE-01 through JUDGE-10 behavior and TRUST-RSCH-02 source preservation on the
judge workflow. Their aggregate requirement rows remain blocked because other
allocating slices retain separate exact-release gates. Independent legal,
provider/source, and law-firm UAT acceptance remains external. No such human
acceptance is claimed by this record.

## Release identity

| Item | Exact evidence |
| --- | --- |
| Canonical main | `60328f48514ce1006c655bb274c2b1289c6a51c0` |
| Workflow implementation | PR #358, merged as `d2e2791841a4002d6edb1e399cf944270362fb72` |
| Production-data correction | PR #359, merged as `60328f48514ce1006c655bb274c2b1289c6a51c0` |
| API release | `caseops-api-00368-gz2`, image tag `60328f4`, 100% latest traffic |
| Web release | `caseops-web-00347-6pk`, image tag `60328f4`, 100% latest traffic |
| API image digest | `sha256:36e33ffafd506b837957683d20eae142960991100e6795188b78db579ac2692f` |
| Web image digest | `sha256:645248871d68158d0f23e6e3e421cdbd60e5348194ea022ebe754710a577faa7` |
| Migration execution | `caseops-migrate-job-2kbqw`, completed before traffic moved |
| QA bootstrap execution | `caseops-ip-qa-bootstrap-228pp`, completed after exact release deployment |
| Exact-main CI | GitHub Actions run `32973181583`, passed |
| Security | GitHub Actions run for the same SHA, passed |
| CodeQL | GitHub Actions run for the same SHA, passed |
| Production verifier | GitHub Actions run `32973181645`, passed |
| Health | `GET https://api.caseops.ai/api/health` returned `{"status":"ok"}` |

The release was built from a clean, exact-main worktree. The canonical deploy
ran Alembic before service traffic, reconciled all eight recurring jobs to the
immutable API digest, cleared stale revision tags, retained service-level warm
capacity, and left API and web on latest-only traffic. The ClamAV sidecar passed
its immediate startup probe.

## Failed first release gate

The first exact implementation release, `d2e2791841a4002d6edb1e399cf944270362fb72`,
deployed successfully and passed exact-main CI run `32967948330`. Its production
workflow `32967948244` failed, and an independent replay reproduced the same
honest data defect:

- Delhi's first mapped judge returned legacy authority records whose source
  references were bare PDF filenames, so the source-action boundary correctly
  returned `blocked`.
- Bombay and Madras had no canonically mapped judge with an available source
  action.

The assertion and source safety rule were not weakened. PR #359 added a bounded,
idempotent production-QA canary through the existing approved QA bootstrap job.
It creates three visibly synthetic QA judge/authority mappings, refuses any
non-QA identity or authority collision, uses official registry source keys and
approved HTTPS court URLs, and sets all three mappings
`is_analytics_eligible=false`. The records state that they are synthetic QA and
not legal authority.

## Pilot-court proof

The strict exact-release production journey authenticated as the isolated IP QA
tenant and proved one canonical mapping per required pilot court:

| Court | Canonical QA judge | Authority source | Source action | Analytics count |
| --- | --- | --- | --- | --- |
| Delhi High Court | `Justice CaseOps QA Pilot - Delhi` | `https://delhihighcourt.nic.in/` | `available`; opaque open returned HTTP 307 to the exact official URL | 0 |
| Bombay High Court | `Justice CaseOps QA Pilot - Bombay` | `https://www.bombayhighcourt.nic.in/recentorderjudgment.php` | `available`; opaque open returned HTTP 307 to the exact official URL | 0 |
| Madras High Court | `Justice CaseOps QA Pilot - Madras` | `https://hcmadras.tn.gov.in/sitting_arrangements.php` | `available`; opaque open returned HTTP 307 to the exact official URL | 0 |

Each authority response reported `coverage_state=mapped_results`, mapping count
1, and analytics-eligible count 0. The direct strict production replay passed
in 6.1 seconds. The unchanged merge-triggered production batch also passed the
IPLF-060B journey in 15.1 seconds, including the 360px judge profile, source
link, filters, admin curator tabs, Product Guide content, and law-firm page.

## Automated acceptance

| Gate | Result |
| --- | --- |
| PR #358 final CI | 29 passed checks, including 13 API shards, aggregate coverage, web build/tests, Security, CodeQL, PostgreSQL, OpenAPI cleanliness, and Playwright |
| PR #359 corrective CI | 29 passed checks with the same complete matrix |
| Exact-main CI | Run `32973181583` passed all 19 jobs |
| Complete RAM/IP production batch | 88 passed, 4 documented conditional skips, 22.1 minutes |
| IPLF-060B within unchanged production batch | Passed in 15.1 seconds |
| IPLF-027B A0 quiescence | 1 passed in 34.6 seconds |
| IPLF-037B governed renewal gate | Not run; all four approved fixture IDs remain unconfigured |
| Notice production module | 2 passed in 21.9 seconds |

The production batch also retained the real-data bench probe: Justice Yashwant
Varma resolved canonically with 43 decisions and strong mapping quality. That
probe is separate from the synthetic three-court release canary. The four RAM
skips were explicit conditional fixtures, not hidden failures. The governed
renewal non-run remains IPLF-037B's documented external legal-governance
boundary and does not block IPLF-060B.

## Scheduler and activation boundary

Production scheduler inventory returned `result: pass`. The judge refresh
contract remains:

| Field | Verified value |
| --- | --- |
| Cloud Scheduler job | `caseops-judge-mapping-refresh-daily` |
| Cloud Run job | `caseops-judge-mapping-refresh` |
| State | `PAUSED` |
| Schedule | `15 1 * * *` |
| Time zone | `Asia/Kolkata` |
| Task timeout | 3600 seconds |
| Image | exact API digest `sha256:36e33ffa...ac2692f` |
| Invoker | dedicated `caseops-scheduler-invoker` service account |

The implementation and deployed user workflow are verified, but scheduled
refresh activation still requires independent legal, provider/source, and
law-firm UAT approval. Until then, the approved safe fallback is the paused
scheduler with source-backed manual curator workflows.

## Ownership and next work

IPLF-060B extends the canonical Court, Judge, JudgeAlias, JudgeAppointment,
AuthorityDocument, JudgeDecisionIndex, source-action, capability, audit, worker,
and scheduler owners established by IPLF-060A and earlier slices. It adds no
schema, second identity catalog, second authority corpus, duplicate profile
route, analytics store, worker, or scheduler owner.

No repository implementation, automated-test, documentation, deployment, or
production-verification work remains for IPLF-060B. The next ordered repository
slice is IPLF-061A. Independent human acceptance and judge-refresh activation
remain external and fail-closed while that work proceeds.
