# IPLF-066B local completion evidence - 2026-08-31

## Scope and truthful boundary

This record covers the completed IPLF-066B private-retrieval consumer,
projection-event maintenance, revocation, saved-output, intelligent-review,
portal, bounded analytics and private-index disposition integration merged by
PR 399. Its candidate commit was
`f0c6a6eb67a9cb7ac8f45b33403e1834b90ba853`; the resulting `main` merge commit
was `1f9e21b36928b05e28c3c8bcf25ea8c3f1503515`. It also covers the local
verification of the corrective production-acceptance branch based on that
merge.

The reciprocal IPLF-071 integration is intentionally narrow. The
`data_disposition` adapter executes only a separately approved, immutable,
matching `private_index_projections` operation through the existing canonical
data-governance owner. It does not claim that the remaining tenant/client
export, object-storage purge, backup restoration, offboarding or throwaway-
tenant work in IPLF-071A/IPLF-071B is complete.

No hosted CI, merge, production deployment, production request, real client
content, provider deletion or legal/UAT acceptance is represented by this
local record.

## Initial clean isolated Docker acceptance

The final `scripts/verify-docker.ps1 -PreCommit` run calculated full source
fingerprint
`3c1324e31659e8f2cbbf7f8c18c374f236b7566a729b0b1f43020b5fced568f7`
and used its Git-shaped runtime revision
`3c1324e31659e8f2cbbf7f8c18c374f236b7566a`. The isolated Compose project was
`caseops-acceptance-3c1324e31659`.

The harness built fresh production API and web images, verified their labels
and runtime identities, migrated a fresh PostgreSQL 17/pgvector database to the
single Alembic head `20260831_0001`, and passed complete index health under the
production migration job's 512 MiB memory limit. The report contained zero
foreign-key gaps, invalid indexes, missing or mismatched declared indexes,
schema-revision mismatches, or sequential-scan warnings.

Playwright discovered 174 browser tests. The result was 172 passed, zero
failed, and two intentional skips for provider-gated Pine Labs and deployed-
only release identity. The run included the dated IPLF-066B scenario proving
that canonical revocation removes answers, citations and cache results,
persists after reload, remains usable at 360 pixels, and treats malicious
instructions in private source text as untrusted content. Post-test API/web
health passed. The browser run took 12.7 minutes. Automatic cleanup and an
independent label check left exactly zero containers, volumes and networks for
that Compose project.

The committed candidate `dbd30b0cfe1da73ff18cad91f71a9d5dea6c32de` was then
rerun through the same isolated harness. Its production images, migration head,
normal and 512 MiB index-health gates, all 81 web routes, and 174 browser tests
passed. The result was again 172 passed and the same two intentional skips,
with zero failures in 16.8 minutes. The exact Compose project
`caseops-acceptance-dbd30b0cfe1d` was independently confirmed to leave zero
containers, volumes, or networks.

## PostgreSQL-only gate correction

PR 399's first hosted PostgreSQL validation exposed a local-gate omission after
the exact browser harness had passed. The PostgreSQL savepoint regression
expected a poison event to be terminal after one attempt while invoking the
worker's default three-attempt retry policy. The production worker correctly
persisted the event as `pending` with backoff, so the assertion failed.

The regression now explicitly requests `max_attempts=1` for its terminal-state
case and verifies the durable attempt count, absent retry timestamp, PostgreSQL
`DataError`, successful sibling event, and completed revocation. The formerly
failing test passed against a fresh local pgvector/PostgreSQL 17 container, then
the complete PostgreSQL marker suite passed 108/108 in 82 seconds.

`scripts/verify-docker.ps1` now supplies the isolated Compose database to the
host PostgreSQL fixtures and runs all `test_postgres_validation.py` PostgreSQL
cases after both index-health gates and before Playwright. A future candidate
therefore cannot reach hosted CI with a PostgreSQL-only failure hidden behind
the normal SQLite environment skip.

## Complete repository verification

The complete API inventory ran once across the same 13 deterministic CI shards.
Three assertions correctly exposed hard-coded migration head
`20260830_0002`; they were repaired to derive the one live Alembic graph head.
The three focused regressions then passed, and both complete affected shards
were replayed successfully: `243 passed, 108 skipped` and `384 passed`.
Together with the eleven originally green shards, the final API result is
3,774 passed, 135 environment/provider skips and zero failures.

The web suite was rerun alone after the API load finished. The functional QA
runner passed 5/5, route generation and TypeScript passed, Vitest coverage
passed all 157 files and 800/800 tests, and the optimized Next.js production
build generated all 81 pages/routes successfully.

Repository-wide Ruff, program manifest, ownership ledger, architecture/ops
contract, data-class and governance registries, governance change gate,
projection, Product Guide, migration preflight/change, M2 ownership audit,
legal fixtures, golden queries, scheduler inventory, AI safety release gate and
`git diff --check` all passed. Migration preflight confirmed 182 migrations and
one head, `20260831_0001`; scheduler validation confirmed nine recurring jobs.

A final performance review found that the global maintenance selector also
needed a state-leading generation index in addition to its tenant-leading
indexes. Migration `20260831_0001`, the ORM and the governance fingerprints now
include `ix_private_index_generation_maintenance (state, company_id)`. The
migration round trip, full index-health module, bounded selector, scheduler and
monitoring-alert group then passed 36/36 tests, followed by another complete
static/governance gate replay.

## Implemented operational controls

- Projection events are tenant-bounded, indexed, retryable at most three times,
  and preserve content-minimized terminal evidence.
- The five-minute Cloud Run maintenance job caps tenant/event/rebuild work,
  blocks on lag or integrity failure, logs a correlation ID, and has a
  reconciled log-based production alert and operator runbook.
- Approved private-index disposition rechecks retention and legal holds,
  emits the canonical tenant tombstone, verifies neutralized projections, and
  records a durable local receipt or explicit provider contractual-delay
  exception. A later rebuild cannot resurrect disposed content.
- Streaming, autocomplete, count, Assistant answers/citations/exports,
  intelligent-review reports and portal document access all reauthorize at
  delivery and fail closed after access, source, lifecycle or epoch change.

## Remaining release gate

Implementation and local verification are complete. Release remains blocked
until the final commit is rerun through exact-commit Docker acceptance, pushed,
passes hosted CI/Security/CodeQL, merges to `main`, passes post-main checks,
deploys migration-first with latest-only traffic, and passes immutable-image
production health, index, scheduler, alert and dated browser verification.
The protected authority-metadata and judge-mapping schedulers must remain
paused throughout release. The production QA bootstrap may run exactly once
only after all local, PR, post-main, migration, exact-traffic, health and
sidecar gates are green; the production browser workflow then consumes that
SHA-scoped fixture.

## Production-verifier gap found before deployment

After PR 399 merged, a release-control review found that the local dated
IPLF-066B browser spec was not selected by `playwright.prod-ram.config.ts`.
Production was held before any migration, image build, QA bootstrap or traffic
change. The corrective branch adds a release-SHA-scoped synthetic private
fixture to the existing IP QA bootstrap, exact-generation one-run enforcement
to the canonical deploy script, and a public-API/browser production spec that
disposes the Matter through the canonical lifecycle endpoint and proves search,
saved-answer and citation revocation after reload. The fixture is idempotent
before use and refuses terminal resurrection.

Focused local verification passed 56/56 bootstrap and deploy-hardening tests,
Ruff, Bash parsing, TypeScript route/type checking, Playwright production-suite
discovery, Prettier and `git diff --check`.

The fresh corrective Docker run used source fingerprint
`c94a5cb9f0eb1991d13073ae91b8061350489453b13ed78ff17acc0369d885b9`,
Git-shaped runtime revision
`c94a5cb9f0eb1991d13073ae91b8061350489453`, and isolated project
`caseops-acceptance-c94a5cb9f0eb`. Both normal and 512 MiB index-health checks
reported zero gaps, the complete PostgreSQL/pgvector suite passed 108/108, and
the original dated IPLF-066B browser regression passed against the exact local
images.

The exact API image then ran the QA bootstrap with that 40-character release
revision. It created exactly one active Matter named
`IPLF-066B-C94A5CB9F0EB`, one attachment and one active private generation.
With the loopback browser API proxy running, the newly selected production
spec passed 1/1 against the local web and API images. It proved exact API and
web release identity, authenticated public-API discovery, private search,
Assistant answer and citation delivery, canonical Matter disposal, saved-answer
redaction after reload, citation removal, empty post-disposal retrieval, policy
restoration and a no-overflow 360-pixel viewport. The final exact Compose
cleanup left zero project containers, volumes and networks.

## Corrective complete-workstation verification

The complete API inventory ran through the repository's 13 deterministic
whole-file shards with `CASEOPS_OCR_PROVIDER=none`. Twelve shards passed on the
first run. Six deploy-harness subprocesses in the remaining shard exceeded
their 30-second deadlock guard only while ten migration-heavy local shards were
competing for the workstation. The complete deploy-hardening file immediately
passed 48/48 in 72 seconds without saturation; the full affected shard then
passed 393/393. The final complete API result is 3,779 passed, 135 intentional
environment/provider skips and zero unresolved failures.

The complete web gate passed 5/5 functional-runner security tests, route type
generation and TypeScript, all 157 Vitest files and 800/800 tests with coverage
thresholds, and a fresh optimized production build of all 81 routes.

The first full corrective Docker attempt found that the fingerprint-derived
port block overlapped a Windows excluded range at port 49669. The harness now
bind-tests all five ports simultaneously and walks a deterministic bounded set
of 6,000 candidate blocks before Compose starts. A later full browser attempt
also exposed a Windows fail-fast exit in a long-lived Playwright worker before
IPLF-059B executed. The same IPLF-059B test passed in 13.4 seconds on a fresh
exact-image stack. The harness now runs two sequential desktop shards and the
mobile project without retries, bounding worker lifetime while still running
every test exactly once and treating every assertion failure as fatal.

The final executable pre-commit candidate used source fingerprint
`888b7e39a9842d9b46821184a289f727cf597c86bc47e13f7614c73b3effd03a`,
Git-shaped runtime revision
`888b7e39a9842d9b46821184a289f727cf597c86`, and project
`caseops-acceptance-888b7e39a984`. Fresh production API and web images,
migration to the single `20260831_0001` head, normal and 512 MiB index health,
and all 108 PostgreSQL/pgvector tests passed. Desktop shard 1 passed 84 tests
with one provider-gated skip, desktop shard 2 passed 84 tests with one
deployed-only skip, and the mobile project passed 4/4. The complete browser
inventory therefore finished with 172 passed, two intentional skips and zero
failures across all 174 discovered tests.

The exact images were then recreated from fresh volumes for the release-owned
journey. The original IPLF-066B test and all 108 PostgreSQL tests passed, the
exact API image bootstrapped one `IPLF-066B-888B7E39A984` fixture, and the
production-config IPLF-066B spec passed 1/1 against exact API/web identities.
Final cleanup left zero containers, volumes and networks for the exact Compose
project.

This is corrective local evidence, not a deployment claim. Exact-commit Docker
acceptance and the replacement hosted/release gates remain required.
