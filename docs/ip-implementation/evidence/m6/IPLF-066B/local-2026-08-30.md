# IPLF-066B local verification — 30 August 2026

## Verdict

Implementation commit `55d2af96e38b1b8eaa1150646c2de69f66df3424`
passes the focused repository-controlled workflow, adversarial browser, and
real-PostgreSQL scale checks recorded below. The slice remains `in_progress`,
`not_run` for complete acceptance, and release-blocked because reciprocal
review/report, analytics, purge/provider, hosted, deployment, and production
proof is not present. This record makes no production verification claim.

## Identity and bounded effects

- Worktree: `C:\Users\mishr\OneDrive\Documents\CaseOps-worktrees\iplf066b-private-retrieval`
- Branch: `codex/iplf066b-private-retrieval-20260830`
- Base: `origin/main` at
  `45ba2853b2a4d7fff316161922cb5d080e3f6ade`
- Implementation: `55d2af96e38b1b8eaa1150646c2de69f66df3424`
- Schema effect: none; the implementation uses the five additive IPLF-066A
  tables at migration head `20260830_0002`.
- External effect: none; no push, merge, deployment, production request,
  external message, or real provider/model call was performed.

All data was synthetic: isolated companies and memberships, an entitled tenant,
Matter/document content with a unique harmless marker and malicious source
instruction, a second tenant with distinct content, stale source versions,
pending revocation event, 1,200 authorized PostgreSQL projections, and a forged
other-tenant candidate. The provider-minimization test used an in-process spy.

## Verification

| Gate | Result |
| --- | --- |
| Focused Ruff over all changed API/service/script/test modules | passed |
| `pytest -q tests/test_private_retrieval_foundation.py tests/test_private_retrieval_workflow.py tests/test_workspace_assistant_qa.py tests/test_role_guards.py tests/test_openapi_quality.py` on the rebased candidate | `24 passed`, `0 failed` in 100.36 seconds |
| Final route/OpenAPI selection after removal of the restricted-candidate disclosure field | `5 passed`, `0 failed` in 48.20 seconds |
| Generated OpenAPI client | regenerated successfully from the current application |
| Final web type generation and TypeScript | passed after the current OpenAPI client was regenerated |
| `npm run build --workspace @caseops/web` | passed; optimized Next.js production build generated 81 static pages |
| Dated Playwright `tests/e2e/iplf-066b-private-retrieval-2026-08-30.spec.ts` on the rebased candidate | `1 passed` in 16.1 seconds (`52.5` seconds total); local API/web fixture only |
| Real PostgreSQL 17/pgvector scale selection on the rebased candidate | `1 passed`, `106 deselected` in 47.68 seconds against a disposable known-credential container |
| Diff integrity | `git diff --check` passed before commit |

The PostgreSQL test inserts 1,200 same-tenant permitted projections plus a
forged other-tenant candidate. The ACL SQL prefilter returns at most 200 IDs,
hydration returns at most 20 results, the measured request uses no more than 20
SQL statements, every result remains in the permitted tenant/scope, and direct
hydration of the forged ID returns nothing. The exact disposable container was
removed after the passing run.

## Assertions proved locally

- Current active company, user, membership, `ai:generate` capability,
  entitlement, rollout, and tenant AI policy gate every consumer.
- SQL company/current-generation/canonical ACL/ethical-wall/lifecycle/source
  prefiltering occurs before bounded rank; hydration reauthorizes again.
- Scope and source selectors are closed, unique, non-empty, and bounded.
- A stale source version is rejected even when its old candidate ID is cached;
  the integrity surface reports the stale-source blocker.
- Candidate caches are tenant/membership/capability/generation/epoch/query/
  filter/source/locale partitioned and never bypass hydration.
- One-tenant rebuild batches never mix a second tenant. The provider spy sees
  bounded approved text only, with no tenant ID, record ID, label, ACL, scope,
  projection metadata, or second-tenant secret.
- Pending event lag is observable; the bounded worker applies the event,
  blanks content/embedding through the existing tombstone owner, and prevents
  retrieval and known-ID hydration.
- When private retrieval is active, Workspace Assistant cannot resurrect a
  revoked/missing document through its legacy direct extracted-text fallback.
- Browser reload hides the saved answer text, removes its citation, and forces
  repeat-query abstention; the narrow viewport has no horizontal overflow.
- The deterministic local provider strips embedded source instructions, so the
  browser regression exercises the intended untrusted-source boundary.

## Lifecycle hardening follow-up

Code revision `dc46780b60b47b0beff65c9ad466617fadd3c976` closes a
rebuild/disposition race and the remaining local portal/export crossover in
this bounded slice:

- private rebuild source enumeration and empty-shadow creation commit before
  provider I/O, so no tenant/generation lock is held while an external provider
  is running;
- projection writes and generation verification require the exact captured
  access-policy and tombstone generations, while activation and event enqueue
  serialize on the tenant row and refresh locked state;
- an event idempotency key reused for different actor/type/target/version/reason
  is rejected instead of silently aliasing the earlier event;
- Assistant session export reauthorization is exercised after a private source
  revocation and returns only the permission-changed placeholder with no copied
  answer or citation; and
- portal list/open/download now recheck the canonical document tenant, current
  version, privilege, confidentiality and shareable state without adding an
  N+1 query. A later restriction returns `review_required`, removes document
  metadata, and makes the download route return 404.

Local evidence for this follow-up:

| Gate | Result |
| --- | --- |
| Foundation + private workflow + portal regression files | `25 passed`, `0 failed` in 258.46 seconds |
| Final four affected lifecycle/export/portal cases | `4 passed`, `0 failed` in 92.64 seconds |
| Exact provider-deletion-during-rebuild case after the clean-session fence | `1 passed`, `0 failed` in 89.11 seconds |
| Focused Ruff, Python compile, and `git diff --check` | passed |
| Program manifest, ownership, ARCH-OPS, data-class registry, governance registry/map/change gate and runtime data-class projection validators | passed |

The provider-deletion regression commits a same-tenant tombstone from a second
session during embedding. The active projection is content/embedding-neutral,
the stale shadow is failed, retrieval returns nothing, and no stale write or
activation succeeds. Existing cross-tenant forged-ID and provider-batch tests
remain part of the 25-test run.

## Review/report manifests and safe outcome analytics follow-up

Code revision `505828addd4b8f16b3daed3ae610591d883a7f5c` closes the
private saved-output and AI-GUIDE-12 portion of the reciprocal scope without a
schema change or second analytics store:

- intelligent review freezes the exact private projection ID, source
  type/ID/version/hash, active generation ID and access/tombstone generations;
- worker post-provider fencing, review reads, publication, Draft/report reads,
  lists and exports reauthorize that exact manifest and fail closed on a stale
  epoch, revoked source or changed ACL;
- published `DraftVersion` output retains the private entry alongside selected
  public authorities instead of dropping private provenance;
- citation-open success is recorded only after creator/session/citation/source
  reauthorization; and
- the admin outcome endpoint derives the six AI-GUIDE-12 signals from canonical
  tenant rows, exposes no content/tenant/member/record dimensions, performs no
  employee scoring, and leaves permission-denial rate null because no honest
  denominator exists.

Local focused evidence for this follow-up:

| Gate | Result |
| --- | --- |
| Existing intelligent-review, AI-feedback and Workspace Assistant suites | `24 passed`, `0 failed` in 326.45 seconds before the new cases were added |
| New private review/report, tenant aggregate and citation-open/revocation cases | `4 passed`, `0 failed` in 136.07 seconds |
| Final aggregate plus route/OpenAPI/role selection | `4 passed`, `6 deselected`, `0 failed` in 109.79 seconds |
| Full API Ruff, generated OpenAPI TypeScript and web typecheck | passed |
| `git diff --check` | passed |

The private review regression publishes a lawyer-finalized report, verifies the
same private manifest in Recommendation and DraftVersion, applies an
access-generation event, then proves direct review/Draft/DOCX reads fail,
Draft list omission persists and another tenant receives 404. The aggregate
regression inserts a forged second-tenant denial and proves it is excluded.

## Delivery fence, inference surfaces and hosted-failure repair follow-up

Code revision `457c6190f83ca8aa68c24dde048288dcb27465cf` closes the
remaining local stream/autocomplete/count scope and repairs the exact failures
reported by Draft PR 399 at prior head `090d4ce7667229640a563f02ba1fed9a40a7daf0`:

- search, autocomplete and count capture the exact company, membership, user,
  capability, active generation, access-policy generation and tombstone
  generation, then reauthorize current canonical source state at delivery;
- autocomplete serializes labels and source references only. Workspace
  Assistant scope suggestions reload the current membership role, tenant AI
  policy, ACL/lifecycle/document policy and exact resource version before
  returning any title or secondary text;
- count rechecks the same fence after candidate selection and reports only a
  bounded visible count with an explicit ceiling and conservative capped flag;
- NDJSON streaming is no-store/nosniff and opens a fresh session before every
  row. Any missing row or changed actor, generation, epoch, ACL, lifecycle,
  source version or policy ends the stream without emitting later content; and
- hosted shards 5, 7 and 11 all failed because the earlier saved-output
  hardening made `_load_draft` require the current context while filing
  checklist, filing bundle and draft comparison still used the old call shape.
  All three callers now pass the exact context; no test, coverage or scanner
  rule was weakened.

The adversarial regression creates two private sources under one restricted
Matter, grants a member access, captures the stream fence/candidates, emits one
authorized row, then invokes the canonical grant-removal service from inside
the count path after prefilter and before count delivery. The count is zero,
the in-flight stream emits no second row, a not-yet-started stale stream emits
zero rows, autocomplete and Assistant scope options contain no revoked title or
snippet, and a second tenant receives empty results/counts even when it submits
the known first-tenant scope identifier.

Local focused evidence for this follow-up:

| Gate | Result |
| --- | --- |
| Private retrieval foundation + workflow suites | `22 passed`, `0 failed` in 73.83 seconds |
| Workspace Assistant QA, including bounded document-policy autocomplete | `6 passed`, `0 failed` in 47.11 seconds |
| Prior hosted-failure files: drafting studio, filing checklist and draft comparison | `62 passed`, `0 failed` in 229.16 seconds |
| Route-coverage matrix and OpenAPI quality | `5 passed`, `0 failed` across the two suites |
| Exact CI Ruff command and generated-client web typecheck | passed |
| Focused revoked-during-stream/count race after final header assertions | `1 passed`, `0 failed` in 18.87 seconds |

This is synthetic local evidence. Latest-head hosted CI/Security/CodeQL,
current-main integration, production worker scheduling, deployment and dated
exact-image production acceptance remain unproved at this revision.

## Remaining blockers

- Connect the future IPLF-071 canonical purge/provider executor to the proven
  private tombstone event contract and retain provider receipt or explicit
  deletion-delay exception evidence; this slice does not invent that executor.
- Wire the bounded event/rebuild entry points to the canonical durable
  production worker/scheduler and prove retry, alerting, lag SLO, and rollback.
- Run the complete PostgreSQL/migration/security suites, hosted CI/Security/
  CodeQL, integrate the exact reviewed commit onto `main`, deploy migration-
  first with the feature off, and collect dated exact-image production proof.

Until these gates pass, production activation must remain disabled and the
last verified generation plus fail-closed hydration/tombstones must be retained.
