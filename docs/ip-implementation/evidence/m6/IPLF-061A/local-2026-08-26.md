# IPLF-061A local evidence - 26 August 2026

## Verdict

Implemented and locally verified; release remains blocked until the exact
merged main revision is deployed and the dated production acceptance passes.
No requirement family or user-journey path is claimed by this derived
foundation slice.

## Delivered

- One reviewed Product Guide catalog with 27 guide sections and 43 navigation
  commands, compiled byte-identically into the API and web build contexts.
- A fail-closed projection validator wired into CI.
- Public versioned guide catalog and authenticated, permission-aware search
  contracts with current/stale, matched/permission-required/no-match states.
- Server-resolved fixed, custom, revoked, and platform capability handling;
  anonymous command search is rejected and an active custom role loses its
  command immediately after revocation.
- Deterministic bounded lexical search with no model, network, writer,
  analytics, worker, scheduler, migration, or corpus-scale query.
- Existing Sidebar and `/guide` contents now consume the generated projection.
- Reconciled a pre-existing guide defect: the page rendered 27 sections while
  the hand-maintained contents list claimed 26 and omitted judge mapping.

Catalog fingerprint:
`1f6ecb81d1a4507eb0995c2c06c2c3c2c8373c5dcf8f4110cfea5b37098168f5`.

## Verification run

- `pytest tests/test_product_guide.py tests/test_product_guide_catalog_gate.py -q`
  - 10 passed.
- `ruff check` on the new API, tests, and projection gate
  - passed.
- `python scripts/product_guide_catalog.py validate`
  - 27 sections, 43 commands, exact projection fingerprint passed.
- `npm run typecheck --workspace @caseops/web`
  - passed.
- `vitest components/app/Sidebar.test.tsx`
  - 6 passed.
- `playwright tests/e2e/iplf-061a-product-guide-foundation-2026-08-26.spec.ts`
  - 1 passed at 360px and 1280px.

## Release gate

Before deployment verification, require merged-main CI, Security, CodeQL,
migration-first deployment, latest-only API/web traffic, health and scheduler
audit, exact release identity, and
`iplf-061a-product-guide-foundation-2026-08-26-prod.spec.ts`. Judge refresh must
remain paused independently of this release.
