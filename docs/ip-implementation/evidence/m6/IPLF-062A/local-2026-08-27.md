# IPLF-062A local foundation evidence

**Implementation revision:** `25e0c50cf5839aa8f90377daf9e7544549ec1e6b`
**Storage-hardening revision:** `398b2ab23e9fa1032d63d0ecb4357a611c20b2a5`
**Recorded:** 27 August 2026
**Environment:** Windows 11 workstation, Python 3.13, SQLite migration
rehearsal, local API, and the repository's complete deterministic API matrix.

## Foundation delivered

Additive migration `20260827_0002` adds tenant AI policy enablement, assistant
model allowlist, policy version, and bounded retention plus tenant-scoped
sessions, explicit scopes, turns, and citations. Composite tenant foreign
keys, enum/range checks, uniqueness constraints, access/list query indexes,
retention index, `ModelRun` link, and downgrade refusal for retained evidence
are present in both Alembic and ORM definitions.

The foundation API is creator-private, versioned with optimistic concurrency,
and fail-closed for disabled policy, unsupported model, inactive membership,
cross-tenant scope, restricted Matter/IP scope, archived session, stale
version, and missing legal-hold-aware destruction authority. Data governance
registers 296 SQL tables and 4,772 columns with 1,598 ORM indexes and 651
Alembic/raw index declarations. Runtime projection and human view match the
canonical map.

## Automated evidence

| Gate | Result |
|---|---|
| Complete deterministic API inventory | 3,636 passed; 129 intentional skips; 362 files exactly once |
| Workspace assistant migration/foundation/QA selections | passed |
| Data-governance map validator | passed |
| Runtime data-class projection validator | passed |
| Ruff over API `src` and `tests` | passed |
| `git diff --check` | passed |

The matrix includes migration upgrade/downgrade/re-upgrade, populated downgrade
refusal, schema/index parity, tenant and creator isolation, policy OCC, scope
reauthorization, bounded queries, model-purpose allowlist, and audit/event
contracts.

## Residual release boundary

No production or deployment claim is made here. The exact integrated candidate
must pass Docker/PostgreSQL/index acceptance, hosted CI/security, canonical
`main` merge, migration-first deployment, exact API/web identity, and dated
production acceptance. Destructive conversation deletion remains unavailable
until IPLF-071 supplies approved legal-hold-aware disposition authority.
