# M6 IPLF-063 production release evidence

**Release revision:** `0610be30d8f856cd75898625c9357265d734b0b4`  
**Verified:** 29 August 2026  
**Pull request:** [#378](https://github.com/mishrasanjeev/caseops/pull/378)  
**Production verifier:** [GitHub Actions run 33252754848](https://github.com/mishrasanjeev/caseops/actions/runs/33252754848)

## Exact serving identity

| Surface | Revision | Release SHA |
|---|---|---|
| API | `caseops-api-00383-6km` | `0610be30d8f856cd75898625c9357265d734b0b4` |
| Web | `caseops-web-00362-hbp` | `0610be30d8f856cd75898625c9357265d734b0b4` |

The API image was pinned at
`sha256:d8a1cbbed0cbbb965bcdbfce0fa2d5909623a57058d736ab1c85cd8b370604de`.
Both services served 100 percent latest-only traffic with no revision tags. The
public health endpoint returned `{"status":"ok"}` and remained responsive at
about 104 ms while the production browser suite was running. API and web
release-identity endpoints returned the exact SHA above.

## Migration and recurring work

Migration execution `caseops-migrate-job-9jtjb` completed successfully before
traffic moved. Database-index execution `caseops-db-index-health-9wqcb` then
passed the pre-route health gate on the same immutable API image.

The complete eight-job recurring-work inventory reconciled successfully to the
new digest. `caseops-extract-authority-metadata-daily` and
`caseops-judge-mapping-refresh-daily` remained `PAUSED` and were not executed.

The production QA bootstrap job was repinned before traffic without execution.
After the release gates passed, it executed exactly once as
`caseops-ip-qa-bootstrap-gvtxl`; the total execution count advanced from 15 to
16 and the execution succeeded on the released API digest.

## Hosted acceptance

Main CI run `33251125345`, Security run `33251125320`, and CodeQL run
`33251125339` passed for the exact release commit. Production verification run
`33252754848` checked the exact serving identity before checking out that source
revision and completed successfully in 27 minutes 12 seconds.

The single-worker production RAM batch passed 94 tests in 26 minutes with five
intentional skips. It included the dated IPLF-063B UJ-18 production journey,
which passed in 4.7 minutes and exercised the Matter and IP-opposition review
workflow, source links, supporting and contrary authority handling, stale and
inaccessible evidence, citation-removal gating, and approved Draft handoff. The
independent notice-module batch passed a further two tests in 24.5 seconds.

The historical IPLF-027B A0 replay was not requested. IPLF-037B governed renewal
acceptance remained fail-closed and did not run because none of its four
externally approved legal fixture IDs is configured; that pre-existing legal
activation blocker is not IPLF-063 implementation scope.

## Local release gate

Before publication, the exact committed candidate passed a clean migration-first
Docker rebuild over fresh PostgreSQL and Valkey state, unrestricted and 512 MiB
database-index checks, API and worker health, exact image-label verification,
and the complete local browser inventory: 169 passed, two intentional skips,
zero failures, followed by a successful post-suite health check. The Docker
containers, network, and volumes were then removed.

Complete API shards and coverage gates, 111 live-PostgreSQL tests, complete web
Vitest inventory, TypeScript, a fresh Next.js build, governance projections,
manifest checks, scheduler inventory, and exact CI Ruff also passed.

## Honest release boundary

This evidence changes IPLF-063A and IPLF-063B to `deployment_verified`. It does
not close shared AI evaluation, feedback, analytics, disposition, legal,
provider, or human UAT work owned by later slices. IPLF-065 and IPLF-066 remain
independent owners of those controls.
