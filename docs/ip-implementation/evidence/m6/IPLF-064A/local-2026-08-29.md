# IPLF-064A local foundation evidence

**Implementation revision:** `274d537fa445030e086008acc0b2a033a4959ca5`
**Recorded:** 29 August 2026
**Environment:** Windows 11 workstation, Python 3.13, Node.js 22,
fresh production Docker images, PostgreSQL 17 with pgvector, Valkey 8.1,
API/document worker/web services, and Chromium.

## Foundation delivered

Additive migration `20260829_0001` creates only retained
`assistant_action_previews` handoff evidence. A preview seals company, actor,
assistant session and turn, proposal, action and target, target version,
canonical payload hash, token hash, AI-policy version, expiry, status, and
result linkage. It does not create an alternate Matter, task, Draft, IP,
audit, worker, or scheduler owner.

Preview performs no domain write. Confirmation locks the policy, assistant
session, preview, actor membership, and target; rechecks tenant, capability,
session version, policy version, target access/version/lifecycle, expiry,
payload hash, and HMAC token; then delegates one atomic mutation to the
existing MatterTask, Draft, Matter, or IP shared-work service. Successful
replay returns the existing result without a second write. Superseded,
expired, tampered, cross-tenant, stale-policy, stale-session, stale-target,
or no-longer-authorized confirmations fail closed.

The migration includes complete leading indexes for direct and composite
foreign keys. Downgrade refuses retained evidence. Existing canonical writers
received only an internal transaction-owner option so confirmation can commit
the domain result and both audit records atomically.

## Automated evidence

| Gate | Result |
|---|---|
| Focused migration/action/foundation/QA API selection | 18 passed; 0 failed |
| Complete API inventory | 3,681 passed; 129 intentional skips; 0 failed |
| Full API Ruff and route coverage | passed |
| Migration graph | single head `20260829_0001` |
| Manifest, ownership, ARCH-OPS, data governance, product guide, legal fixtures, golden queries, and scheduler inventory | passed |
| Data-governance map/runtime projection | valid; 297 SQL tables and 4,815 columns |
| Complete web Vitest inventory | 155 files; 788 passed; 0 failed |
| TypeScript and fresh Next.js production build | passed; 80 routes generated |
| Exact committed Docker migration and runtime identity | passed for `274d537fa445030e086008acc0b2a033a4959ca5` |
| PostgreSQL declared/FK index health | zero missing, invalid, mismatched, or uncovered indexes; zero schema drift |
| 512 MiB production index-health job | passed |
| Exact committed Docker browser inventory | 170 passed; 2 intentional provider/production-only skips; 0 failed |
| Post-browser API health and isolated Docker cleanup | passed |

## Release boundary

Local verification passed for the exact committed candidate. Hosted
CI/Security/CodeQL, canonical `main` merge, migration-first production
deployment, exact serving identity, one post-route QA bootstrap, and dated
production acceptance remain required before release can be marked deployed.

The authority-metadata and judge-mapping refresh schedulers remain PAUSED and
were not executed.
