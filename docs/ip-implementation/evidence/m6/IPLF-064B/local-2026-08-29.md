# IPLF-064B local workflow evidence

**Implementation revision:** `274d537fa445030e086008acc0b2a033a4959ca5`
**Recorded:** 29 August 2026
**Environment:** Windows 11 workstation, exact committed API/web production
images, fresh PostgreSQL and Valkey volumes, Chromium desktop and 360px
viewport acceptance, complete API/web inventories, and generated docs.

## Workflow delivered

Ask this Workspace continues to retrieve and answer only inside an explicit,
permissioned scope. Navigation and search proposals remain immediate and
read-only. A compatible task, Draft, or allowlisted Matter field proposal now
opens a separate editable review dialog. The browser does not invent tenant,
target, policy, session, catalog, or version identifiers.

The first action submits editable details only to the preview endpoint. The
dialog then shows the exact target, before/after changes, warnings, expiry,
and the explicit statement that nothing is written until confirmation. Only a
separate `Confirm action` command can create the canonical result. Success
shows one exact deep link and updates the assistant session to the server's
returned version.

The dated UJ-23 acceptance proves that proposing and previewing a task leave
the Matter task list empty, confirmation creates exactly one task, result
handoff is exact, and the dialog remains non-overflowing at 360px. It then
previews an allowlisted Matter field change, makes a concurrent canonical
Matter edit, and proves confirmation rejects the stale target without changing
the client name. API cases additionally cover Matter and IP tasks, Matter and
IP Drafts, allowlisted Matter updates, tampering, expiry, supersession,
capability revocation, cross-tenant access, policy/session drift, idempotent
replay, and transaction rollback.

## Automated evidence

| Gate | Result |
|---|---|
| Focused assistant component suite | 4 passed; exact preview then separate confirmation asserted |
| Complete web Vitest inventory | 155 files; 788 passed; 0 failed |
| Complete API inventory | 3,681 passed; 129 intentional skips; 0 failed |
| TypeScript and fresh Next.js production build | passed |
| Focused fresh-Docker UJ-23 action journey | 1 passed; 0 failed |
| Exact committed full Docker browser inventory | 170 passed; 2 intentional skips; 0 failed in 6.9 minutes |
| IPLF-064 journey inside full matrix | passed in 2.0 seconds |
| 360px layout | no document overflow; dialog actions visible and uniquely accessible |
| No-write-before-confirmation and one-write-after-confirmation | passed through browser plus direct PostgreSQL-backed API reads |
| Stale-target exception | confirmation rejected; canonical field remained unchanged |
| Public law-firms page and Product Guide | updated and included in full browser/build gates |

## Honest residual scope

The local normal and proposed-write exception journeys passed against the
exact committed candidate. Hosted gates, `main`, production migration and
identity, and the dated production journey remain mandatory. IPLF-065 retains
independent safety/evaluation ownership and IPLF-066 retains private
retrieval/analytics ownership; their shared UJ-23 rows therefore remain in
progress.

No production or deployment claim is made here. The authority-metadata and
judge-mapping refresh schedulers remain PAUSED and were not executed.
