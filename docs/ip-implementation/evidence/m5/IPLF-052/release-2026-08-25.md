# IPLF-052/053 release evidence

**Date:** 2026-08-25
**Capabilities:** IPLF-052A/B journal ingestion, watch profiles and review hits;
IPLF-053A/B opposition, enforcement, Matter, task/deadline, and client-report
handoffs.

## Exact review and merge chain

- Implementation PR `#332`, reviewed head
  `5ec38480bda3713173849acddf0faf50b337b352`, merged as
  `34dc2a33b38a5ae1e5da3c787a469845129061f5`.
- Release-hardening PR `#333`, reviewed head
  `f36b23cf1cab78ef9eda70f564954958ccb00a3b`, merged and deployed as
  `8ed5351374e194044e24abe773501f52f75b8c3c`.
- PR `#332` CI `32758807024`, Security `32758807036`, and CodeQL
  `32758807055`: passed.
- PR `#333` CI `32768290099`, Security `32768290007`, and CodeQL
  `32768289980`: passed, including all 13 API shards, aggregate coverage,
  PostgreSQL/pgvector, web build/tests, and the application Playwright suite.
- Exact-main CI `32770786149`, Security `32770786146`, and CodeQL
  `32770786209`: passed.

## Corrective release history

The first exact implementation deployment exposed two release gaps rather than
concealing them behind a completion claim:

- `caseops-ip-journal-watch` had no existing Cloud Run job, while the former
  inventory reconciler could only update jobs that already existed. PR `#333`
  added a validated bootstrap contract and fail-closed create/converge/verify
  behavior for its command, arguments, environment, secrets, service account,
  Cloud SQL binding, resources, retry policy, timeout, image, schedule, and IAM.
- Production verifier `32765358384` passed IPLF-052 but found the existing
  IPLF-026B access-preview assertion raced a successful request that could take
  4-9 seconds under a cold concurrent page burst. PR `#333` added visible
  pending feedback and bounded the actual preview response before asserting the
  rendered panel. It did not turn an API error into a pass or merely extend a
  locator timeout.

Push-triggered production runs `32761194946` and `32763788219` also began before
their expected release was serving and did not establish product acceptance.
These failed runs remain part of the audit record.

## Exact production deployment

The canonical `scripts/deploy-prod.sh` pipeline ran from a clean worktree at
the exact final merge revision.

| Surface | Evidence |
| --- | --- |
| API Cloud Build | `deda6f4c-bfb5-4926-8ca3-1b7dcc4e1077` |
| API image digest | `sha256:67800985f4c23ed1f06833443bb10161d82d82b95a8bd08a42b30f9da409009d` |
| API revision | `caseops-api-00344-b8v`, 100% latest traffic |
| Web Cloud Build | `b82b7264-696f-47fb-bf74-425e018f7e32` |
| Web image digest | `sha256:6fecba8583e79ee18701c776861fb76d9bae6e65ef809e0a15fa903f08ef340a` |
| Web revision | `caseops-web-00322-zwm`, 100% latest traffic |
| Migration execution | `caseops-migrate-job-nb8q6`, passed before traffic |

The deploy reconciled and verified seven recurring jobs against the immutable
API digest. `caseops-ip-journal-watch` was enabled at `15 */2 * * *` in
`Asia/Kolkata` with the complete bootstrap-owned runtime contract. Obsolete
revision tags were cleared, the ClamAV sidecar/startup probe passed, API health
returned `{"status":"ok"}`, and `scripts/verify_deployed_release.py` resolved
both serving revisions to the full expected SHA.

## Production acceptance

Exact-production workflow `32770786174` passed in 32m21s after checking out the
serving SHA:

- RAM production batch: 81 passed and five expected skips in 19m03s, including
  the dated IPLF-052 journal/watch journey and corrected IPLF-026B preview,
  grant, apply, revoke, and stale-write flow;
- IPLF-027B A0 quiescence acceptance: one passed;
- notice-module suite: two passed;
- IPLF-037B renewal execution: explicitly skipped because no approved external
  legal fixture IDs are configured.

## Activation boundary

Repository implementation for IPLF-052A/B and IPLF-053A/B is implemented,
reviewed, merged, deployed, and verified at the exact production revision. The
release proves sourced fixture/manual journal ingestion, watch review,
notifications, and canonical handoffs. It does not claim a live IP India
provider contract or automated registry feed, lawyer/legal-SME approval,
independent provider acceptance, pilot-firm UAT, or an activated IPLF-037B
renewal rule set. Those external decisions remain fail-closed and separate from
repository completion.
