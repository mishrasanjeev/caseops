# IPLF-057B exact-production release evidence

**Date:** 2026-08-25

**State:** `deployment_verified` for repository implementation. This record
does not claim WIPO/provider activation, Registry confirmation, lawyer or legal
SME approval, or independent UAT acceptance.

**Pull request:** `#339`

**Merged and deployed revision:** `57706e9bb51fd17e495ecc9cd9f4533bb7511010`

## Hosted and exact-main gates

PR #339 merged only after its required matrix passed: Ruff and program-control
validation, all 13 API coverage shards, aggregate API coverage, PostgreSQL 17
and pgvector validation, web typecheck/Vitest/build, application Playwright,
OpenAPI drift, secret, licence, dependency, Cloud Run manifest, audit, Codex
review, and CodeQL checks.

The exact merge then passed CI run `32833476775`, Security run `32833476783`,
and CodeQL Advanced run `32833476965`. Local `main` and `origin/main` resolved
to the same merge before deployment.

## Migration-first exact deployment

The canonical guarded deployment built and published immutable images before
running the migration and moving traffic.

- API Cloud Build `5a3da251-0382-4d17-8dfe-0c6f3b3e573d`; digest
  `sha256:ad6351f57ddba8e8b6e2bb236527fd46668b104bc8a11414cbf2a188c56c07e4`.
- Web Cloud Build `9d6af2f4-8334-4883-bb71-b2c293b255f5`; digest
  `sha256:3f58a4e4f37fe357473a5a9d91b4b4c5cdcab61984144277102afe0ff7a9c0db2`.
- Migration execution `caseops-migrate-job-rjjtj` completed successfully in
  14.67 seconds, including additive migration `20260825_0004`.
- API revision `caseops-api-00350-nj9` and web revision
  `caseops-web-00328-qnn` each received 100 percent latest-only traffic; stale
  revision tags were absent.
- Scheduler/job inventory reconciliation passed and active jobs were pinned to
  the immutable API digest. The superseded midnight case-tracking schedule
  remained paused.
- `/api/health` returned `{"status":"ok"}` and API/web release-identity
  endpoints returned the full expected SHA. The exact-release verifier passed.

## Dated production acceptance

The first workflow-dispatch run `32836745085` checked out the exact serving
revision. Its release identity, secret gate, setup, A0 quiescence, and notice
acceptance passed. The broad batch completed `83 passed` and `4 skipped`, with
one failure in the older 16 August navigation proof: production correctly
included the new `Madrid portfolio` action, while that test's explicit expected
navigation map still described the pre-Madrid sidebar. This was test-fixture
drift, not a hidden or missing production action.

The production-proof branch added Madrid to the canonical production suite and
updated that expected navigation map. Verification against the same exact
release then passed:

| Gate | Result |
| --- | --- |
| Dedicated UJ-35 normal plus EXC-01 through EXC-04 | 1 passed in 11.1s |
| Corrected 360px grouped-navigation proof | 1 passed in 29.0s |
| Isolated IPLF-025B reminder retry after one no-request browser timeout | 1 passed in 21.7s |
| Complete corrected production batch | 85 passed, 4 governed skips, 0 failed in 8.7m |
| Web TypeScript typecheck | passed |
| Production Playwright discovery | 89 tests in 29 files; Madrid assigned to the isolated tester project |

The first complete corrected local batch had one transient browser-side
timeout before any hearing POST was emitted. The same reminder journey had
passed in hosted run `32836745085`, passed immediately in isolation, and passed
again in the final complete batch in 19.2 seconds. No API error or persisted
reminder defect was observed. The final complete batch is the promotion gate.

The four governed skips are data-dependent legacy checks whose preconditions
were absent in the QA workspace. The separately configured external renewal
fixture also remained unavailable, so its workflow step was skipped. These do
not defer IPLF-057B repository behavior or turn external legal/provider
acceptance into a pass.

## UJ-35 coverage and boundaries

The dated production journey created synthetic QA records only and proved the
outbound basic-mark/MM2/Office-of-Origin/IR sequence, independent India and EU
designations, impact-review-only central attack, designation refusal isolation,
unresolved WIPO/national candidates, separately attributed local-agent work,
openable source links, 360px controls, guide navigation, truthful landing-page
copy, and fail-closed WIPO readiness.

No live WIPO call, filing, fee payment, Registry mutation, external recipient,
or client record was used. WIPO activation remains blocked by independent
contract, permitted-use, coverage, credential, legal, security, cost, release,
and operator approvals. Manual sourced Madrid work remains the released safe
fallback.

## Result and next ownership boundary

IPLF-057B is implemented, passed, merged, migrated, deployed, and verified on
the exact serving implementation revision. IPLF-058A may proceed from released
main. Its backend foundation must extend the deployed `IpTitleInterest` writer
and shared docket-event/deadline/document/cost owners; it must not create a
second ownership ledger or reuse the IPLF-049 rectification/cancellation
proceeding workspace for assignment/licence recordals.
