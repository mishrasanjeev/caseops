# IPLF-056A exact-release evidence

**Date:** 2026-08-25

**Merged revision:** `ec5846a0c5f9add68531efac2bbf5acaa429c50f`

**Pull request:** #336

## Hosted gates

The pull request and exact merged `main` revision passed the repository's required
CI, security, and CodeQL gates. Exact-main CI run `32805990891` included all 13
API shards, aggregate coverage, PostgreSQL, Ruff, web checks, and the full Playwright
suite. Security run `32805990860` and CodeQL run `32805990923` also passed.

The push-triggered production verifier `32805990899` timed out while production
still served the preceding release. It did not execute acceptance against a wrong
revision and is superseded by the successful post-deployment exact-release run below.

## Deployment identity

The canonical production script deployed the exact merged revision.

| Component | Verified identity |
| --- | --- |
| API image | `sha256:3d1aeea0342137e9a20e8cbd41c41aa84424191030f50b991466a215481adaef` |
| API revision | `caseops-api-00347-6ls` |
| Web image | `sha256:1abd5cff5457bc37838d0cfd8183f12b452ea50985888797a720f6a654cd9bf8` |
| Web revision | `caseops-web-00325-mfl` |
| Migration execution | `caseops-migrate-job-kp7dz` succeeded |
| Health | `{"status":"ok"}` |

Both services route 100% of traffic to their latest revision with obsolete tags
cleared. All seven recurring jobs were reconciled to the exact API digest, the
superseded midnight case-tracking scheduler remained paused, and the ClamAV sidecar
check passed.

## Production acceptance

Exact-release workflow `32808381776` checked out the revision actively served by
both production services and completed successfully. Its broad RAM batch, IPLF-027B
A0 quiescence acceptance, and notice-module production suite passed. The optional
IPLF-037B renewal execution was skipped only after its configuration gate passed and
is unrelated to IPLF-056A.

## Boundary

This verifies deployment of the shared provider-operation foundation. It does not
claim that IP India or Indian Kanoon live calls are authorized or enabled. Those
external calls remain fail-closed until provider, legal, security, cost, secret, and
operator acceptance is supplied by the authorized owners.
