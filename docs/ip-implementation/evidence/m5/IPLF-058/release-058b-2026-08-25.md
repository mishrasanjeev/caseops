# IPLF-058B exact-production release evidence

**Date:** 2026-08-25

**State:** `deployment_verified` for IPLF-058B and all five UJ-36 paths. This
record does not claim completion of UJ-61 outside IPLF-058B's bounded title
presentation, Registry/provider activation, lawyer or legal-SME approval, or
independent UAT acceptance.

**Implementation pull request:** `#343`

**Production-scale correction pull request:** `#344`

**Merged and deployed revision:**
`7809ed33c0b26f7f3b8e84d3a107d6ef690c8c56`

## Hosted and exact-main gates

PR #343 delivered the IPLF-058B operator journeys and merged at
`971c1cf4ac856c62f4cfee95e90d1163a6ad70da` after its required hosted checks
passed. Its exact production verifier then exposed a real production-scale
defect: the selected recordal detail waited for corpus-wide docket and document
catalogues. Production timings were about 8.1 seconds for 433 dockets and 2.7
seconds for 124 documents, so the dated recordal journey could not render its
detail tab within the acceptance budget.

PR #344 corrected that defect without a schema change. The selected recordal
now loads one docket directly and documents through a bounded, tenant-scoped
`docket_id` filter. Corpus-wide catalogues are deferred until Create or
title-family selection actually requires them. API and component regressions
prove bounded document selection and detail rendering while both full
catalogue promises remain unresolved.

PR #344 merged only after Ruff and program-control validation, all 13 API
coverage shards, aggregate API coverage, PostgreSQL 17 and pgvector validation,
web typecheck/Vitest/build, full application Playwright, OpenAPI drift,
security, licence, dependency, Cloud Run manifest, audit, automated review, and
CodeQL checks passed.

The exact merge then passed CI run `32874739865`, Security run `32874739895`,
and CodeQL Advanced run `32874739871`. Local `main` and `origin/main` both
resolved to the exact merge before deployment.

## Migration-first exact deployment

The canonical guarded deployment built immutable images, ran the migration job
first, reconciled recurring jobs, and only then moved production traffic.

- API Cloud Build `ab68c9ce-15c9-484c-9057-1a53b6f6bec4`; digest
  `sha256:34c731b099bcf0071ddef5743a24880b3c1e7c2e308d0e78b36e34dfcaec5dd9`.
- Web Cloud Build `0178dd9f-ec83-48a8-99e3-52aa98a1b369`; digest
  `sha256:33de361319f5cdc4762382e0362f7f39c1ba532dcab4e98e64825dc27445556d`.
- Migration execution `caseops-migrate-job-4gt5h` completed successfully on
  the immutable API image. IPLF-058B itself introduced no schema change.
- API revision `caseops-api-00355-8rn` and web revision
  `caseops-web-00333-jls` each received 100 percent latest-only traffic; stale
  revision tags were cleared.
- Scheduler/job reconciliation passed, including immutable API-image pinning
  and the paused superseded midnight case-tracking schedule.
- `/api/health` returned `{"status":"ok"}` and both serving services reported
  release tag `7809ed3`.

## Dated production verification

Prod-verification run `32874739889` checked out the exact serving revision and
passed in 33m45s:

| Gate | Result |
| --- | --- |
| Exact API/web serving identity and production-secret gate | passed |
| Complete production RAM batch | 86 passed, 4 governed skips, 0 failed in 20.1m |
| IPLF-058B dated recordal workflow | passed within the RAM batch |
| IPLF-027B A0 quiescence acceptance | 1 passed in 23.4s |
| Notice-module production suite | 2 passed in 19.8s |
| IPLF-037B external renewal fixture | governed skip; approved fixture IDs remain unconfigured |

The four RAM skips are data-dependent checks whose preconditions were absent in
the dedicated QA workspace. The renewal skip is the existing external
legal-governance activation blocker; neither category defers IPLF-058B.

## Released scope and boundaries

The release provides the post-registration recordal workspace for assignment or
transmission, registered-user/licence, name/address, and related recordals. It
preserves registered, beneficial, effective, pending, and disputed title states;
requires canonical instruments and exact Registry sources; supports correction,
partial assignment, conflict review, approval, and immutable transaction
history; and exposes title at date without allowing family association to mutate
another right.

All five UJ-36 normal/exception paths are implemented, verified, and deployed.
IPLF-058B's IPLF-039E-owned chain-of-title presentation also passed its allocated
tests, but global UJ-61 remains `in_progress` because IPLF-039H still co-owns
unfinished scope. IPLF-037 remains the renewal owner, IPLF-049 remains the
proceeding owner, and existing Registry, document, deadline, cost, access, and
audit writers remain canonical.

No live Registry mutation, filing, payment, provider call, external recipient,
or client record was used. Independent human, legal-SME, provider, and UAT
acceptance remain pending external approvals rather than deferred repository
implementation.

## Result and next ownership boundary

IPLF-058B is implemented, passed, merged, deployed, and verified on the exact
serving revision. The IPLF-058 epic is deployment-verified for its bounded scope.
The ordered implementation queue advances to IPLF-059A.
