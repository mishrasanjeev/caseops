# IPLF-055 release evidence

**Date:** 2026-08-25  
**Release SHA:** `cb31627fbd6d08e168024f159105195929390ed4` (`cb31627`)

## Release result

- PR #335 merged to canonical `main`; exact-main CI, Security, and CodeQL passed in runs `32799314337`, `32799314329`, and `32799314397`.
- Production API revision `caseops-api-00346-6bp` and web revision `caseops-web-00324-2cc` served 100 percent latest-only traffic with no stale revision tags.
- The migration job, scheduler reconciliation, production health, and exact API/web release identity passed for `cb31627fbd6d08e168024f159105195929390ed4`.
- Immutable image digests began `sha256:ad986` for API and `sha256:9e90` for web.

## Production acceptance reconciliation

Exact-release workflow `32801782888` checked out the serving SHA and reported 82 passed, four intentionally skipped, and one failed assertion in the broad production suite. The product rendered the new `IP client portal` navigation item correctly; the test's static expected-navigation list had not yet registered that route. A0 quiescence passed and both Notice tests passed. Governed renewal acceptance was skipped because its approved production fixture IDs were not configured.

Commit `2db21d9bca770dfd23e7c7e8c4adfa4c9e6e7031` registers `/app/ip/client-portal` in the production-safe navigation expectation. The exact previously failing test was rerun at 360px against the still-serving `cb31627` API/web release using the production QA secret-manager identity and passed: one test in 24.7 seconds. This is a test-harness correction against the unchanged released product, not an unverified product substitution.

## Claim boundary

IPLF-055 repository implementation and exact-production release verification are complete. The dedicated grant/report/document/instruction/revocation journey remains backed by its dated local browser and API acceptance because production-safe fixture creation does not mint unrestricted portal credentials. Independent law-firm UAT, legal acceptance, and provider acceptance remain external acceptance rather than deferred repository work.
