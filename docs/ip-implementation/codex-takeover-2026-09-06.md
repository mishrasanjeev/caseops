# Codex Backlog Takeover

Owner: Codex. Status: in progress. Local Docker first; no release claim.
Baseline: main `6a85cd5025101fd453bdc9b8472e963d16ba708a`, with the preserved
September 05 bug candidate. The other CaseOps task, "Review pending work",
was idle on inspection. Its old release activity is not current acceptance.

## Scope And Evidence

### Priority Continuation - September 07

Current work extends the canonical relationship owner with append-only patent
priority evidence, sourced correction/withdrawal, cycle and application-fact
validation, bounded family graph views and the Product Guide intake entry.
Focused evidence is in `evidence/local-patent-priorities-2026-09-07.md`: 88 API,
nine PostgreSQL, one 10,000-application/1,000-family scale and 64 frontend checks
pass. The newer 7ec22 Docker runtime now has 178 PostgreSQL passes, 968 frontend
passes, all three corrected priority browser widths and four mobile passes.
Exact desktop reconciliation records 225 passing identities, BUG-010 failed
and five skipped checks out of 231; the original failed reports are retained.
All 4,314 API identities have explicit passing evidence, including the 29-test
deadline correction, rather than a clean original full-run certificate.
Dense/deep-history ancestry bounds, P2-P6 and whole-product release acceptance
remain open. The older c351 checkpoint and its workbook tab remain historical.
All 25 incomplete slices remain incomplete; production stays NO-GO.

### Party Continuation - September 07

Mapping: UJ-29 / PAT-01 / IPLF-080-FAMILY / IPLF-080-COMPAT, P1.
Status: local party acceptance passed; whole-program release remains NO-GO.
Current evidence: `evidence/local-patent-parties-2026-09-07.md`. All 168
PostgreSQL checks pass in both fresh-stack and isolated-container runs. The
complete frontend passes 899 tests; the corrected complete patent browser
selection passes 13 journeys. The original API inventory failure was corrected
in a 121-test replay; all 4,286 collected API identities have explicit passing
evidence. The complete desktop run finished at 222 passed, one BUG-010 failure
and five skips; the separate mobile project passed all four tests. Production
remains NO-GO. The explicit dirty migration/governance gate also passes after
reviewed comment-only annotations, with tested-image AST equivalence proved.
These counts are not full-PRD completion or production acceptance.
The existing IpPartyAndRole remains the party owner. Its one-to-one patent
detail pins address, exact source version/hash, actor, reason, parent versions
and docket-scoped sequence. Inventor/applicant/proprietor/agent/licensee facts
are append-only and may be explicitly superseded without rewriting history.
This does not infer ownership, recordal, priority or authority to share data.
Family and application workspaces use one shared party editor/history view.

Offline focused API: 62 passed (`ip080-party-corrected.xml`), after correcting
an invalid second-tenant bootstrap fixture. Focused frontend: 48 passed in four
files (`ip080-party-web-focused.log`); full TypeScript passed. The initial
PostgreSQL selection had nine passes and one real different-actor closure
deadlock (`ip080-party-postgres.xml`). The private tenant authority fence now
permits implicit Company FK locks without weakening authority serialization;
bootstrap shares that fence. The corrected 14-test PostgreSQL run passed both
parent-kind closure races, interrupted-index recovery, immutable/composite
integrity, 10,000-fact bounded retrieval and three existing private-index overlap
regressions (`ip080-party-lock-corrected.xml`). Two dedicated authority-FK tests
also passed (`private-authority-fk-lock.xml`), proving a real pending claim does
not block authority, a second authority writer does wait, deletion remains
blocked and bootstrap retains exactly one active generation.

The six dated party Playwright journeys pass within the corrected 13-journey
fresh-image replay. The older application runtime below and production do not
contain these party changes. Priority/graph work and P2-P6 remain pending. No
commit, push or deploy has occurred. The dated evidence record preserves the
failed original browser run and later test-only corrections. Evidence files
are under `C:/tmp/caseops-ram05sep-20260905/`.

### Application Continuation - September 07

Mapping: UJ-29, PAT-01, IPLF-080A/B, IPLF-080-FAMILY and IPLF-080-COMPAT.
Status: Partially implemented; release remains NO-GO. The application vertical
now has independent restricted dockets/assets, immutable fact and identifier
versions, source hashes, bounded identity reservations/search, corrections,
history, source download and canonical lifecycle controls. Migration
20260906_0002 extends the schema without rewriting existing trademark records.
OpenAPI, governance projections and the program manifest were regenerated.

The complete current frontend suite passed 882 tests in 166 files in offline
Docker (`ip080-applications-web-docker-complete.log`). TypeScript and targeted
Ruff checks passed. These are not browser or production acceptance.
An additional local probe reproduced an active application correction failing
with 404 after a sibling sharing the source was closed. The source-lock helper
treated read-only source parents as mutation targets. Its restricted historical
source exception now preserves terminal writes and source access checks. The
first focused Docker run had 55 passes and one invalid-fixture failure: the
database correctly rejected a contradictory closed/active docket. The corrected
negative test asserts that constraint before testing a real closed target.
Subsequent offline adjacent coverage passed all 163 tests
(`api-full-ip080-source-guard-adjacent-corrected.xml`), and all ten dedicated
application PostgreSQL tests passed (`ip080-applications-postgres-final.xml`).

Fresh Docker runtime `1a0853e04849d96c2c37ead95d6f3385320b7ff4` was built from
source fingerprint `1a0853e04849d96c2c37ead95d6f3385320b7ff4966b14e4e6aa292b671b8369`.
API and web builds, migration 20260906_0002, complete index coverage and the
512 MiB index-health gate passed. The full PostgreSQL selection passed 155 tests
in 2,316.46 seconds (`ip080-applications-current-docker.log`). The post-rehearsal
index report had no missing/invalid/mismatched indexes but retained a cumulative
Matter sequential-scan warning; this is not a whole-product latency claim.

The first browser invocation passed all three family journeys but stopped the
three application journeys at a strict locator collision with Next's route
announcer. Scoping the existing error assertion to its named form was the only
test change before replay; application runtime code and images were unchanged.
All seven complete application/family/source-scope journeys then passed with
zero skips in seven minutes (`ip080-browser-corrected.xml` and matching log).
Mobile and desktop editor/lifecycle screenshots were inspected. The PowerShell
wrapper returned nonzero after the passing test output; independent JUnit,
API/web identity, API health and running-worker checks passed. Therefore the
browser result is accepted separately, not misreported as a successful original
`verify-docker.ps1` invocation. No production acceptance is claimed.

The source guard now rejects changed source after build, before browser
acceptance and before certification; its four native PowerShell cases pass.
The earlier 1842a07a2038 image was superseded before browser acceptance and is
not evidence for the source-lock correction. The earlier partial full API run
was stopped, not passed. All detailed logs remain under
`C:/tmp/caseops-ram05sep-20260905/`.

The party continuation above supersedes party implementation status. Priorities,
the graph and full P1 acceptance remain pending; P2-P6 are
not complete. No new application code has been committed, pushed or deployed.
The retained f2f21af176f9 tester below remains an older family-only baseline.

### Historical Family Acceptance Checkpoint

This section supersedes historical "current/final" labels below. Source runtime
identity: `f2f21af176f93c5ec4c18be36745798250a3a415`; complete dirty-source
fingerprint: `f2f21af176f93c5ec4c18be36745798250a3a4157249feb44c2b4507fcb49381`.
It is not a Git commit. Docker project: `caseops-acceptance-f2f21af176f9`.
Tester app: http://127.0.0.1:38452; API 38450; PostgreSQL 38453; Valkey 38454.
The user's local test-legal account and unchanged original import data exist here.
The test-owned loopback proxy is stopped after each run. After acceptance, a
separate hidden loopback helper on port 38451 was left running for the retained
tester app (PID 32880 at startup); its API build identity was checked. It is a
local helper, not a production service. No paid APIs were used.
The retained tester subsequently signed in and loaded the first 50 Matter
records through that proxy (`current-local-tester-browser.log`); the screenshot
was inspected. Its first ad hoc smoke invocation used the incorrect heading
"Matters" and failed after successful login/data load. The actual page heading
is "Matter portfolio"; the corrected read-only check passed. This did not
change product code or erase the earlier failed assertion.

| Verification | Latest result | Evidence and limitation |
| --- | --- | --- |
| Complete offline API | 4,093 passed, 0 failed, 147 skipped; 2,265.09s | `api-full-ip080-full-api-pinned-tools.xml`; eight workers, network disabled, pinned test tools, executable tmpfs |
| Exact API skip reconciliation | All 147 skipped node identities have independent passing evidence | 145 in `api-full-ip080-current-all-postgres.xml`, one in `api-full-ip080-current-native-reranker.xml`, one in `ip080-current-windows-shim.xml`; zero unmatched. All 4,240 collected nodes have evidence, not every possible PRD flow |
| Real Temporal and adjacent gates | 106 passed, 54.02s | `api-full-ip080-temporal-offline-focused.xml`; real local workflow plus executable/hash guards, guide, health and deployment regressions |
| Native reranker | 15 passed, 14.36s | `api-full-ip080-current-native-reranker.xml`; baked fallback model, network disabled. No Voyage or representative production-quality claim |
| Windows-specific shim | One passed, 0.69s | `ip080-current-windows-shim.xml`; mocked subprocess resolution, no real cloud command |
| Complete frontend | 862 passed in 164 files | `ip080-lifecycle-full-web-corrected.log`; no frontend source changes afterward |
| Fresh image builds and migration/index checks | Passed; schema 20260906_0001 | `ip080-current-fresh-full-docker.log`; API and web built from current source; 512 MiB index-health job passed |
| Full fresh PostgreSQL selection | 145 passed, 4,095 deselected, 1,117.46s | Same verifier log; host test process against Docker PostgreSQL. Separate 145-test in-container proof also passed |
| Complete desktop browser selection | 213 passed, 1 failed, 5 skipped; 30.0 minutes | Same verifier log and `ip080-current-full-browser-artifacts`; 219 tests, one worker, zero retries. BUG-010 remains a real failure |
| Mobile project | Four passed, 37.8s | `ip080-current-mobile.log` and matching artifacts; one worker, zero retries |
| Original-data browser commit and replay | 496 rows: 391 created, 97 invalid, 8 duplicates; idempotent confirmation | `ip080-current-original-import/browser-replay.log`; original input values unchanged |
| Every original cell and mapped field | 10,416 exact cells; 391 Matters x 28 fields | `ip080-current-original-import/local-all-data-evidence.json`; current runtime, tenant/active-state and complete list identity also checked |

All evidence names above are relative to `C:/tmp/caseops-ram05sep-20260905/`.
The dated Downloads summary workbook was refreshed after these runs. Its five
original sheets preserve 1,054 populated cells except two explanatory headings;
the two current tabs report this exact candidate and formula-reconciled totals
of 25 incomplete, ten in progress and fifteen not started. Formula-error scanning,
changed-range visual review and read-only export/preservation checks passed.
Fresh mobile and desktop patent lifecycle screenshots were also inspected.
API image manifest: `sha256:e542e885084dacfa9eb251226b81697ec937b52416eb340a1ef52bccb7f302b0`.
Web image manifest: `sha256:d7f2f396d9ed89bf88b92fe100a78a669b8d5bcd4ed730ff2e932b4913f0054b`.
Test-tools image manifest list: `sha256:9bba253a6bc44091bb357574c1d4fe5d79bdaf044f808377dc9726848b191574`.
Later evidence/documentation edits are not retroactively included in those images.
The post-test index scan had no missing, invalid or mismatched indexes, but
retained a cumulative Matter sequential-scan warning. This is not whole-product
performance acceptance. The fresh full browser run includes the guide-search
change, all three family lifecycle widths and both qualified public pages.

Five desktop skips remain explicit: four exact-production revision scenarios
and the provider-gated Pine Labs payment-link scenario. They are not acceptance
passes. BUG-010 still has unavailable verified provisions for the reported Acts;
the existing Article 14 positive proof does not close catalogue completeness.
Do not weaken the test, enable unverified legal text or deploy this as complete.

Read-only production observation at this checkpoint: API revision
`caseops-api-00443-s4j` and web `caseops-web-00420-96j` both serve main `6a85cd5`.
The private-projection job's latest observed execution `...-q7hpg` succeeded.
Three observed summaries at 13:15, 13:20 and 13:25 UTC on September 06 report
four tenants, no truncation, no blockers/pending/failed events and no rebuilds.
The latest returned maintenance-error entry was September 04 16:10 UTC.
This is baseline health, not a deployment of this candidate or proof of every
historical execution. Evidence: `production-readonly-20260906.json`.

All 25 program slices remain incomplete: ten in progress, fifteen not started.
At that checkpoint applications, parties, priority graph, prosecution and
maintenance were still missing. The application continuation above supersedes
that implementation status, not the old runtime. Product Guide also lacks a patent/disclosure entry; add its real
intake journey through the existing generated catalogue owner as part of 080B,
not a parallel chatbot. PR 402 remains draft/conflicted/incomplete. The judgment
enrichment candidate remains separate and unintegrated; no 4.5+/4.8+ quality
score is established. No Git publication or production mutation occurred.

### Lifecycle Continuation

Mapping: IPLF-080A, PAT-01/04, UJ-29; M02/M08/M13/M14. The existing
canonical lifecycle service is reused, not replaced. New local work adds a
closed/all family filter, authorized read-only disclosure/source access after
closure, bounded canonical-event history and preview/confirm/reopen controls.
Creation replay, correction and document mutation remain blocked on terminal
records. Revoked grants remain denied on current/history/source endpoints.
Initial network-disabled Docker tests passed: 46 API tests (including shared
trademark/document/lifecycle controls) and 18 frontend tests. The full frontend
rerun passed 862 tests in 164 files after correcting the route-page assertion
for the new explicit active/terminal/all list scope. Its first run was 861/862;
the failed assertion is retained, not erased.

Fresh dirty-source image identity `0ecca1eeb20171ce18e49e6e637c85894258a670`
(not a Git commit) passed 143 PostgreSQL tests and post-test index health.
Its browser run passed the source-scope journey, but all three 393/768/1280px
family journeys failed at the second same-day close after a successful explicit
reopen. Close, authorized retained facts, exact source download, terminal-list
discovery and controlled reopening had already completed. The event duplicate
check incorrectly treated a different parent lifecycle version on the same
calendar date as a duplicate. This is a real shared lifecycle defect.

The local correction keys parent lifecycle identity to its expected resulting
version, keeps generic root-event forgery unavailable and aligns the conflict
display. Adjacent review also found missing backdate acknowledgements in the
canonical lifecycle event adapter; preview and commit now use the same blocker
for close and reopen. New regressions retain stale-command rejection, unchanged
state after rejection, source/child history and all three lifecycle versions.
These corrections are NOT in image 0ecca1eeb201. The corrected offline Docker
lifecycle/family/document/opposition/Madrid suite passed 106 tests. Fresh image
`2b53b74f031bbac56ac4010447dd382c3227acb3` built successfully and passed both
index-health checks, including the 512 MiB job limit. Its PostgreSQL run was
143 passed, one failed: the new wrapper invoked two independent bootstrap
journeys against one fixture, causing an expected duplicate-tenant 409 in the
second setup. The wrapper now parameterizes those journeys so each receives
its own isolated database. The complete corrected in-container PostgreSQL run
passed all 145 tests in 481.88 seconds (`ip080-current-all-postgres.log`, JUnit
`api-full-ip080-current-all-postgres.xml`). Post-test index health is ok with
no missing, invalid or mismatched indexes. Cumulative sequential-scan warnings
for Matters, memberships and companies are retained, not interpreted as proof
of production query performance. The failed verifier itself did not reach
Playwright; a separate identity-checked run subsequently passed all 12 selected
family/source/domain/Madrid browser journeys on this exact 2b53 runtime.
That includes same-day close/reopen/backdated-reclose at 393/768/1280px, final
version-three persistence, historical source downloads and unchanged legacy
trademark behavior. Screenshots were inspected; proxy stderr is empty.
Failure artifacts: `C:/tmp/caseops-ram05sep-20260905/ip080-lifecycle-first-browser-artifacts`.
Logs: `ip080-lifecycle-fresh-docker.log`, `ip080-lifecycle-full-web.log` and
`ip080-lifecycle-full-web-corrected.log` in that same evidence parent folder.
The first focused correction command used two nonexistent test-module names
and collected no tests; it is a harness failure, not acceptance.

The public solo-lawyer/general-counsel pages now remove unsupported corpus,
recall/rank, timing and statutory-completeness metrics. They state that no
representative legal-retrieval rating is claimed and statutory coverage remains
incomplete. New dated marketing tests require those qualifications and visible
metric text at 393/768/1280px. All six read-only browser checks passed on image
2b53; mobile/desktop screenshots were inspected. Evidence is in
`ip080-qualified-marketing-browser.log` and its matching artifacts directory.
Earlier image 2ba0658 is not evidence for these changes. P1 and all 25 slices
remain open. No production release has been attempted.

### Full-Suite Findings And Harness Correction

The full offline API run `ip080-lifecycle-full-api.log` finished with 4,076
passes, 145 skips and two failures. It captured the source before the shared
lifecycle corrections. One health test hardcoded the local environment while
the suite correctly ran in ci; it now explicitly tests local, ci and e2e.
The other failure exceeded the unchanged 1.5-second guide-search budget.
Repeated tokenization of immutable catalogue fields was real unnecessary work.
The service now caches at most 512 public text records, keyed by their complete
content, and normalizes each query once. Current permissions and result objects
remain uncached. Content changes, privilege changes and response mutation have
regressions. The focused guide/health/deployment suite passed 82 tests inside
network-disabled Docker (`ip080-guide-health-deploy-focused.log`).

The first tmpfs full rerun is not acceptance: it was interrupted after 10
deployment-harness failures, 370 passes and 123 skips. The mount was noexec;
chmod did not make fake CLIs executable, and PATH fell through to real git.
Network isolation blocked the attempted remote resolution. Storage was not full.
The corrected harness uses exec-enabled temporary storage, and fake creation
now rejects a non-executable filesystem before launching a deploy shell.
`ip080-full-api-current-exec-tmpfs.log` completed with 4,085 passed, one failed
and 146 skipped in 3,133.92 seconds. The one failure was Temporal trying to
download its test server: the tmpfs hid the binary cached under /tmp in the
base test image. The real binary is now retained under /opt in a test-only
tools layer, with an explicit executable path and SHA-256 checked before SDK
startup. Seven setup regressions reject incomplete configuration, missing,
non-executable, directory, oversized and mismatched artifacts. The real workflow
must still run offline; no skip or network exception was added. See
`../runbooks/offline-docker-test-tools.md` for the reproducible tools layer.
The 146 skips comprise 144 PostgreSQL nodes (subsequently expanded to 145 and
all passed in the separate database run), one opt-in native-reranker node and
one Windows gcloud-shim node. The latter two retain their separate recorded
platform evidence; the offline API run alone does not cover them.
These failures are preserved and are not rewritten as successful reruns.

The complete original-input replay also passed on 2b53: 496 original rows,
391 created, 97 invalid and eight duplicates. Every one of 10,416 retained
source cells and all 28 mapped fields on every created Matter matched. The
tenant/active-state checks, full listing identity and confirmation replay all
passed. Evidence: `ip080-lifecycle-original-import/local-all-data-evidence.json`.
The user's local test-legal tester account now exists at
`http://127.0.0.1:27397`; its API is port 27395 and PostgreSQL is 27398.
The loopback browser proxy is stopped after each run. This image includes the
lifecycle and marketing corrections but predates the later guide-search
optimization; do not use it as fresh-image evidence for that service change.

The manifest's IPLF-080B not-started label was stale: family UI, lifecycle UI
and their dated tests exist. It is now in progress, with the concrete files
referenced; all 25 slices remain incomplete (10 in progress, 15 not started).
Derived patent requirement/journey states follow the canonical validator's
aggregate policy, not a claim that prosecution or maintenance code exists.

### Official Statute Source Recovery

No statutory text was promoted to verified by these downloads. Multiple
India Code publication URLs, including the indexed January 2026 CPC URL,
currently returned an error page/404. A search snippet is not checked source
availability. The following public originals were successfully recovered and
hashed in the local `official-statute-sources` evidence directory:

- BNS: [official MHA Gazette PDF](https://www.mha.gov.in/sites/default/files/2024-04/250883_english_01042024.pdf),
  102 pages, SHA-256 `c9da896e7a16c481a46235789f74f545b7a9ed7f3a5c8049d0b1b9252c6731f4`.
- BNSS: [official MHA Gazette PDF](https://www.mha.gov.in/sites/default/files/2026-02/3_250884_2_english_01042024.pdf),
  249 pages, SHA-256 `5e60e2afe30d0fe7eca4f8126301146b76c86a444e690581f81eb564843517fe`.
- Companies Act: [official NCLT publication](https://nclt.gov.in/sites/default/files/basicpage/the_companies_act_2013.pdf),
  294 pages, SHA-256 `ac1a0f48a071edec253997a48c136873fa55fea5a9a4d5b1d7bab2f9e117c4bc`.

These are original enactment publications, not proof of current consolidated
law, effective provision dates or amendment coverage. Section-by-section text,
page, source version, commencement/amendment reconciliation and selectable
attachment/reload proof remain required. The current seed still contains only
17 BNSS and 14 BNS catalogue entries; even catalogue completeness is missing.
BUG-010 remains open. Do not turn source-file recovery into a legal-data closure.

Fresh remote read on September 06 confirmed main still at `6a85cd5`; it does
not imply a fetched local remote ref. PR 402 remains draft, conflicted and
incomplete. The referenced other task is interrupted/not loaded, not actively
mutating this candidate. Judgment enrichment remains in its separate preserved
worktree and has not been integrated by this checkpoint.

The manifest has 144 slices, of which 25 are not marked implemented. Its
September 06 checkpoint records the local candidate; the earlier September 01
production evidence remains historical and is not current acceptance.
The April backlog contains stale descriptions of already shipped modules.
Do not implement duplicate drafting, Teams, audit, OpenAPI, notification,
private-index, or evaluation subsystems from those descriptions.

| Queue | Slices | Current classification | Required completion evidence |
| --- | --- | --- | --- |
| Shared completion | 027B, 028A/B, 029A/B | Partially implemented; existing workflow, data governance and ownership contracts need gap reconciliation | Mixed revision, restore/export, one-writer and compatibility-retirement proof |
| Trademark remainder | 039G/H | Partially implemented parent; unstarted completion allocations | Requirement-by-requirement audit of intake, instruction, costs, filing, lifecycle and shared owners |
| Domain labels | 079A/B | Partially implemented locally: strict server catalogue, machine-evidence reader/writer, landing and workspace status. Release evidence collector and complete report/claim parity remain pending | One server catalogue, fail-closed activation, UI/API/report/marketing parity |
| Patents | 080A/B | P1 family disclosure has local persistence, corrections/history, restricted document pins and mobile/desktop browser proof. Applications/parties/priorities and complete patent workflows remain missing; catalogue status is intake only, never authoritative automation | PAT-01..04, IP-SCOPE-01..10, UJ-29/39/40 and their exceptions; typed child PRD, immutable relationships, sources, access and browser proof |
| Pilot and recovery | 070A/B, 072A/B | Partially implemented platform foundations; parent completion is not verified | Migration exceptions, capacity, measured recovery and no-dual-send drills |
| Export/offboarding | 071A/B | Partially implemented dry-run and hold foundations | Tenant/client scope, active holds, cleanup, tombstone-on-restore and disposable-tenant recovery |
| Access governance | 073A/B | Missing campaign/emergency workflows; existing target-aware grants remain canonical | Expiry, independent post-review, cache/session revocation and zero standing residue |
| Other IP domains | 090A/B, 091A/B | Missing specialist workflows | Independent child contracts and type-specific journeys; no trademark-rule reuse |
| Cross-IP GA | 100A/B | Missing integrated completion evidence | All domain journeys, migration, security, performance, support and exact-release claim gates |

Slice numbers in this table have prefix `IPLF-`. Classification describes the
observed parent foundations and does not silently upgrade any manifest status.

## Ordered Execution

1. Make Codex ownership explicit and regress both former assignment names.
2. Implement 079 using the existing capability owner; define the patent child
   contract and tests before adding 080 typed persistence and workflows.
3. Reconcile and complete trademark/shared-owner gaps without duplicating
   existing services. Merge the preserved judgment-enrichment candidate through
   explicit diff review, serial migration reconciliation and local tests.
4. Complete data operations, access governance, recovery and other domain
   contracts with capability-scoped legal/source fail-closed behavior.
5. Run integrated fresh-image Docker PostgreSQL, API, web and Playwright;
   enumerate skips, preserve failures, update documents and public claims.
6. Publish only the validated canonical revision. Exact-main production
   verification is a separate gate, never inferred from local tests.

## Test Contract

### Family Intake Recheck

The first fresh-image run (`54c8998223448f1763e49de798ddbc1aa84216e9`, a
derived pre-commit identity, not a Git release) passed 135 PostgreSQL tests but
exposed a minutes-long 10,000-family join. Index existence was not sufficient:
the listing now admits an ACL-filtered page before loading exact current-version
keys. The rerun entirely inside Docker passed all 135 marked tests in 99.57s,
including the 5-second scale-query bound, migration refusal and stale-writer race.
Evidence: `C:/tmp/caseops-ram05sep-20260905/api-full-ip080-family-bounded-postgres.xml`.
The first browser run passed seven domain journeys and stopped both patent
journeys at an overly broad checkbox-width assertion. The wrapping label is the
native checkbox's click target; the input must still be visible. Full rerun is
required; these first patent runs are not acceptance evidence.

The first full frontend run exposed omitted root fixtures, missing route-page
tests and a Radix tab test using only a synthetic click. Those were corrected.
A second full run passed 852 tests but had four readiness-timing failures in
existing work areas. All 43 tests in those four modules then passed with one
worker, and the complete single-worker offline Docker rerun passed all 856 tests
in 163 files (516.88s). No application timeout or assertion was relaxed.
None of the prior failures is erased or treated as a production fix.

The second fresh build, runtime revision
`2bee6c9c940ea2f977c8db9b5ba7e48a3ebad412`, passed all 135 PostgreSQL tests in
154.74s and post-rehearsal index health. The browser run reached source upload,
pinning, download and exact byte-hash verification at both widths, then failed
closure because a second tenant's bootstrap replaced the owner's cookie.
Authentication intentionally prefers cookies over bearer headers. The second
tenant now uses its own request context. All nine domain/patent journeys passed
on rerun (one worker, zero retries, no skips). Screenshots were inspected.
The API terminal regression also now invokes the actual HTTP lifecycle route;
all 36 patent/shared-access tests passed in network-disabled Docker.

The then-pending delta was migration-installed PostgreSQL/SQLite append-only
version guards, refusal-retained trigger assertions, and catalogue intake-only
truth with the updated child-PRD hash. These do not exist in the earlier 2bee
images. Their final fresh-image result is recorded separately below.

Affected baseline modules: M02/M03 (Matter/document reuse), M04/M06/M07
(verified research/drafting/recommendations), M08 (shared deadlines/calendar),
M10 (billing lineage), M13/M14/M15 (access, administration and data governance).
The IP PRD supplies the specialist stories PAT-01..04 and IP-SCOPE-01..10.
New tests use IPLF-079-* and IPLF-080-* IDs and map each normal/exception path.
Required security checks: cross-company and restricted-record isolation,
terminal immutability, stale writes, access revocation, unpublished-invention
exclusion, no paid test requests and immutable source/version history.
Performance checks bound candidates, child rows, SQL and provider budgets.

The September 05 candidate remains unreleased: BUG-010's statutory content
gap and unresolved original court context are not resolved by this takeover.

## Local Implementation Checkpoint

- Ownership: WTD explicitly assigns all pending work to Codex. The manifest
  already had a Codex owner; the new guard covers both former-executor names
  while preserving historical and provider attribution. The active quality
  audit and future-reopen checklist now use the same owner and are included
  in the guard; their historical findings are not a fresh missing-work list.
- Domain claims: 11 domains share one public catalogue and authenticated
  readiness response. Existing machine-evidence storage/HMAC verification owns
  promotion; stale, tampered, wrong-release or incomplete evidence cannot enable
  beta/GA. No tenant-writable promotion endpoint was added.
- Patent P0: a domain mismatch cannot write trademark particulars/assets or
  interpret trademark prosecution. Existing Madrid registration/designation
  record types remain part of the trademark domain. Shared lifecycle, grants,
  documents and audit owners are retained.
- Typed P0 facts now separate patent family/application identities, kinds and
  phases; preserve original identifiers and filed party/address snapshots;
  require optimistic-concurrency tokens on correction/related commands; and
  bound independently paginated application and priority pages. Closed source
  unions pin the existing IpDocumentVersion or IpRegistrySnapshot owner and
  content hashes, never an invented source table or a client `verified` flag.
  Family disclosure contracts now connect to a tenant-composite migration,
  restricted canonical docket/asset, idempotent creation, append-only service
  corrections, exact document-version pins and separate browser pages. The
  focused offline Docker suite passed 92 family/contract/document/lifecycle
  tests after the access-order and bounded-query changes. Fresh-image PostgreSQL
  and dated Playwright acceptance of this vertical passed as recorded above. Application,
  party/priority persistence, graph cycles and duplicate reconciliation remain
  P1 obligations. Database immutable-version guards passed in the final fresh
  migration delta; they are not silently attributed to earlier images.
- Disclosure: unsupported domains and all documents linked to them are omitted
  before general discovery/pagination. Old private projections are denied on
  read. Mixed-domain documents remain internally downloadable by an authorized
  reader but cannot enter AI, portal, export or notification-content workflows.
  Portal reads, downloads and queued delivery revalidate current disclosure.
- Patent P1-P6: not complete. No patent application persistence, full
  prosecution, separate patent proceedings, claim history, maintenance/title,
  import/report or complete patent-domain journey is claimed by the family
  intake vertical. Shared closure is API-verified; a patent-specific lifecycle
  control/history surface remains an intake completion obligation.
- No commit, push, PR, merge or production mutation has occurred in this local
  checkpoint. Existing September 05 changes and the separate judgment-enrichment
  worktree remain preserved.

## Verification And Review Findings

Proof files are retained under `C:/tmp/caseops-ram05sep-20260905/`.
These runs overlap; counts must not be added into an invented total.

| Run | Result | Interpretation |
| --- | --- | --- |
| `api-full-ip079-corrected.xml` | 72 passed | Initial catalogue, capability, machine-writer and ownership checks |
| `ip079-browser-corrected` | 5 passed | Initial fresh-image public/mobile/unconfigured workspace and outage journeys; screenshots inspected |
| `api-full-ip080-domain-boundaries.xml` | 45 passed | First P0 isolation and adjacent trademark/document/lifecycle/portal checks |
| `api-full-ip080-adjacent-exact.xml` | 131 passed, 1 failed | Found excess document-policy queries; not green evidence |
| `api-full-ip080-disclosure-batched.xml` | 134 passed | Batched policy passes the unchanged query budget and mixed-domain portal tests |
| `ip080-fresh-docker.log` | Build/index checks passed; PostgreSQL 122 passed, 1 failed; browser not reached | Found incorrect exclusion of existing Madrid record types; not release acceptance |
| `ip080-postgres-madrid-corrected.xml` | 2 passed, 121 deselected | Corrected Madrid mapping and disclosure-before-LIMIT query on Docker PostgreSQL; focused only |
| `api-full-ip080-madrid-notifications-exact.xml` | 185 passed, 2 failed | Product paths passed; ownership guard rejected a descriptive sentence in this record. The sentence was corrected, not the forbidden-assignment rule |
| `api-full-ip080-ownership-final.xml` | 57 passed | Corrected ownership record, domain catalogue and disclosure boundary checks |
| `api-full-ip080-ownership-complete.xml` | 25 passed | Expanded ownership grammar plus active quality-audit and future-reopen instruction coverage |
| `ip080-web-unit.log` | 34 passed | Existing IP page plus domain component, including recovery when authoritative readiness arrives after failed public discovery |
| `ip080-web-full-final.log` | 837 passed in 159 files | Complete offline Docker frontend suite; no provider calls |
| `ip080-fresh-docker-corrected.log` | Fresh API/web build, index checks, 123 PostgreSQL tests and 7 Playwright tests passed | Local Docker fingerprint `df59595edbf4bc705470e8f2fbb84e91b550f5dca0ded3aeb48b7b54dfdde6e6`; not a Git commit or production revision |
| `ip080-browser-full-final.log` | 201 passed, 2 failed, 5 skipped | Complete 208-test desktop run: known BUG-010 plus a new test-boundary failure; not green acceptance |
| `api-full-ip080-full-final.xml` | 4,000 passed, 1 failed, 141 skipped | Complete offline API run; the unchanged timeout inventory caught the new component's direct fetch. This is a failed run, not a clean API result |
| `ip080-browser-full-corrected.log` | 199 passed, 4 failed, 5 skipped | Complete retained-state 208-test run: H702 fixture isolation, two demo rate-limit failures, and known BUG-010. Corrected workspace request counter passed |
| `ip080-isolation-retest.log` | 21 passed | H702 on retained corpus, all six then-current domain journeys and unchanged marketing tests; only the local web process was restarted to reset its one-hour demo limiter |
| `api-full-ip080-timeout-corrected.xml` | 60 passed | Corrected shared HTTP boundary, domain isolation/catalogue and ownership checks; original timeout guard unchanged |
| `ip080-web-full-deadline.log` | 842 passed in 159 files | Complete offline frontend run with shared JSON header/body deadlines and caller cancellation; expected unit-test API origin supplied explicitly |
| `ip080-fresh-docker-deadline.log` | Build/index checks, 123 PostgreSQL tests and 7 browser journeys passed | Fresh runtime fingerprint `17eba63665a44feeb1581e4021c26efdef1fcd49f5a3916750077d429b73e6cf`; all seven browser tests were domain journeys. A mistyped Madrid selector matched no test and is not Madrid evidence |
| `api-full-ip080-local-ci-coverage.xml` | 95 passed | Local/CI selection, deployment, timeout and ownership guards; all PostgreSQL-marked modules are now selected rather than one historical file |
| `ip080-all-marked-postgres.log` | 1 passed, 3 failed, 7 setup errors; stopped after 10 failures | Docker invocation supplied only the test DSN; migration runtime used inherited container-local localhost. Corrected by explicitly setting both DSNs to the isolated Docker database; no production database was touched |
| `ip080-all-marked-postgres-corrected.log` | 127 passed, 5 failed | Full 132-test PostgreSQL selection on retained state exposed partial downgrade commits and missing indexes; every newly included marked test passed, but the complete run was not green |
| `api-full-ip080-atomic-rollback-unit.xml` | 104 passed | Offline Docker migration, deployment, restore-forward and September 05 identifier guards; matching source-archive hash and source-status inventory retained |
| `ip080-fresh-docker-atomic-rollback.log` | 132 PostgreSQL tests, post-rehearsal index health and 12 Playwright tests passed | Fresh API/web images; seven domain journeys, the actual Madrid workflow and four mobile tests, one worker and zero retries |
| `api-full-ip080-postgres-atomic-repeat.xml` | 132 passed, 4,013 deselected | All marked PostgreSQL tests rerun inside Docker against retained state with the worker paused, both DSNs explicit and a clean post-test index-health check; worker restarted |
| `api-full-ip080-patent-contracts.xml` | 106 passed | First typed patent-contract run plus domain isolation/catalogue and ownership guards, network-disabled Docker; not patent workflow or persisted integrity acceptance |
| `api-full-ip080-patent-contracts-final.xml` | 111 passed | Final closed-object, source-pin, raw-identifier, party/address, strict-version/allocation and independent graph-pagination contracts plus adjacent isolation/catalogue/ownership checks; network-disabled Docker |
| `ip080-browser-after-rollback-repeat.log` | 12 passed | Same seven domain, one Madrid and four mobile journeys after the retained-state PostgreSQL rerun; current 4374833 images, one worker, zero retries and no skips |
| `api-full-ip080-family-final-focused.xml` | 92 passed | Family/contract/domain/document/lifecycle checks after bounded-page and access-order corrections; network-disabled Docker |
| `api-full-ip080-family-bounded-postgres.xml` | 135 passed | All marked PostgreSQL tests entirely in Docker in 99.57s, including 10,000-family five-second query bound; predates final immutable-version triggers |
| `ip080-family-fresh-docker-corrected.log` | 135 PostgreSQL passed; 7 browser passed, 2 failed | Fresh 2bee images and clean indexes; browser closure failed from shared tenant cookies, not counted as a passing run |
| `ip080-family-browser-isolated.log` | 9 passed | Seven catalogue plus two full disclosure/source/closure journeys on 2bee images, mobile and desktop, isolated tenant cookies, one worker, zero retries |
| `ip080-shared-access-http.log` | 36 passed | Public HTTP patent lifecycle and existing trademark access foundation/workflow/reassignment; network-disabled Docker |
| `ip080-family-full-web-serial.log` | 856 passed across 163 files | Complete offline Docker frontend suite with one worker; no skipped tests or loosened timeouts |
| `api-full-ip080-family-full-api.xml` | 4,066 passed, 2 failed, 144 skipped | Full API inventory caught missing literal route references and an incorrect per-column FK-index rule; not green evidence |
| `ip080-family-immutable-corrected.log` | 134 passed | Family/version immutability, catalogue, migration and adjacent domain checks; predates the final canonical source-link correction |
| `ip080-source-scope-inventory.log` | 126 passed, 2 failed | Canonical source-link/privacy and actual-schema index checks passed; two new unauthenticated POST assertions incorrectly expected 401 before the existing CSRF 403 boundary. Assertions now separately prove CSRF rejection and invalid-bearer 401 |
| `ip080-family-final-fresh-docker.log` | 135 PostgreSQL passed; 14 browser passed, 3 failed | 74f998 fresh images. BUG-010 remained open; destructive PostgreSQL test cleanup erased global court aliases and caused two bulk-import failures. This was not green acceptance |
| `ip080-source-scope-final-docker.log` | Fresh images and both index-health checks passed; host PostgreSQL run interrupted | Candidate d5bd35f. The Windows-host PostgreSQL run was deliberately replaced with complete in-container PostgreSQL execution; it is not a passing run and never reached browser execution |

### Source Scope And Test Isolation Findings

The first family source pin retained the exact document/version/hash but did not
create the existing canonical `IpDocumentLink`. A document previously linked only
to a trademark therefore remained eligible for general disclosure despite being
used as unpublished patent evidence. Family create/correction now acquires the
existing document/scope locks, requires `documents:manage` when adding a link,
uses the shared link writer, emits its audit and private access-change event,
and retains the link when later corrections remove the current source. Repeated
pins do not duplicate links or access events. Existing trademark particulars and
authorized internal downloads are preserved. API regressions cover both write
paths, permission rollback, replay and historical retention; IPLF-080B adds the
mobile browser journey for an existing trademark document used as a patent source.

The 74f998 browser failures exposed a separate verification defect, not a reason
to loosen court validation: `TRUNCATE companies, users CASCADE` in destructive
migration tests removed global `forum_catalog_aliases` through administrator
foreign keys. The parent court rows remained, so a schema/index-only check missed
the loss. Three destructive probes now own independently named local databases.
They compare the shared application's exact migration revision and full alias
identity/verification snapshot before and after, and require a nonempty baseline.
The removed shared reset must never be reintroduced as test cleanup.

The old FK inventory tested each component column of composite constraints in
isolation and accepted some declared migration indexes without inspecting the
migrated database. The replacement uses the existing release-health constraint
prefix rule against the actual migrated schema. A negative test drops the patent
document-version composite index and requires detection. No redundant standalone
indexes or acceptance waiver were added.

These findings and their prevention rules are recorded in repository `AGENTS.md`.
The separately referenced task `Review pending work` was checked on September 06:
it is inactive in another worktree, not a current competing writer. Its historical
release statements are not evidence for this candidate.

### Final Local Candidate And Data Replay

Current Docker project: `caseops-acceptance-2ba0658b3074`. API and web serve
`2ba0658b307465c87cbd593dd5d31b082c0ed53a`, derived from dirty-source fingerprint
`2ba0658b307465c87cbd593dd5d31b082c0ed53a918136a2e223264c0274d1d1`.
This is not a Git commit. These images include immutable-version triggers,
canonical restricted source linking, the scalar family-list query/cursor index,
and the regenerated data-governance runtime projection. Later browser assertion
and test-fixture changes are test-only; the results below identify their scope.

| Final or delta proof | Result | Scope and limitation |
| --- | --- | --- |
| `api-full-ip080-candidate-final-api.xml` | 4,076 passed, 0 failed, 144 skipped in 2,473.03s | Complete offline Docker API baseline with source-link/privacy fixes. Predates the final scalar-query/cursor-index delta and generated metadata; not a complete exact-current API run |
| `api-full-ip080-scalar-page-focused-corrected.xml` | 158 passed in 317.10s | Final family, contracts, domain, document, lifecycle, FK, manifest and migration delta in offline Docker; replaces an invocation that collected zero tests from a mistyped module |
| `api-full-ip080-scalar-page-postgres.xml` | 135 passed, 4,085 deselected in 568.82s | Complete then-marked PostgreSQL suite inside Docker, with adverse query-planner conditions and 5-second listing budget; clean post-test index health |
| `ip080-final-governance-index.log` | 45 passed in 56.80s | Regenerated 310-table/5,017-column governance map, runtime projection and index contracts |
| `ip080-family-full-web-serial.log` | 856 passed, 163 files in 516.88s | Complete offline frontend suite; frontend runtime unchanged since this run |
| `ip080-native-reranker.log` | 15 passed in 4.01s | Baked native local reranker, network disabled. Not Voyage production quality evidence |
| Windows shim test | 1 passed in 0.09s | Host-only `test_scheduler_inventory.py::test_gcloud_runner_resolves_windows_command_shim`; mocked process, no cloud calls |
| `ip080-governance-final-full-docker.log` | Fresh builds, two index checks and 135 PostgreSQL tests passed; browser 209 passed, 2 failed, 5 skipped | Full 216-test selection on final images, one worker and zero retries. Failures: real BUG-010 catalogue completeness and the source-name test assertion described below; this complete run is not green |
| `ip080-final-browser-source-corrected.log` | 15 passed in 2.1 minutes | Seven domain, two family, one source-scope, one Madrid and four mobile journeys on final images; one worker, zero retries, no skips |
| `api-full-ip080-all-142-postgres-corrected.xml` | 142 passed, 4,078 deselected in 262.75s | Complete expanded PostgreSQL suite inside Docker on retained app state, including bench, API report and notification-worker isolation; exact-image index health passed afterward at 512 MiB |
| `api-full-ip080-final-control-gates.xml` | 108 passed in 51.32s | Final offline Docker ownership, deployment, migration, actual-schema FK and adjacent SQLite court-sync gates after the fixture/document changes; no paid or network requests |
| `ip080-final-original-import/browser-replay.log` | 496 source identities checked; 391 created, 97 invalid, 8 duplicates; confirmation replay idempotent | Real local browser import using the user's test account and unchanged recovered source values; no production mutation |
| `ip080-final-original-import/local-all-data-evidence.json` | 10,416 original cells exactly equal; 391 records x 28 mapped fields verified | Full bounded list identity, tenant and active-state assertions; API build identity matches final images |

The retained final source archive is `source-ip080-governance-final.tar`,
49,643,520 bytes, SHA-256
`7aa7c66241bce40462afc4a38b6564f8a12ce704e7af850b99d79c4f7655115d`.
It predates the source-name assertion correction and later documentation/test
fixture edits, not a runtime change. The full 216-test traces/screenshots are
preserved in `ip080-final-216-artifacts/`. Mobile and desktop disclosure images
and the corrected 393px source-download image were visually inspected.

The source-scope failure was a test expectation mismatch: the shared document
panel correctly rendered the canonical controlled filename, not its title.
The corrected journey asserts that exact returned filename, the fully visible
download button and the SHA-256 of the actual downloaded bytes. No document
naming rule or product behavior was changed to satisfy the assertion. The
corrected 15-test slice does not erase the failure from the full 216-test run.

The original 144 API skips were 135 PostgreSQL nodes, seven bench/court nodes
incorrectly tied to SQLite, one native model node and one Windows shim node.
The PostgreSQL/native/Windows proofs above close those execution gaps at their
stated revisions. The seven bench/court probes now use independently migrated
PostgreSQL databases and the normal PostgreSQL marker. Their initial run passed
five and failed two: an old unknown-court fixture used an actually catalogued
court, and raw SQL used obsolete statute-reference column names. The corrected
test asserts a unique unresolved court and its retained review record, and
creates statute references through the canonical ORM with explicit parent
ordering. `api-full-ip080-bench-postgres-corrected.xml` passed all seven in
85.85s, including the actual court-sync/bench-strategy HTTP flow.

The first expanded retained-state run (`ip080-all-142-postgres.log`) passed
140 and failed two in 204.47s. A report fixture reused its fixed bootstrap slug
from a prior run. The global notification drain's bounded first page encountered
older fixtures before the intended hearing. The API report and global-worker
probes now also use their own migrated databases, without deleting app data or
increasing queue limits. The complete expanded rerun passed all 142 tests in
262.75s. The post-test exact-image index-health job reported no FK gaps,
missing/mismatched/invalid indexes or revision mismatch under a 512 MiB cap.
Its synthetic-workload sequential-scan warning on private generations is not a
missing-index finding. The application worker was restarted afterward.

The original ledger was freshly exported again after these database rehearsals.
`ip080-final-original-import/post-postgres-data-verification.log` proves all
10,416 cells, 391 saved records, 28 fields per record and complete list identity
still match. The earlier export and pre-rehearsal result are retained separately.
The updated Downloads workbook is
`CaseOps_Bug_Fix_Summary_Ram05Sep2026_Update06Sep2026.xlsx`: its five original
sheets retain 1,054 populated cells, with only two explanatory headings changed;
two new tabs record current Docker evidence and all 25 canonical pending slices.
Formula totals reconcile to 25 incomplete, 9 in progress and 16 not started.
No password or original source-row data was added to the report.
All changed Python files passed Ruff. Manifest, data-governance map and runtime
projection validation passed, as did `git diff --check`. Local HEAD, main and
the cached origin/main ref still point to the unreleased-work predecessor
`6a85cd5025101fd453bdc9b8472e963d16ba708a`; no remote refresh or publication
is implied by that local comparison.
Five browser skips remain unverified: four exact-production revision checks
and the provider-gated Pine Labs payment-link flow. No paid calls were used to
turn those skips into passes.

### Family Listing Repeated-Load Finding

`ip080-source-scope-postgres-final.log` retained a real 134-pass/1-failure run:
the first bounded-page implementation still exceeded the unchanged five-second
SQL budget on a fresh 10,000-family workload. A later diagnostic EXPLAIN of the
same query completed in 25.547ms; that later fast plan does not disprove the
earlier timeout or establish which failing plan PostgreSQL used.

The final query pages the tenant's indexed family IDs and uses one correlated,
bounded docket/ACL lookup per candidate, then fetches at most the admitted
dockets and exact version keys. Migration and models add the `(company_id, id)`
cursor index. Permission changes between reads fail closed. The PostgreSQL
regression disables hash and merge joins as an additional adverse-plan check
and retains the five-second SQL timeout. Both the complete in-container run and
the final fresh-stack PostgreSQL run passed. No application, database or test
timeout was raised. Governance/index fingerprints were regenerated before the
final image build, not patched into an already tested image.

The earlier `ip080-fresh-docker-corrected.log` seven-test run contains the
dated IPLF-057B Madrid journey and six IPLF-079B journeys. They cover public widths 360/768/1280, an unconfigured
workspace, outage/recovery, and configured workspace navigation/reload at
393/1280. The mobile and desktop availability screenshots were inspected.
This does not constitute the missing patent family/prosecution journeys.

Two earlier regressions introduced during this local work were caught before Git or
production. Root causes: confusing a record discriminator with a product domain,
and repeating linked-document authorization instead of batching it. Both now have
explicit regression coverage; permanent instructions preserve the lessons.

The broader browser run also exposed harness issues. The configured-workspace
request counter started before the sign-in helper's public landing discovery
finished: trace time 693650.243 for the request preceded `/app/ip` navigation at
693698.115. The test now finishes landing discovery before counting and still
requires zero supporting workspace requests. A rerun on the retained corpus
then exposed the older H702 unreadable-text test's unfiltered shared-corpus
assumption: unrelated judge fixtures displaced its source. That probe now keeps
its original query but scopes a uniquely identified synthetic court fixture and
asserts one raw candidate, one unreadable omission and zero rendered results.
No application ranking rule or quality threshold was changed to satisfy it.

The second broad run exhausted the existing five-request/hour demo limiter in
the retained local web process (HTTP 429). The unchanged marketing suite passed
after restarting that process. The limiter was not disabled or relaxed. A
focused unit invocation also inherited the build image's API origin at port
5100 instead of the unit suite's expected localhost:8000; its three auth URL
assertions failed. The complete 842-test run used the documented unit origin
and passed without changing those assertions.

The API inventory failure was a real implementation convention gap: the new
public component bypassed the shared request helper. It now uses the shared
JSON consumer, whose deadline and caller cancellation cover both headers and
the response body. Existing request callers keep their prior contract. Three
client tests, two component cases and a dated browser timeout/retry journey
were added; no silent retry, credentials or supported-domain claims were added.

Skip reconciliation found that both CI and `verify-docker.ps1` selected only
`test_postgres_validation.py`, omitting nine marked tests in three other
modules. Both now use the full `-m postgres` selection, with a regression guard.
The original 141 API skips comprise 132 PostgreSQL-marked nodes, seven
bench/court tests still bound to a SQLite fixture, one native reranker test and
one Windows-only gcloud shim test. Passing the 123-test subset never proved
the other nine PostgreSQL nodes or these nine remaining platform/dialect nodes.
The five broad-browser skips are four exact-production checks and the
provider-gated Pine Labs payment-link scenario; they are not local acceptance
passes. Regular regression does not spend provider credits to close that gate.

### Rollback Defect Found By Repetition

The previous 123-test PostgreSQL subset could finish green while its blocked
downgrade rehearsal left missing indexes. Its assertion checked retained
idempotency evidence and an ancestor revision, not exact head/schema/index
equality. Repeating the broader selection then failed on the missing trademark
family index, followed by temporary-identifier column/index failures and a
second refusal failure. These were not failures of the nine newly admitted tests.

Root cause: five index downgrade functions used independent autocommit blocks.
Those committed earlier reversals before the shared-reliability migration
correctly refused to discard retained evidence. A subsequent upgrade from the
partially advanced revision could not repair every independently dropped index.
The five affected paths are conflict-search trigrams (20260708_0001), authority
search trigrams (20260811_0005), trademark family index (20260822_0001), tracked
operation uniqueness (20260904_0001), and temporary Matter identifiers
(20260905_0002). All now keep downgrade DDL in the enclosing transaction;
upgrades retain concurrent builds and the existing migration lock budgets.

The strengthened PostgreSQL regression attempts the refused downgrade twice
and compares the exact head, all column definitions, every index definition and
valid/ready state, plus retained evidence. An AST guard rejects future explicit
downgrade commits, and the local verifier runs index health after migration
rehearsals before restarting the worker. This is a reproduced local rollback
defect, not proof that production ran a downgrade or that it caused a specific
historical reopening. Prior failed stacks were stopped with volumes preserved.

The full API run's source snapshot is retained as `source-ip080-full-final.tar`,
SHA-256 `94715bb45e29b35788140d1c32cb228035cdaa770ac00c860814c1d7da2d9787`.
Fresh integrated acceptance of the corrected P0 source passed locally; the
broader failed runs remain recorded above. A focused correction is not a newly
green complete run. The historical P0 retained app was at `http://127.0.0.1:43697`,
with API proxy 43696 and PostgreSQL 43698. Runtime fingerprint:
`4374833a1e1c8d476b561c8003ea404935de2721923dc82c3eb18511325c5b0c`.
This is a dirty-source fingerprint, not a Git commit. API/web image suffix:
`4374833a1e1c8d476b561c8003ea404935de2721`. Its twelve selected browser tests
include the five-second domain deadline, explicit recovery, actual Madrid
workflow and mobile controls. Both the fresh and retained database runs prove
all 132 marked tests and no missing/invalid indexes after rollback rehearsals.
That P0 runtime and the later 2bee/74f998/d5bd35f/cb3ed stacks are stopped,
with their volumes preserved. The current final family-disclosure app is
`http://127.0.0.1:35547`, API proxy 35546, API 35545, PostgreSQL 35548 and
Valkey 35549. The user's local `test-legal` account has its 391 imported Matters.
Unrelated workstation applications and their volumes were not cleaned up.
Full backlog acceptance and production verification remain pending: NO-GO.
There has been no commit, push, PR, merge or production mutation in this work.
